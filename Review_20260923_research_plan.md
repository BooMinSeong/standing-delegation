# standing-delegation 연구 계획 리뷰

2026-09-23 · @Someone

## 1. 총평

**판정: 논제와 계측 설계는 투고 수준에 올라와 있다. 지금 가장 큰 위험은 아이디어가 아니라 규모와 검정력이다.** 이대로 가면 ARR 1월에 "주장이 많고 각 주장의 증거가 얇은 논문"으로 읽힐 가능성이 높고, 범위를 좁히면 main track을 노릴 만하다.

읽은 범위는 `Plan.md` 전부, `docs/`의 CASE-D01, GEN-ALGO, B2-D01, M1-D01, INTRO, DECISIONS(D-039\~D-041), 9/22 외부 리뷰, 러너와 환경 코드, `data/runs/`의 원자료다. 9/22 리뷰의 권고(𝒬 해석 집합, 쌍의 전이로서의 영역, B\_indiff, 노출 1급화, 강한 B0, 원시 트레이스 비교군, C1b, CLAIM 정정)는 D-039\~D-041로 이미 반영되어 있으므로 이 문서는 그것을 반복하지 않고 **반영 이후에 새로 보이는 문제**만 다룬다.

### 강점

- **정답 R 없이 채움을 정의했다.** 최소쌍 의존과 Q 기준 영역으로 "저자가 정답을 심었다"는 순환을 절차로 끊었다. 이 분야에서 가장 방어하기 어려운 지점을 먼저 막은 설계다.
- **통로 비교가 같은 상태 위에 선다.** B2a, M1, P2가 상태 집합을 공유하므로 "말과 행동의 격차"가 상태 출처와 섞이지 않는다.
- **자기 증거의 자격을 스스로 깎는다.** CASE-D01 §8, 봉투 밖 편집 비율, 𝒬 최소 대 전체 민감도처럼 약점을 수치로 노출하는 장치가 많아 심사자의 신뢰를 산다.
- **실측이 이미 논제를 부분적으로 지지한다.** M1에서 GLM의 CLAIM 6/40(발주 없이 발주 보고)과 가용량 초과 발주 6/40은 텍스트 통로에서 볼 수 없는 행동이고, R5의 목적 재해석이 모델마다 갈린다는 관측은 "관습적 기본값이 아닌 자리"의 첫 실물이다.

### 이 리뷰가 짚는 네 가지

| # | 문제 | 왜 치명적인가 | 절 |
| --- | --- | --- | --- |
| 1 | 주장 넷, arm 여섯, 인간 연구 둘, 척도 열다섯을 16주 안에 한 논문에 담으려 한다 | ARR 8쪽에 들어가지 않고, 10/9 게이트까지 구현할 산출물이 다섯이다 | §3.1 |
| 2 | 편집 k=5, 기준 k≈15에서 BH를 걸면 정책 확률 0.5 변화의 검출력이 10\~22%다 | 자리 수가 거의 결정적인 뒤집힘만 세고, 모델 간 비교가 안정성에 교란된다 | §3.2 |
| 3 | 자리 수에 "환경이 답을 정하는 제약"과 "주인만 아는 선호"가 섞인다 | ACCORD와의 구분이 인간 기준선 하나에만 기대고, C1의 헤드라인 수치가 부풀 수 있다 | §3.3 |
| 4 | held-out이 발견용 상태와 같은 분포(단일 편집)에서 나온다 | 정책표의 예측 타당도가 구성상 높게 나와 "정책을 드러냈다"의 증거가 되지 못한다 | §4.1 |

데이터 생성 알고리즘은 방향이 옳고 규칙이 잘 닫혀 있다. 부족한 것은 **발견한 의존을 규칙으로 바꾸는 단계**(경계 국소화)와 **정책이 일반화하는지 확인하는 단계**(조합 검증)이며, §4에서 구체적인 절차와 의사코드를 제시한다.

## 2. 논제와 기여 검토

**문제 정의는 선명하다. 다만 논문의 정체가 벤치마크, 방법, 분석 세 가지를 동시에 주장하고 있어서 심사자가 무엇을 기준으로 평가할지 정하기 어렵다.** 하나로 고르고 나머지는 그것을 받치는 자리로 내리는 편이 낫다.

### 2.1 문제 정의

"무인·비가역·반복"이라는 세 조건이 abstain 처방을 무력화한다는 출발점은 설득력이 있다. "에이전트가 틀렸다"가 아니라 "주인이 모른다"로 프레이밍한 것도 좋다. 이 프레이밍 덕분에 모든 모델이 같은 채움으로 수렴하는 경우까지 문제로 잡히고, 이것이 ACCORD류의 "합리적 복원" 연구와 갈리는 지점이다.

보강할 고리는 둘이다.

- **모델 교체가 정책을 조용히 바꾼다는 동기를 서론에 넣을 것.** C1의 "모델 간에는 갈린다"는 지금 현상 기술로만 쓰이는데, 실무 관점에서는 이것이 가장 강한 동기다. 상시 위임은 몇 달씩 돌고 그동안 백엔드 모델이 바뀐다. R5에서 CASE 응답자는 NONE, Qwen과 GLM은 100을 발주했다는 관측은 "같은 지시, 다른 모델, 다른 재고 정책"의 실물이다. 정책표를 모델 판 간 회귀 검사로 쓰는 용도를 한 문단 넣으면 비준이 왜 반복 비용을 감수할 만한지가 자연스럽게 설명된다.
- **상시성의 증거를 C1b 그림 한 장에만 맡기지 말 것.** 최소쌍 계측은 구조상 단발 무대다. 이력 편집(미입고 발주 × status)이 상시성을 최소쌍 안으로 끌어오는 유일한 통로이므로, 이력 차수를 시드 무작위가 아니라 **모든 위임에 필수 할당**으로 두고 결과를 별도 표로 보고하기를 권한다. 현재 할당표는 이력을 "전부"로 두었지만, 환경 패치 둘(실행 시각, 자기 커밋 읽기 툴)이 없는 환경에서는 이 항목이 조용히 빠질 수 있다. 씨앗 필터에 "자기 커밋을 읽는 툴을 패치로 더할 수 있는가"를 다섯 번째 조건으로 넣는 것이 안전하다.

### 2.2 기여의 정체를 하나로

| 후보 정체 | 중심 주장 | 심사자가 요구할 것 | 지금 계획의 충족도 |
| --- | --- | --- | --- |
| 벤치마크 | C1 | 위임·환경 수, 모델 수, 공개 자산 | 위임 14, 출처 1, 모델 4\~5. 벤치마크로는 작다 |
| **방법** | **C2 + C3** | 강한 비교군, 절제, 일반화 검증 | 강한 B0, 기계 v1 절제, 심은 자리가 이미 있다. 일반화 검증만 비어 있다(§4.1) |
| 분석 | C1 + C2 | 현상의 크기와 원인 분해 | M1′ 노출 분해가 있으나 표본이 작다 |

권고는 논문의 정체를 **배포 전 정책 추출 방법과 그 계측 프로토콜**로 고르는 것이다. 그러면 C1은 "방법이 잡을 대상이 실재한다", C2는 "다른 통로보다 더 잡는다", C3는 "생성기가 그 이득의 원천이다"로 한 줄에 엮이고, C4는 "드러난 정책이 예측에 쓸모 있다"는 타당도 검사로 좁혀진다. 범위 조정의 구체안은 §3.1에 있다.

### 2.3 선행연구 대비 위치

정찰 문서가 충실해서 ACCORD, LogiSafetyGen, NLSI, AgentAbstain, Trace2Policy와의 구분은 이미 서 있다. 빠진 계보가 하나 있다. **선호 유도(preference elicitation) 계열**이다.

- [Li, Tamkin, Goodman, Andreas, *Eliciting Human Preferences with Language Models* (GATE), arXiv:2310.11589](https://arxiv.org/abs/2310.11589)는 모델이 "정보량이 큰 경계 사례를 합성"해 사용자의 의도를 끌어내는 틀을 제안했다. 이 논문의 비준 단계는 구조적으로 같은 일을 한다. 차이는 보여 주는 것이 **질문이나 가상의 경계 사례가 아니라 에이전트가 실제로 한 행동**이라는 점이고, 그래서 주인은 자기 선호를 처음부터 말할 필요 없이 드러난 정책에 대해 예/아니오만 하면 된다.
- [Tamkin, Handa, Shrestha, Goodman, *Task Ambiguity in Humans and Language Models*, arXiv:2212.10711](https://arxiv.org/abs/2212.10711)는 과제 모호성을 사람과 모델에서 나란히 재는 AmbiBench를 냈다. 인간 발산 기준선의 선례로 인용할 수 있다.

둘을 넣으면 "상황 생성 + 비준"이 갑자기 나온 장치가 아니라 선호 유도의 행동 기반 변종이라는 자리를 얻는다. 인용 전에 `docs/related/`의 정찰 절차로 판본과 게재처를 확인하기 바란다.

## 3. 핵심 문제점과 개선 방향

네 가지를 문제 정의와 해결 방향으로 나눠 적는다. 3.1과 3.2는 10/9 게이트 전에 정해야 하고, 3.3과 3.4는 생성기를 짓기 전에 정하면 비용이 거의 들지 않는다.

### 3.1 범위가 투고 일정과 지면을 넘는다

**문제.** 계획은 주장 넷, arm 여섯(B0, B2a, B2b, M1, M1′, P2), 인간 연구 둘, 척도 열다섯, LLM 자리 아홉, 감사 수치 일곱을 담는다. D-039 이후 본 실험 규모는 1.6배가 되었고, `docs/GEN-ALGO.md`는 아직 구현이 없다. 10/9까지 16일 안에 최소쌍 판독기, Q 추출(게이트 넷과 𝒬 포화), 영역 판정기, 노출 기록기, 생성기를 짓고 롤아웃 약 2,750회를 돌려야 한다. 설령 일정을 맞추더라도 ARR 본문 8쪽에 C1\~C4와 인간 연구 둘의 결과를 실을 수 없고, 부록으로 밀린 주장은 심사에서 증거로 치지 않는다.

**해결 방향.** §2.2의 정체(방법 + 계측 프로토콜)에 맞춰 아래처럼 자른다.

| 구성 요소 | 처리 | 이유 |
| --- | --- | --- |
| C1 현상·안정성, 조회 행 | 본문 유지 | 방법이 잡을 대상의 실재 |
| C2 B0(강한 판), B2a, M1 | 본문 유지 | 논문의 중심 비교 |
| C3 생성기 세 조건 | 본문 유지(방법 절) | 이득의 원천 |
| C4 예측 타당도(모델 판정 + 소규모 인간) | 본문 유지 | "정책을 드러냈다"의 직접 정의 |
| 패치 후 새 자리 수 | 본문 유지 | 자동 계산이고, §0의 "미리 닫을 수 없다"를 받친다 |
| 인간 발산 기준선(10\~20명) | 유지, 10월 | ACCORD 구분의 실증. 단 §3.3의 자동 분할이 부담을 나눈다 |
| C1b 연속 실행 | 그림 한 장 | 계획 그대로 |
| B2b | 부록 | 이미 측정됨. 한 컨텍스트 효과는 본문 주장이 아니다 |
| M1′ 노출 분해, P2 | 파일럿 위임 4 × 모델 2, 분석 절 | 원인 분해는 보조 주장 |
| 비준 연구(12\~20명, 4조건) | 예비 시행 3\~6명까지만, 본시행은 후속 | 4조건 피험자 내 설계는 16명으로 검정력이 부족하고 일정의 최대 위험이다 |
| 런타임 가드 반영 | 후속 | §3.4의 정의가 있으면 부록에 한 표로 충분하다 |

10/9 게이트 자체도 순서를 바꾼다. 다섯 산출물을 위임 넷에 동시에 걸지 말고, **D01 하나로 파이프라인을 끝까지 한 번 통과시킨 뒤**(10/2 목표) 나머지 셋에 펼친다. D01은 규칙을 만들 때 본 개발 데이터이므로 게이트의 "생성기 ≥ 저자 설계의 절반"은 나머지 셋에서만 잰다.

### 3.2 의존 검정의 검출력이 낮고, 그 방향이 문서의 예상과 반대다

**문제.** 계획은 편집 k=5, 기준 k√m(≈15)에서 Fisher 정확 검정을 하고 위임 × 모델 안에서 BH(FDR 0.10)를 건다. 가장 유리한 경우(결과를 이진으로 굵혔을 때)를 단순 시뮬레이션으로 계산하면 아래와 같다. 유의수준 0.006은 편집 17개 중 참 의존이 하나일 때 BH의 첫 문턱(0.10 × 1/17)이다. 반복 4,000회.

| 기준 n / 편집 n | 기준의 최빈 행동 확률 | 확률 변화 0.3 | 0.5 | 0.7 |
| --- | --- | --- | --- | --- |
| 15 / 5 (현재 계획) | 0.95 | 0.04 | 0.22 | 0.57 |
| 15 / 5 (현재 계획) | 0.68 (GLM M1 수준) | 0.03 | 0.10 | 0.25 |
| 15 / 10 | 0.95 | 0.13 | 0.47 | 0.84 |
| 15 / 10 | 0.68 | 0.07 | 0.33 | 0.82 |
| 20 / 20 | 0.95 | 0.20 | 0.74 | 0.99 |
| 20 / 20 | 0.68 | 0.12 | 0.60 | 1.00 |

현재 설계는 **정책 확률이 거의 완전히 뒤집히는 자리만** 잡는다. 부분적 의존(확률이 0.9에서 0.4로 내려가는 정도)은 대부분 놓친다. 더 중요한 것은 교란의 방향이다. `docs/GEN-ALGO.md` §18은 "불안정한 모델일수록 자리가 많고 발산이 커 보인다"고 적는데, 이것은 최빈 비교를 할 때의 이야기다. Fisher와 BH로 위양성을 통제하면 불안정한 모델은 검출력이 떨어져 **자리가 적게 잡힌다.** 그러면 "모델 A가 모델 B보다 자리가 많다"는 비교가 정책이 아니라 안정성의 차이를 잰다. 심은 자리 회수율도 같은 검정을 거치므로 같은 편향을 받는다.

**해결 방향.**

1. **편집 k를 10으로 올린다.** 파일럿(위임 4 × 편집 11 × 모델 3)에서 추가는 660런이고 핵심 1,020런 대비 약 65% 증가다. §3.1에서 덜어낸 arm이 그 예산을 낸다.
2. **자리 정의를 유의성에서 효과 크기로 옮긴다.** 조대화한 결과 분포 사이의 총변동거리(TV)를 추정하고, 사전 등록한 δ(예: 0.3)를 넘는지로 자리를 판정한다. Fisher와 BH는 발견 단계의 1차 걸러내기로 남기고, 보고하는 수치는 TV의 부트스트랩 CI다. 모델별로 검출 가능 최소 효과(MDE)를 같은 표에 적으면 모델 간 자리 수 비교가 같은 자로 재는 것이 된다.
3. **위임과 모델을 묶는 주장은 계층 모형으로 낸다.** "3차 변수의 SILENT 편집이 일반적으로 의존을 낸다" 같은 C1의 일반 주장은 쌍별 검정의 합이 아니라 혼합효과 로지스틱(편집 종류 고정효과, 위임·모델·환경 무작위효과)으로 낸다. 부분 풀링이 검출력을 돌려주고, 이미 계획한 위임·환경 군집 구조도 자연스럽게 들어간다.
4. `docs/GEN-ALGO.md` §18의 교란 문장을 "검정을 통제하면 불안정한 모델의 자리는 과소 검출된다"로 고친다.

### 3.3 자리에 환경 제약과 주인의 선호가 섞인다

**문제.** 영역은 q의 문자만으로 계산한다. 그래서 R7(최저가 업체에 "200캔 이상만 접수")은 SILENT가 되고, 에이전트가 그 업체를 건너뛰면 "끼워 넣은 조건"으로 자리에 들어간다. 그런데 현실에서 그 발주는 업체가 거부한다. 이것은 주인만 아는 자리가 아니라 ACCORD가 말하는 "환경이 답을 정하는 자리"다. 환경이 가용량과 제한을 검사하지 않아 과발주가 성공으로 남는 것(`data/env_patches.md`)도 같은 혼동을 키운다. 지금 이 둘을 가르는 장치는 인간 발산 기준선 10\~20명 하나뿐이고, 그것마저 "사람이 갈리지 않는 자리"를 접을 뿐 환경 제약인지 관습인지는 가르지 못한다.

**해결 방향.** 세계 제약 W를 생성 시점에 **구성으로** 기록한다. 판단이 필요 없다. 생성기가 자유 텍스트에 조건을 써넣을 때(§9) 이미 그 술어를 알고 있고, 계약 만료일·최소 주문량·위조품 표시처럼 환경 의미가 거부를 말하는 편집은 생성기가 쓴 것이므로 술어를 같이 내보낼 수 있다.

- 영역을 두 번 계산한다. Q 위에서 한 번, Q∧W 위에서 한 번.
- 둘 다에서 자리인 것은 **주인 자리**, Q에서는 자리지만 Q∧W에서 CONTROL이 되는 것은 **세계 제약 자리**로 나눠 보고한다. R7은 뒤쪽, R5(재고 450)는 앞쪽이다.
- 가용량이 발주 상한인가(M1 자리 6)처럼 환경 문서가 정하지 않는 것은 **W 미정**으로 두고, 인간 발산 기준선이 정한다.

이렇게 하면 ACCORD와의 구분이 인간 연구에만 기대지 않고 자동 분할이 된다. 인간 기준선은 "세계 제약 자리에서 사람 간 불일치가 낮고 주인 자리에서 높다"는 예측의 검증으로 역할이 바뀌고, 그 예측은 사전 등록할 수 있다. C1의 헤드라인은 주인 자리 수로 쓴다.

### 3.4 분포인 정책에 대해 승인이 무엇인지 정의되어 있지 않다

**문제.** 실측에서 GLM의 R2는 5표본 5답, Qwen의 R8은 분할 3/5 대 단일 100 2/5다. 계획은 이런 셀을 분포로 적겠다고 했다. 그런데 주인이 "분할 60%"를 승인한다는 것은 무슨 뜻인가. 승인 기록(수락·금지·지정)이 행의 최빈 행동에 붙는지 분포 전체에 붙는지 정해져 있지 않고, 그래서 "승인 후 이탈률"과 "패치 후 새 자리 수"의 계산식도 비어 있다. 법률 유비의 §4.07(행위 전체성) 한계도 같은 뿌리다.

**해결 방향.** 승인을 **행마다 허용 행동 집합을 정하는 것**으로 정의한다. 수락은 관측된 행동들을 허용에 넣고, 금지는 빼고, 지정은 하나로 좁힌다. 그러면 승인 기록 전체가 하나의 프로그램 Q\_owner = Q ∧ (주인이 더한 술어들)이 되고, 이미 지어 둔 장치가 그대로 재사용된다.

| 용도 | 계산 |
| --- | --- |
| 승인 후 이탈률 | held-out에서 행동 ∉ Q\_owner(s)인 런의 비율 |
| 런타임 가드 | commit 직전에 Q\_owner를 실행해 밖이면 차단 |
| 지시문 패치 | 더한 술어를 문장으로 옮긴 q′ (역파싱으로 되받음) |
| 패치 후 새 자리 수 | q′로 생성기를 다시 돌려 Q\_owner 기준 영역에서 새로 나온 FILL·OPEN·SILENT 자리 |

"분할이든 단일 100이든 괜찮다"는 허용 집합 {분할, 단일 100}이고, 분포인 셀도 그대로 승인할 수 있다. 승인 대상이 행위가 아니라 규칙이 되므로 ratification보다 actual authority의 사전 수여에 가까워지고, 용어 선택도 다시 볼 여지가 있다.

## 4. 데이터 생성 알고리즘 조언

**현재 생성기(변수·변형, Q 기준 영역, 할당 동결)는 의존을 발견하는 장치로는 잘 설계되어 있다. 빠진 것은 발견 뒤의 두 단계(규칙으로 바꾸기, 일반화 확인하기)와 동률 상태의 교란 통제, 그리고 관측 층의 통제된 편집이다.** 아래 다섯 가지 개정은 전부 저자 판단을 다시 들이지 않는다(D-040 유지).

### 4.0 현재 파이프라인 진단

| 단계 | 잘 된 점 | 남는 문제 | 개정 |
| --- | --- | --- | --- |
| 범위(1·2·3차·이력) | 툴 응답이 정하고 관련성 판단이 없다 | 3차 변수의 노출률이 낮다(재고: Qwen 12/40, GLM 4/40). 노출률 × 정책 변화가 실효 효과라서 M1에서는 검출이 사실상 불가능하다 | G4 |
| 기준 셋 | 덮어쓰기와 동률 깨기를 분리했다 | 쌍둥이의 목록 순서와 ID 순서가 동률 깨기 규칙과 교란된다 | G3 |
| 값 변형(삼분법) | 편집 공간이 닫혀 있다 | q 상수와의 관계만 한 점씩 찍는다. 모델의 임계가 q 상수와 다르면 규칙을 적을 수 없다 | G2 |
| 할당·동결 | 사후 선택을 막는다 | held-out이 발견용과 같은 단일 편집 분포에서 나온다 | G1 |
| 자유 텍스트 조건 | 역파싱으로 되받는다 | 쓴 조건이 세계 제약인지 기록하지 않는다 | G5 |

### 4.1 G1: 발견용 상태와 검증용 상태를 다른 분포에서 뽑는다

**문제.** held-out은 "생성기가 낸 상태 중 어떤 통로도 보지 않은 부분집합(위임당 4)"이다. 같은 단일 편집 분포에서 나오므로 정책표에는 거의 항상 그 편집과 같은 변수의 행이 있고, 예측은 표를 조회하는 일이 된다. 예측 타당도가 높게 나와도 "상태를 넘어 유지되는 규칙"의 증거가 되지 못한다.

**방향.** held-out을 위임당 8개로 늘리고 두 종류로 나눈다.

- **조합 상태 4.** 발견 단계의 단일 편집 둘을 한 상태에 합친 것(예: 재고 450 + 쌍둥이 모두 가용량 60). 각 편집은 표에 있지만 조합은 없으므로, 표의 규칙이 **합성되는가**를 묻는다. 두 편집은 시드 무작위로 고르되 둘 다 자리로 잡힌 것끼리의 쌍에서 뽑는다(발견 뒤에 정해지는 것이 아니라 규칙으로 정해진다).
- **무작위 유효 상태 4.** 영역을 겨냥하지 않고 §15의 제약 넷만 만족하도록 닻에서 도달하는 값을 무작위로 돌린 상태. "실제 운영에서 오는 상태"의 대리이고, 표가 덮지 못하는 영역이 얼마나 자주 오는지를 잰다.

보고는 단일 편집 예측률, 조합 예측률, 무작위 예측률 셋을 나란히 적는다. 단일과 조합의 차이가 작으면 "정책"이라는 말이 서고, 크면 정책표는 사례집이다. 이 값 자체가 C1의 "안정적"이라는 주장에 대한 가장 직접적인 증거다.

### 4.2 G2: 발견한 자리는 경계를 국소화해 규칙으로 적는다

**문제.** 삼분법은 q 상수와의 관계(미달·동률·초과)를 한 점씩 찍는다. 재고 450에서 발주를 건너뛴 모델이 "재고 ≥ 100"을 쓰는지 "재고 ≥ 400"을 쓰는지는 이 한 점으로 구분되지 않는다. 주인이 승인해야 하는 것은 "재고가 많으면 건너뛴다"가 아니라 "얼마부터 건너뛰는가"다.

**방향.** 발견 단계에서 자리로 잡힌 전순서 변수마다, 기준 값과 편집 값 사이를 이분 탐색해 최빈 행동이 바뀌는 임계 τ̂를 찾는다.

- 탐침 하나에 k=5, 탐침 4\~5회(값 범위를 1/16\~1/32로 좁힘). 자리당 20\~25런이다.
- 출력 행은 "재고 ≥ τ̂(구간)이면 발주하지 않는다"의 형태가 된다.
- τ̂를 q 상수와 비교한다. τ̂ ≈ q 상수이면 규칙이 q에 상대적이고, 아니면 모델이 **자기 상수를 지어낸 것**이다. 뒤쪽은 §4.2 계획의 "지어냄(q에도 툴 출력에도 없는 값)"이 행동 수준에서 나타난 것이고, 보고로는 거의 드러나지 않을 종류다.
- 순서 불일치 자리(R4의 단가 대 리드타임)는 한 변수를 고정하고 다른 변수의 간격을 이분 탐색해 교환비("리드타임 1일은 단가 $x와 맞바꾼다")를 낸다. 사전식 순서인지 가중합인지가 여기서 갈린다.

국소화는 적응적이므로 **의존 검정에는 쓰지 않고 표의 기술에만 쓴다.** 계획의 "적응 단계(선택)"를 이 절차로 구체화하면 된다. Daikon 계보에서 보면 불변식(의존의 존재)에서 술어의 상수(임계)까지 가는 단계다.

### 4.3 G3: 동률 상태는 순서와 ID를 균형 배치한다

**문제.** B\_many, B\_indiff, FILL 편집에서 가장 있을 법한 동률 깨기 규칙은 "목록에서 먼저 나온 것"과 "ID가 작은 것"이다. 실측에서 GLM은 B2b의 R2 동률에서 021(ID 순)을 4/5 골랐다. 지금 계획은 순서를 시드로 섞고 ID를 고정 풀에서 뽑는다. 이것은 위치 규칙을 **평균으로 지울 뿐** 재지는 않는다. 표면 변형 둘도 위임 부분집합에만 걸려 있다. 게다가 모든 필드가 같은 쌍둥이는 "중복 레코드"로 읽혀 제도 다른 행동(하나로 합치기, 데이터 오류로 보고)을 낳을 수 있다.

**방향.**

- 동률이 있는 모든 상태에서 k를 2 × 2로 나눈다. (후보 A가 먼저 / B가 먼저) × (A의 ID가 작음 / B의 ID가 작음). k=8 또는 k=12면 칸마다 같은 수가 들어간다.
- 이렇게 하면 "동률이면 먼저 나온 것", "동률이면 ID가 작은 것"이 **정책표의 자기 행**이 되고, 필드 기반 동률 깨기와 분리된다. 주인 입장에서 "목록 순서로 고른다"는 가장 거부하고 싶을 정책 중 하나다.
- 쌍둥이의 식별자 필드(`supplier_name`, 연락처, sku)는 서로 다른 그럴듯한 값으로 채운다. §10이 이미 식별자를 변수 범위에서 뺐으므로 최소쌍의 한 자유도 원칙을 깨지 않는다.
- §3.2의 k=10과 합치면 추가 비용이 거의 없다. 이 균형 배치는 표면 변형과 달리 보류 항목이 아니라 **동률 상태의 기본값**이어야 한다.

### 4.4 G4: 관측 층에 통제된 조회 실패를 주입한다

**문제.** 정책표의 조회 행("확인에 실패하면 무엇을 하는가")은 지금 자연 발생한 실패에서만 나온다. GLM의 `read_inventory` 실패 8/10은 인자 이름 실수이고, `"Coke"` 검색 실패는 질의어 선택의 결과다. 둘 다 모델 능력에 묶여 있어서 모델마다 다른 실패를 격고, "실패 시 정책"을 모델 간에 비교할 수 없다. 또 3차 변수는 노출률이 0.1\~0.3이라 정책 변화가 1이어도 실효 효과가 0.1\~0.3에 머물고, §3.2의 표로 보면 M1에서는 검출되지 않는다.

**방향.**

- 관측 편집이라는 별도 종류를 둔다. 상태는 그대로이고 특정 툴의 **첫 응답만** 바꾼다. (i) 오류 응답, (ii) 빈 결과, (iii) 오래된 시각이 찍힌 결과. 대상은 닻의 조회 툴(재고, 계약)이다.
- 이 편집은 모든 모델에게 같은 실패를 주므로 조회 행이 모델 간에 비교 가능해진다. "계약 확인이 실패해도 발주한다"는 주인이 가장 먼저 금지할 정책이다.
- 영역 표에 넣지 않고 별도 절로 보고한다. 계획이 말한 "조회 실패 경로는 고치지 않는다"와 충돌하지 않는다. 고치는 것이 아니라 통제 조건을 더하는 것이다.
- 3차 변수의 SILENT 편집은 **M1′(도달 레코드를 한 번에 돌려주는 판)에서 의존을 재고**, M1에서는 조회 행만 보고한다. "3차 변수를 정책이 쓰는가"와 "3차 변수를 에이전트가 찾아보는가"를 두 무대로 나누는 것이 검정력으로도 해석으로도 깔끔하다.

### 4.5 G5: 자유 텍스트 조건에 세계 제약 태그를 단다

§3.3의 W를 생성기 쪽에서 구현하는 것이다. §9의 조건 써넣기가 내는 술어에 `kind ∈ {hard, soft}`를 붙인다. "200캔 미만 주문은 접수하지 않는다"는 hard(거부), "대량 주문을 선호한다"는 soft(선호)다. 문장화 프롬프트에 kind를 입력으로 주고, 역파싱이 kind까지 되받는지 검사한다. hard만 W에 들어간다. soft 조건은 그 자체로 좋은 SILENT 편집이다. 모델이 선호 문구에 따라 수량을 바꾸면 그것이 주인 자리다.

### 4.6 개정 의사코드 (GEN-ALGO §13 표 대비 차이만)

```python
# 4 편집: 자유 텍스트 조건에 세계 제약 태그 (G5)
for e in free_text_edits:
    e.pred, e.kind = write_condition(field, kind=choice({'hard','soft'}, seed))
    assert backparse(e.text) == (e.pred, e.kind)
W = [e.pred for e in edits if e.kind == 'hard']      # 구성으로 앎다. 판단 없음

# 4b 관측 편집 (G4): 상태는 그대로, 첫 응답만 교체
obs_edits = [ObsEdit(tool=t, mode=m) for t in anchor_lookup_tools(env)
                                    for m in ('error', 'empty', 'stale')]

# 6 라벨: 영역을 두 번 계산 (§3.3)
region_Q  = region(Q, base, edit)
region_QW = region(Q & W, base, edit)
site_kind = 'owner' if both_sites(region_Q, region_QW) else 'world'

# 8 실행: 동률 상태는 2x2 균형 배치 (G3), 편집 k=10 (§3.2)
for s in states:
    cells = [(o, i) for o in ('A_first','B_first') for i in ('A_lowid','B_lowid')] \
            if has_tie(Q, s) else [None]
    run(s, k=10 if s.is_edit else k_sqrt_m, counterbalance=cells)

# 9 판정: 효과 크기 + CI, MDE를 함께 (§3.2)
site = fisher_bh_screen(pair) and tv_ci_lower(pair) > DELTA

# 10 국소화 (G2): 자리로 잡힌 전순서 변수만. 검정에 쓰지 않음
for f in sites if ordered(f):
    tau_hat = bisect(f, lo=base[f], hi=edit[f], probes=5, k=5)
    row(f).rule = f'{f} >= {tau_hat}';  row(f).invented = not near(tau_hat, q_constants)

# 11 검증 (G1): held-out 8 = 조합 4 + 무작위 유효 4. 승인 전 어떤 통로도 보지 않음
held_out = compose_pairs(sites, n=4, seed) + random_valid(env, constraints, n=4, seed)
```

### 4.7 비용

위임 하나 × 모델 하나의 롤아웃 수다. 편집 11개 기준, 자리 3개가 전순서 변수라고 가정한 어림이다.

| 단계 | 상태 수 | k | 런 |
| --- | --- | --- | --- |
| 기준 셋 | 3 | k√m 배분 | 약 30 |
| 발견 편집 | 11 | 10 | 110 |
| 관측 편집 | 3 | 5 | 15 |
| 경계 국소화 | 자리 3 × 탐침 5 | 5 | 75 |
| held-out | 8 | 5 | 40 |
| 합 |  |  | 약 270 (현재 계획 약 85) |

세 배가 되므로 모든 모델에 걸 수는 없다. 권고는 발견과 관측 편집은 모든 모델에, 국소화와 held-out은 **로컬 1 + 상업 1의 두 모델에만** 건다. 그러면 파일럿(위임 4 × 모델 3)의 핵심 부분은 발견·관측 1,860런과 국소화·held-out 920런으로 약 2,800런이고, 계획의 핵심 1,020런을 대체한다. 나머지 항목(생성기 비교, M1′, P2)을 더하면 파일럿 합계는 현재 어림 2,750에서 약 4,500으로 늘고, §3.1대로 M1′와 P2를 모델 하나씩으로 줄이면 약 4,000이다. 대부분 로컬 모델의 GPU 시간이므로 비용보다 일정의 문제이고, D01 하나로 게이트를 먼저 통과하면(§3.1) 나머지 셋은 10월 중순까지 돌려도 된다.

## 5. 평가 설계와 베이스라인, 통계

**비교군은 이미 강하다. 남은 것은 이미 가진 자료를 한 번 더 쓰는 것과, 비교가 우연이나 하네스 차이로 이기지 않게 막는 것이다.**

### 5.1 예측 타당도에 자기 예측 기준선을 넣는다

심사자가 가장 먼저 묻는 것은 "에이전트에게 이 상황에서 뭘 할지 물어보면 되지 않나"다. B2a가 정확히 그 질문이다. held-out 상태에 B2a를 돌리면 모델의 자기 예측이 나오고, 이것을 정책표의 예측과 같은 자리에 둔다. 비용은 모델당 held-out 8 × k 5 = 40회 단발 호출이다.

| 예측기 | 입력 | 무엇을 재나 |
| --- | --- | --- |
| B0 자리 목록 | 지시문, 스키마 | 미리 쓰기의 한계 |
| **B2a 자기 예측** | held-out 상태 텍스트 | 에이전트가 자기 정책을 아는가 |
| P2 실행 보고 | 발견 상태의 보고 | 보고에서 읽히는 규칙 |
| 원시 트레이스 | 발견 상태의 런 전부 | 압축 전 자료 |
| 정책표 | 발견 + 국소화 | 방법의 산출물 |

정책표가 B2a 자기 예측을 넘어야 "대입이 증언보다 낫다"는 주장이 예측의 언어로도 선다. 반대로 B2a가 비슷하면 그것도 실무적으로 좋은 결과(싸고 빠른 사전 점검)이고, D-034의 분기가 무엇을 뜻하는지를 held-out에서 다시 확인하게 된다.

### 5.2 B2a와 M1의 일치율은 자리 행에서, 분포로 잰다

Qwen의 M1 = B2a 8/8은 인상적이지만 여덟 상태 중 여섯은 두 통로 모두 최빈 비율 100%인 결정적 행동이고, 그중에는 R1처럼 q가 답을 정하는 행도 있다. 최빈 일치를 전체 행에서 세면 이런 행이 일치율을 끌어올리고, "B2a가 M1과 같은 수준이면 2단계를 예측 질의로 바꾼다"는 분기가 쉽게 켜진다. 게다가 R8처럼 최빈은 같아도 분포가 다른 행(B2a 분할 2/5 + NONE, M1 분할 3/5 + 단일 100 2/5)은 최빈 일치로는 같다고 나온다.

- 일치율은 **자리 행(영역이 FILL·OPEN·SILENT인 상태)에서만** 세고, 전체 행 값은 참고로 적는다.
- 최빈 일치와 함께 상태별 분포 간 TV의 평균을 적고, 같은 모델의 M1을 반으로 나눈 split-half TV를 바닥으로 둔다. B2a–M1 TV가 바닥 수준이면 통로 차이가 없는 것이다.
- D-034의 분기 조건도 이 두 수치로 다시 쓴다.

### 5.3 모델 간 하네스 동등성

C1의 "모델 간에는 갈린다"는 하네스가 모델마다 같다는 전제 위에 선다. 지금 러너는 추론 내용을 다음 턴에 돌려보내지 않는다(vLLM 기본 동작). 상업 모델은 툴 루프에서 추론 블록을 보존해 돌려주기를 요구하거나 권장하는 경우가 있고, OpenAI 호환 엔드포인트가 그 기능을 어디까지 지원하는지는 제공자마다 다르다. 파일럿에 Claude Opus 4.7을 넣기 전에 이 부분을 문서로 확인해야 한다.

- 추론 보존 여부를 런마다 로그 항목으로 남긴다.
- 로컬 모델 하나로 추론 보존 있음/없음을 D01 기준 셋에서 비교하는 작은 절제를 둔다(상태 3 × k 10 × 2 = 60런). 차이가 없으면 한 줄로 닫히고, 있으면 모델 간 발산의 일부가 하네스에서 온 것이다.

### 5.4 인간 발산 기준선의 입력을 B2a와 똑같이

사람에게 주는 상태는 생성기의 B2a 렌더링과 바이트 단위로 같아야 한다. 그래야 사람 간 불일치와 모델 간 불일치를 같은 척도에서 비교할 수 있고, §3.3의 사전 등록 예측(세계 제약 자리에서 사람 간 불일치가 낮다)을 검정할 수 있다. 15명이면 한 상태의 다수안 비율 CI가 ±0.25 안팎이므로 상태별 주장이 아니라 자리 종류별(주인 대 세계 제약) 집계로 읽는다.

### 5.5 사전 등록 시점

D01은 개발 데이터이므로 사전 등록 문서는 **D01 게이트 통과 직후, 나머지 세 위임을 돌리기 전**에 git tag로 동결한다. 들어갈 것은 할당표, 결과공간 조대화, δ와 k, W 태그 규칙, held-out 구성 규칙, 예상 방향(계획 §6의 목록에 §3.3과 §5.1의 예측 둘을 더한 것)이다.

## 6. 우선순위 로드맵과 예상 심사 질문

**가장 먼저 할 일은 9/28 종이 검사에 항목 셋을 더하고, 10/9 게이트를 D01 한 위임의 끝까지 통과로 바꾸는 것이다.** 나머지는 그 뒤에 순서대로 따라온다.

### 6.1 로드맵

| 시점 | 할 일 | 절 |
| --- | --- | --- |
| 9/28 종이 검사 | 기존 여덟에 셋을 더한다. (9) 씨앗 필터에 "자기 커밋 읽기 툴을 패치로 더할 수 있는가"를 넣어 131개에 돌린다. (10) §3.2의 검정력 계산으로 δ와 k를 정한다. (11) CASE 여덟 상태를 W 규칙으로 분류해 R7이 세계 제약, R5가 주인 자리, R3·R6·R8이 W 미정으로 나오는지 본다 | §2.1, §3.2, §3.3 |
| 9/29 \~ 10/2 | D01로 파이프라인 끝까지: 생성기, 영역(Q와 Q∧W), 판독기(Fisher 걸러내기 + TV), 노출 기록기, 동률 균형 배치, 관측 편집 셋 | §3.1, §4.3, §4.4 |
| 10/3 | 사전 등록 문서를 git tag로 동결 | §5.5 |
| 10/3 \~ 10/9 | 나머지 위임 셋의 발견 단계, 모델 3, 편집 k=10. 게이트 판정 | §3.2 |
| 10/10 \~ 10/20 | 경계 국소화, held-out 8, B2a 자기 예측, 추론 보존 절제(모델 2) | §4.1, §4.2, §5.1, §5.3 |
| 10월 | 인간 발산 기준선(B2a와 같은 렌더링), 비준 예비 시행 3\~6명, 위임 저작 | §3.1, §5.4 |
| 11월 | 본 실행(범위 조정판) | §3.1 |
| 12월 | 분석과 집필 |  |

### 6.2 예상 심사 질문과 답의 자리

| 질문 | 지금의 답 | 이 리뷰가 더하는 것 |
| --- | --- | --- |
| 자리 수는 검정력의 산물 아닌가 | 미비 | TV + CI + MDE, 편집 k=10, 계층 모형 (§3.2) |
| 합리적 복원(ACCORD)과 무엇이 다른가 | 인간 발산 기준선 | W 자동 분할과 사전 등록 예측 (§3.3) |
| 정책표가 사례 모음이 아니라 정책이라는 증거는 | 단일 편집 held-out | 조합·무작위 held-out, 단일 대비 조합 예측률 (§4.1) |
| 에이전트에게 물어보면 되지 않나 | B2a 행 단위 일치율 | held-out에서 B2a 자기 예측을 예측기로 비교 (§5.1) |
| 승인한다는 것이 구체적으로 무엇인가 | 수락·금지·지정 | 허용 집합과 Q\_owner, 이탈률·가드·패치의 통일 계산 (§3.4) |
| 모델이 바뀌면 다시 해야 하지 않나 | 없음 | 그렇다. 정책표를 모델 판 간 회귀 검사로 쓰는 것이 동기의 일부다 (§2.1) |
| 메타모픽 테스트를 다시 부른 것 아닌가 | 계보 인정 + 차이 셋 | 경계 국소화와 조합 검증은 오류 탐지가 아니라 정책 추출이다 (§4.2) |

## 출처

- 저장소: `Plan.md`, `docs/CASE-D01.md`, `docs/GEN-ALGO.md`, `docs/B2-D01.md`, `docs/M1-D01.md`, `docs/DECISIONS.md`(D-039\~D-041), `Review_20260922_plan_and_state_generation.md`, `src/runner/run.py`, `data/env_patches.md`
- §3.2의 검정력 표: Fisher 정확 검정(양측) 몬테카를로 4,000회, 결과 이진화, 리뷰어 계산
- [Li, Tamkin, Goodman, Andreas. Eliciting Human Preferences with Language Models. arXiv:2310.11589](https://arxiv.org/abs/2310.11589)
- [Tamkin, Handa, Shrestha, Goodman. Task Ambiguity in Humans and Language Models. arXiv:2212.10711](https://arxiv.org/abs/2212.10711)
