# 시행계획: Plan.md v2를 어떤 순서로, 누가, 무엇을 내며 수행하는가

작성 2026-09-16. 지위: `Plan.md`(연구 계획 v2)의 하위 문서다. 무엇을 하는지는 `Plan.md`가 정하고, 이 문서는 순서, 담당(사람·서브 에이전트), 산출물, 다음 단계로 넘어가는 조건을 정한다. 어긋나면 `Plan.md`가 우선하고 이 문서를 고친다. 서브 에이전트의 상세 지시는 `.claude/agents/*.md`, 작업 문서의 형식과 초안은 `docs/`에 있다.

한 줄. Phase 0(9/16~10/9)의 앞 한 주를 "짓는 주"가 아니라 "검증하는 주"(Stage 0)로 쓴다. Stage 0은 코드를 만들지 않고 논증 사슬, 반론 장부, 척도의 계산 정의, 사전 등록, 위임 실물 하나를 만든다. 러너·데이터·계측기 구현(Stage 1)과 파일럿 실행(Stage 2)은 그 뒤에 온다. 10/9 게이트는 그대로다.

## 0. 왜 시작 단계를 검증에 쓰는가

`Plan.md` §9는 1주차에 설치·러너 v0·하네스 검증 50·비가역 라벨 176·섭동표 도출을 넣었다. 그 앞에 Stage 0을 두는 이유 셋.

1. **명제 1이 정의에서 측정으로 바뀌었다** (`Plan.md` §0, §1). 측정이 된 명제는 두 방법을 같은 사건·같은 분모로 비교해야 한다. 지금 척도표(§6)의 자리 재현율(B)과 노출률(M1)은 사건이 다르다(`docs/LOGIC.md` L1). 러너를 먼저 지으면 이 불일치가 로그 형식과 채점 코드에 굳는다.
2. **M1의 동어반복 방어는 절차로만 성립한다** (§4.3 끝 문단 "이 분리가 없으면 M1은 동어반복이다"). fork set 생성기가 R을 입력으로 받지 않는 구조와, 섭동표가 R 유형을 모르는 절차로 도출됐다는 기록이 방어의 전부다. 절차를 먼저 고정해야 생성기가 그 절차를 따른다.
3. **심사자를 설득하는 문장은 실험이 끝난 뒤가 아니라 설계할 때 정해진다.** 반론 장부를 먼저 쓰면 어느 실험이 어느 반론에 답하는지가 설계에 들어가고, 답이 없는 반론은 실험을 돌리기 전에 드러난다. 이전 저장소(underspec)에서 REVIEW → DECISIONS → PLAN 순서로 방향이 잡힌 것과 같은 순서다.

Stage 0의 산출물은 문서 다섯(`docs/LOGIC.md`, `docs/OBJECTIONS.md`, `docs/PREREG.md`, `docs/DECISIONS.md`, `docs/SCHEMA.md`)과 실물 하나(위임 1개의 D+/D−, R 코드, 상태 16개, 정책표 손 계산)다.

## 1. 논증 사슬: 무엇이 참이어야 논문이 서는가

논제(`Plan.md` §0)는 일곱 단계로 쪼개진다. 각 단계는 앞 단계가 참일 때만 뜻이 있다. 심사자는 이 순서로 묻고, 우리는 이 순서로 답한다.

| # | 주장 | 무엇이 보여 주나 (실험 · 척도) | 반증되면 논문은 | `Plan.md` §7 게이트 |
|---|---|---|---|---|
| 1 무대 | 상시 위임(무인·무대화·비가역)에서 abstain은 채움과 중지로 무너진다 | E0: S1/S2/S4를 원 계약과 P0로. abstain 정확도가 무너지는 방향, 지어냄 M 비율 | P0에서 대부분 중지·질문이면 P0 문구를 한 번 수정, 그래도면 논제 재검토 | 현상 |
| 2 정책 | 빈 자리는 실행마다 채워져 정책이 된다: 귀속 가능하고, 일관되고, R과 다르다 | D2: 정책 정확도 < D+ 준수율, 일관성(T=1 3회), 집합 밖 비율 | 집합 밖이 다수면 "정책"이라는 단위가 서지 않는다 → 열린 코딩으로 v1 확장 후 재귀속 | 현상, 계측기 |
| 3 불가시 | 그 정책은 산출물에 없다 | 명제 3(구성상 참) + D1 산출물 동일성, 보고 구분 정확도 ≈ 우연 | 판정기가 보고만으로 D+/D−를 가르면 보고에 정책이 새는 것이므로 명제 3을 축소하고 그 누설을 명제 2의 측정으로 옮긴다 | (게이트 없음) |
| 4 증언 (a) | 작성 시점 자기보고는 대입보다 자리를 덜 드러낸다 (명제 1) | B0/B1 자리 재현율, B1 자기보고 일치율 ↔ M1 노출률. 같은 사건·같은 분모로(L1) | 같거나 높으면 무게중심을 "자기보고로 충분한가"로 옮긴다 | 베이스라인 |
| 5 증언 (b) | 실행 중·후 증언은 늦고 불완전하다 (명제 2) | D1 DISCLOSE/MENTION/SILENT/CLAIM-HALT 비율, 추론 재현율(로컬 3), P2 탐침 | P2가 분기 D−에서 피해 0이면 M1의 자리를 "P2가 놓치는 잔여"로 좁힌다 | P2 탐침 |
| 6 회수 | 실행 전 프록시 대입은 누락된 정책을 드러낸다 | M1: D− 노출률·감지율, D+ 귀속 정확도·오경보율, held-out 잔여 피해(M1+P0 < P0) | 노출률 < 50% 또는 오경보율 > 10%면 방법 기제 미달 → 섭동표·규칙 집합 재설계, 통과 전 확장 금지 | 방법 기제 |
| 7 쓸모 | 주인이 정책표만 보고 비준·보강할 수 있다 | 인간 비준 정확도·소요 시간 (Phase 3) | 정확도가 낮으면 표 형식을 재설계하고 R 저자 정의의 한계로 적는다 | (Phase 3) |

읽는 법. 논제가 서는 데 필요한 것은 2·4·5·6이다. 1은 무대의 존재, 3은 동기, 7은 처방의 크기다. 게이트(§7)의 항목은 2·4·6에 걸려 있고, 5는 정보용 탐침이다.

**설득의 다섯 질문.** 심사자가 첫 페이지에서 던질 질문과 답의 자리. 전체 장부는 `docs/OBJECTIONS.md`.

| 질문 | 답의 자리 |
|---|---|
| "AgentAbstain이 이미 모델은 멈추지 않는다고 보였다" | 현상은 그들이 이름 붙였다. 우리 단위는 "상태에 대한 정책"이고 기여는 통로 구분(증언 대 대입)이다. `Plan.md` §10 첫 줄, v1 `MOTIVATION.md` §10 금지 표현 |
| "t0에 주인이 있으면 그냥 물어보면 되지 않나" | 명제 1을 측정한다(B0/B1). 상태 없는 열거는 상상에 의존한다는 것이 예측이고, 반대면 논문의 무게중심이 옮겨진다(§7 베이스라인 게이트) |
| "fork set이 R을 알고 만들어졌다면 M1은 동어반복" | fork set 생성기는 R을 입력받지 않는다. 섭동표는 R 유형을 모르는 절차로 도출한다(`docs/derivation/`). 커버리지 밖 6개가 방법의 한계를 잰다 |
| "주인 = R 저자는 순환 정의" | 인정하고 인간 비준 연구(Phase 3)로 부분 보완. 한계 절에 적는다 |
| "M1은 메타모픽 테스트 아닌가" | 뿌리를 인정한다(CheckList). 기여는 테스트가 아니라 정책이라는 단위, 증언과의 비교, 비준 절차다 |

## 2. 단계 구조

| 단계 | 기간 | 목적 | 산출물 | 넘어가는 조건 |
|---|---|---|---|---|
| Stage 0 논리 검증과 설득 과정 | 9/16(수) ~ 9/22(화) | 명제·척도·게이트의 정합성 확인, 반론 장부, 사전 등록, 위임 실물 1개 | §3의 S0-1 ~ S0-9 | Gate 0 (§4), 9/23 |
| Stage 1 계측기·러너·데이터 | 9/23(수) ~ 10/1(목) | 러너 v0, 하네스 검증, 위임 8·상태 128, 계측기 v0, 로컬 모델 서빙 | `Plan.md` §7 산출물 중 실행 전 항목 | 재생 100%, 하네스 ±10%p, 판독기 합성 검사 통과, PREREG 동결 |
| Stage 2 파일럿 실행·게이트 | 10/2(금) ~ 10/9(금) | 롤아웃 약 930, 검수 50, 열린 코딩, 게이트 리뷰 | 실측 비용·시간, 규칙 집합 v1, 게이트 판정 | `Plan.md` §7 게이트 (10/7~10/9) |
| Phase 1 이후 | `Plan.md` §9 | 변경 없음 | | |

`Plan.md` §9와 달라지는 것. 러너 v0와 하네스 검증이 1주차에서 Stage 1로 간다. 섭동표 도출과 비가역 라벨 초안은 코드가 필요 없으므로 Stage 0에 남는다. Stage 1은 옛 1·2주차를 합친 분량이 되는데, 러너(harness-engineer)와 위임 저작(delegation-author)이 서로 의존하지 않으므로 병렬로 흡수한다. 하네스 검증은 새 데이터 없이 AgentAbstain 문항으로 돌리므로 Stage 1 초반에 끝낼 수 있다.

## 3. Stage 0 작업

| ID | 작업 | 담당 | 산출물 | 완료 기준 |
|---|---|---|---|---|
| S0-1 | 논리 감사: 명제 1~3과 척도 16개를 정의·사건·분모·반증 조건으로 다시 쓰고, `docs/LOGIC.md`의 감사 대상 L1~L13을 판정한다 | logic-auditor → 저자 확정 | `docs/LOGIC.md` 판정표 | 명제마다 측정 가능한 예측 ≥ 1, 반증 조건 명시. "기각" 판정 0 (있으면 DECISIONS로 해소) |
| S0-2 | 반론 장부: 심사자 시점의 반론을 치명/중대/경미로 등급 매기고, 각각 "무엇이 있으면 해소되는가"를 적는다 | reviewer-adversary → 저자 답 | `docs/OBJECTIONS.md` | 치명 등급 중 답 없는 것 0. 답 = 설계 요소, 게이트 항목, 명시된 한계, 또는 DECISIONS의 변경 |
| S0-3 | 문헌 확인: `Plan.md` §10의 arXiv ID 실재 확인, 감시 목록 검색(2026-06 이후) | related-work-scout | `docs/related/scout-2026-09.md` | ID마다 [확인]/[미확인], 신규 경쟁 연구는 관계(선점/상보/무관)와 인용문 |
| S0-4 | 섭동표 독립 도출: T+ 131개(operational act)의 지시문이 조건을 거는 상태 특징을 세어 빈도표를 만든다. 도출자는 `Plan.md` §4.2의 R 유형과 §4.3의 섭동표 v0를 보지 않는다 | perturbation-deriver (눈가림) | `docs/derivation/perturbation-v1.md` | 절차·필터·집계 코드가 기록되고, 표 v1이 v0와 다르면 v1을 쓴다(`Plan.md` §4.3) |
| S0-5 | 계측기 명세와 로그 스키마: 척도마다 계산식·입력 필드·분모·예외를 적고, 정책표 판독기와 출처 계산을 합성 로그로 단위 검사한다. 러너는 만들지 않는다 | instrument-designer | `docs/SCHEMA.md`, `spec/metrics.md`, `tests/` | 척도 16개 전부 스키마 위의 계산식. 판독기 합성 검사(일관/비일관/집합 밖/동률) 통과 |
| S0-6 | 최소 실물: 조달 환경(`emergent_risk_discovery/preview_018` 상태: 공급업체 3, 단가·가용량·리드타임 상이)으로 위임 1개를 손으로 짓는다. q(D−), R 문장(D+), R 코드, 측정용 8, fork set 8, 규칙 집합 v0 다섯 개의 예측 정책표, 보고 예시와 판정 라벨 | delegation-author → 저자 검토 | `data/delegations/D01/` | R(s) 유일, 분기 4·비분기 4 확인, 분리 설계 기준 통과(어느 두 규칙도 최소 한 행에서 다름), 저자 1인이 표만 보고 R 불일치 행을 찾는 데 성공 |
| S0-7 | 실현 가능성: 로컬 3 모델의 서빙 계획(파티션·GPU·양자화·판 문자열), 상업 2 + 판정기 API 접근, reasoning 필드 이름 | harness-engineer (30분 스파이크, 대형 잡 제출 없음) | `docs/FEASIBILITY.md` | 모델 5 + 판정기 1의 판 문자열 확정. 각 서빙·호출 경로가 1회 성공 또는 미확인으로 표시 |
| S0-8 | 사전 등록 v0: 예상 방향(`Plan.md` §6 통계), 게이트 임계(§7), P0 문구와 "한 번 수정" 허용 조항, 분석 단위 | 저자 + 조율자 | `docs/PREREG.md` | Stage 1 끝에 동결(git tag `prereg-pilot`) 가능한 상태 |
| S0-9 | 종결 기록: Stage 0의 모든 채택·기각을 근거와 반증 조건과 함께 적는다 | 조율자 | `docs/DECISIONS.md` | S0-1의 "보강"·S0-2의 "치명" 항목마다 종결 항목 1개 |

병렬 관계. S0-1, S0-2, S0-3, S0-4, S0-7은 서로 독립이라 같은 날 시작한다. S0-5는 S0-1의 정의를 받아야 하므로 하루 뒤. S0-6은 S0-4의 표 v1과 S0-5의 스키마가 있어야 fork set을 만들 수 있으나, q·R·측정용 상태는 먼저 지을 수 있다. S0-8·S0-9는 나머지의 결과를 받아 마지막에.

**Stage 0에서 이미 보이는 논리 문제.** 계획을 읽으면서 찾은 것이고, 판정은 S0-1이 한다. 전문은 `docs/LOGIC.md` §3.

- L1 명제 1의 사건 불일치: 자리 재현율은 "자리를 언급했는가", 노출률은 "귀속 ≠ R일 때 불일치 행을 냈는가". 공통 사건("주인이 산출물만 보고 R의 자리가 열려 있음을 알 수 있는가")과 공통 분모(D− 위임 × 모델)로 다시 정의해야 비교가 선다.
- L2 B의 "자리 언급" 판정기가 `Plan.md` §5 계측기 목록에 없다.
- L3 판독기의 85% 규칙은 구분 행이 4개일 때 100%를 요구한다. 정수화 규칙과 동률 규칙이 필요하다.
- L4 보고 재현율의 분모를 "선택 채움 인자가 하나 이상인 commit 실행"으로 좁혀야 DISCLOSE의 대상이 정의된다.
- L5 실패한 commit 시도(success=False)는 실행 의도이므로 따로 세야 한다.
- L6 분기점 노출 확인: 모델의 조회가 경쟁 개체를 실제로 받았는지를 회차마다 로그로 확인하지 않으면 "정책"과 "못 봄"이 섞인다.
- L7 명제 3의 "우연 수준"은 균형 표본과 이항 검정으로 사전 등록한다.
- L8 파일럿 위임 8개는 클러스터 부트스트랩의 클러스터로 너무 적다. 파일럿 게이트는 점추정 임계이지 검정이 아니다.
- L9 하네스 검증의 모델: AgentAbstain 리더보드에 Qwen이 없다. 리더보드에 있으면서 중형으로 서빙되는 모델(후보 GPT-OSS 120B)로 하고, S1은 39쌍 78문항 전부를 돌린다.
- L10 P0 문구의 "한 번 수정" 조항은 수정 가능한 범위를 미리 적어야 사후 조정 의혹을 막는다.
- L11 D+ 준수율이 낮은 모델에서 피해율을 어떻게 조건부로 보고할지 미리 정한다.
- L12 자체 루프는 `__runtime_export_snapshot`을 막고, 결과는 execution_log에서 읽는다.
- L13 판정기 검증 표본은 예측 라벨로 층화해 뽑는다.

## 4. Gate 0: 논리 게이트 (9/23)

| 항목 | 기준 |
|---|---|
| 명제 | 명제 1~3 각각 정의(용어는 v1 `MOTIVATION.md` §12 대응), 측정 가능한 예측 ≥ 1, 반증 조건, 실험·척도 매핑이 `docs/LOGIC.md`에 있다 |
| 척도 | `Plan.md` §6 척도 16개 전부 `docs/SCHEMA.md` 위의 계산식·분모·단위가 있고, 프로그램 척도(판독기, 출처 계산, 산출물 동일성)는 합성 검사를 통과했다 |
| 반론 | `docs/OBJECTIONS.md`의 치명 등급 반론 중 답 없는 것 0 |
| 독립성 | 섭동표 v1이 눈가림 절차로 도출됐고 절차가 기록됐다. 분리 설계 기준을 v1 × 규칙 집합 v0에서 자동 검사해 통과했다 |
| 실물 | D01의 D+/D−, R 코드, 상태 16개, R(s) 유일, 분기 4·비분기 4, 다섯 규칙의 예측 정책표가 있고 저자가 표만 보고 R 불일치 행을 찾았다 |
| 실현 가능성 | 모델 5 + 판정기 1의 판 문자열 확정. 로컬 3의 서빙 경로(파티션·GPU 수·양자화 여부)와 상업 3의 API 접근이 확인 또는 미확인으로 표시. 미확인이 파일럿 모델 3에 있으면 대체안이 DECISIONS에 있다 |
| 사전 등록 | `docs/PREREG.md` v0에 예상 방향, 게이트 임계, P0 문구, 수정 허용 조항, 분석 단위가 있다 |
| 기록 | Stage 0의 채택·기각이 `docs/DECISIONS.md`에 근거와 반증 조건과 함께 있다 |

미달 항목이 있으면 Stage 1의 러너 구현은 시작하되(논리와 독립), 위임 저작과 상태 생성은 미달 항목이 닫힐 때까지 시작하지 않는다.

## 5. Stage 1과 Stage 2 (요약)

Stage 1 (9/23~10/1). `Plan.md` §7 산출물 중 실행 전 항목.

| 작업 | 담당 | `Plan.md` 근거 |
|---|---|---|
| 러너 v0: 루프 하나, 어댑터(base_url·키·reasoning 필드), 샌드박스 플래그, 로그 = SCHEMA | harness-engineer | §2 |
| 로컬 모델 서빙(vLLM/SGLang, SLURM), 개발용 소형 모델로 러너 디버깅 | harness-engineer (slurm-assistant 스킬) | §2, §3 |
| 하네스 검증: S1 78문항, 리더보드에 있는 서빙 가능 모델(후보 GPT-OSS 120B, D-002), 원 계약, 원 논문 표 11 대비 ±10%p | harness-engineer → stats-analyst | §2, §7 |
| 위임 8(유형별 2, 비가역 4)의 q·R 문장·R 코드, 측정용 상태 64, fork set 64(프로그램 생성), 재생 100% | delegation-author | §4.2, §4.3, §4.5, §7 |
| 비가역 라벨 176 초안 → 저자 확정 | delegation-author → 저자 | §5 |
| 계측기 v0: 출처 계산, 판독기, 규칙 명시 판정기, 보고 판정기, 추론 판정기, 자리 판정기(L2) 프롬프트 | instrument-designer | §5 |
| B0/B1 프롬프트, P0/P1/P2 문구 확정 | 저자 + instrument-designer | §4.4, §6 B |
| PREREG 동결: git tag `prereg-pilot` | 조율자 | §6 통계 |

Stage 2 (10/2~10/9). 실행 약 930회(상업 약 340)를 로컬 먼저, 상업 병렬. 보고 50개 2인 검수(층화), 열린 코딩 → 규칙 집합 v1, 실측 비용·시간 → 본 실험 재산정, 게이트 리뷰(10/7~10/9)는 `Plan.md` §7 표 그대로. 게이트 계산은 stats-analyst가 PREREG의 정의로만 낸다. 게이트 결과와 τ² go/no-go 판단은 DECISIONS에 적는다.

## 6. 문서 체계와 규칙

| 파일 | 역할 | 쓰는 이 | 규칙 |
|---|---|---|---|
| `Plan.md` | 연구 계획(무엇을) | 저자 | 단일 출처. 변경은 DECISIONS 항목을 먼저 |
| `PLAN_EXEC.md` | 시행계획(순서·담당·조건) | 조율자 | 이 문서 |
| `docs/LOGIC.md` | 명제·척도의 형식 정의, 감사 대상과 판정 | logic-auditor 제안, 저자 확정 | 정의는 여기가 원본. 논문 §2 초고의 재료 |
| `docs/OBJECTIONS.md` | 반론 장부 | reviewer-adversary 제안, 저자 답 | 반론마다 등급·답의 종류·상태. 게이트마다 재실행 |
| `docs/DECISIONS.md` | 종결 기록 | 조율자 | 채택/기각/근거/반증 조건. 새 증거 없이 기각안을 되살리지 않는다 |
| `docs/PREREG.md` | 사전 등록 | 저자 | Stage 1 끝에 git tag로 동결. 이후 변경은 날짜와 사유를 덧붙인다 |
| `docs/EVIDENCE.md` | 수치의 단일 출처 | stats-analyst | [확인]/[미확인] 표시. 논문·계획은 여기서만 수치를 가져온다 |
| `docs/SCHEMA.md` | 로그 스키마(러너와 계측기의 계약) | instrument-designer 제안, harness-engineer 준수 | 필드 추가는 가능, 의미 변경은 DECISIONS |
| `docs/FEASIBILITY.md` | 모델·API·클러스터 실측 | harness-engineer | 날짜와 명령을 남긴다 |
| `docs/derivation/` | 섭동표 눈가림 도출 기록 | perturbation-deriver | 부록 재료. 수정하지 않고 새 판을 덧붙인다 |
| `docs/related/` | 문헌 확인 기록 | related-work-scout | v1 `REFERENCES.md`를 대체하지 않고 보강 |

원칙. 에이전트의 산출물은 제안이고, DECISIONS에 적혀야 효력이 생긴다. 수치는 EVIDENCE에만 산다. 문서는 한국어, 서술은 v1과 같은 문체(짧은 단정문, 표, 출처 표기).

## 7. 서브 에이전트 설계

조율자는 이 세션(주 에이전트)이다. 조율자는 에이전트를 부르고, 산출물을 저자에게 요약하고, 채택된 것을 DECISIONS에 적고, 게이트를 계산한다. 조율자는 논리 감사·반론·문헌 조사·계측기 설계·데이터 저작·러너 구현·통계를 직접 하지 않고 위임한다. 이유는 분리다. 감사자와 저자가 같은 문맥이면 감사가 무르고, 섭동표 도출자가 R 유형을 보면 독립성이 사라진다. 정의 파일은 `.claude/agents/<이름>.md`.

| 에이전트 | 역할 한 줄 | 단계 | 도구 | 쓰기 권한 | 눈가림 |
|---|---|---|---|---|---|
| `logic-auditor` | 명제·정의·척도·반증의 정합성 감사 | 0, 게이트마다 | 읽기 | 없음(판정표를 답변으로 반환) | |
| `reviewer-adversary` | ARR 심사자 역할, 반론 장부 | 0, 게이트마다, 집필 | 읽기 + 웹 | 없음 | |
| `related-work-scout` | 인용 실재 확인, 신규 경쟁 연구 | 0, 월 1회 | 읽기 + 웹 | `docs/related/` | |
| `perturbation-deriver` | T+ 131에서 섭동표 눈가림 도출 | 0 | 읽기 + Bash | `docs/derivation/` | `Plan.md` §4.2·§4.3, `docs/LOGIC.md` 등을 읽지 않는다 |
| `instrument-designer` | 척도의 계산 정의, 스키마, 판정기 프롬프트, 합성 검사 | 0, 1 | 전부 | `docs/SCHEMA.md`, `spec/`, `src/instruments/`, `tests/` | |
| `delegation-author` | 위임·R·상태 저작, 재생·분리 검사, 비가역 라벨 초안 | 0(실물 1), 1 | 전부 | `data/`, `src/gen/` | fork set은 R을 입력받지 않는 프로그램이 만든다 |
| `harness-engineer` | 러너 하나, 어댑터, 서빙, 하네스 검증 | 0(스파이크), 1, 2 | 전부 + SLURM 스킬 | `src/runner/`, `scripts/`, `docs/FEASIBILITY.md` | |
| `stats-analyst` | 사전 등록된 분석, 게이트 계산, EVIDENCE | 2, 3 | 읽기 + Bash + EVIDENCE | `docs/EVIDENCE.md`, `analysis/` | PREREG 밖 분석은 탐색으로 분리 |

호출 규약.
- 조율자는 에이전트에게 파일 경로와 질문을 주고, 에이전트는 정의 파일의 "산출 형식"으로 돌려준다. 형식을 벗어난 산출물은 받지 않는다.
- 읽기 전용 에이전트(감사·심사)는 파일을 고치지 않는다. 채택은 저자가 하고 조율자가 적는다.
- 눈가림 에이전트에게는 조율자가 읽어도 되는 경로만 준다. 정의 파일에도 금지 경로가 있다.
- 같은 논점을 두 에이전트가 다르게 판정하면(예: 감사자는 성립, 심사자는 치명), 조율자가 둘의 근거를 나란히 저자에게 올리고 저자가 정한다. 다수결 없음.
- 에이전트는 수치를 지어내지 않는다. 확인하지 못한 것은 [미확인]으로 남긴다.
- Stage 0의 첫 호출은 S0-1·S0-2·S0-3·S0-4·S0-7을 한 번에 병렬로 낸다.
- 에이전트 정의를 만든 세션에서는 이름으로 호출되지 않는다(정의는 세션 시작 시 로드). 그 세션에서는 general-purpose 에이전트에 "먼저 `.claude/agents/<이름>.md`를 읽고 그 규칙을 따르라"고 지시해 같은 역할로 띄운다. 이 경우 도구 제한은 하네스가 아니라 지시로만 걸리므로, 눈가림 에이전트의 산출물 §0(열지 않은 경로)을 반드시 확인한다. 다음 세션부터는 이름으로 호출한다.

## 8. 지금 확인된 사실 (2026-09-16 실측)

| 항목 | 값 |
|---|---|
| 이 저장소 | uv 뼈대(`main.py`, `pyproject.toml`, python 3.12 지정)와 `Plan.md`뿐. 커밋 0개 |
| v1 자산 | `../standing-delegation/`: `MOTIVATION.md`(논리·금지 표현·용어), `REFERENCES.md`, `AGENTABSTAIN.md`(환경 실측·설계 요구사항 §11·§12), `data/agentabstain-{data,code}`(HF·GitHub 사본), `data/trace/`(문항 1개 전체 트레이스) |
| 이전 프로젝트 | `../underspec/`: `reports/{logic,EVIDENCE,DECISIONS,PLAN,REVIEW_*}.md`의 문서 체계. vLLM 서빙 sbatch(`scripts/vllm_serve.sbatch`)와 Qwen3.6-27B/35B-A3B 서빙 경험 |
| AgentAbstain | 263쌍/526문항, 환경 42, 도구 541(commit 176). 하네스가 모델에게 주는 것은 `system_prompt`·`instruction`뿐. `__runtime_export_snapshot` 차단 필수. 판정기 GPT-5.4. 리더보드에 GLM-5·DeepSeek V3.2·Claude Opus 4.7·GPT-5.5·Gemini 3.1 Pro 있음, Qwen 없음 |
| 클러스터 | H200×8 노드(H200 2, H200-ZT 2, H200-PCIe-ZT 2), A100-80GB×8 노드 14, L40S, RTX6000ADA, A6000, A100-40GB. 잡 시간 상한 3일. 사용자 QOS: 4a100, add_hpgpu, hpgpu, nogpu, normal, zt |
| 시스템 파이썬 | 3.9. uv 있음 → 프로젝트 venv는 3.12(pyproject)로. AgentAbstain 코드는 3.11에서 확인됨(v1 §1.5): 둘의 호환을 Stage 1 첫날 확인 |
| API 키 | 미확인(Anthropic·OpenAI·Google). S0-7에서 확인 |

## 9. 위험과 결정 필요

| # | 위험 | 영향 | 다루는 곳 |
|---|---|---|---|
| R1 | 로컬 중형 후보(예: Qwen/Qwen3.8-27B-FP8)가 vLLM에서 툴 호출과 reasoning_content를 실제로 내는지 미확인. 저자 결정(D-005)으로 대형 모델 서빙 위험은 해소 | 파일럿 로컬 2, 본 실험 로컬 3 | S0-7 후보 표(실재·크기·vLLM 지원·리더보드 여부). 미달 후보는 교체 |
| R2 | 하네스 검증용 모델은 원 논문 수치가 있어야 한다. 중형 후보 중 리더보드에 있는 것은 GPT-OSS 120B 정도 | 하네스 게이트(§7) | D-002 개정. 서빙 불가면 Claude Opus 4.7로 대체하고 하네스 차이 명시 |
| R3 | 상업 API 비용. 파일럿 상업 약 340회, 본 실험 약 6,000회 | 예산 | Stage 2 실측으로 외삽(§8) |
| R4 | Stage 0 삽입으로 Stage 1이 옛 1·2주차 분량 | 10/9 게이트 | 병렬화(§2). 밀리면 위임 8 → 6(유형별 1~2)으로 줄이고 상태 수는 유지 |
| R5 | 판정기(Gemini 3.1 Pro) 접근 | 계측기 게이트 | S0-7. 대안: 리더보드 밖 다른 상업 모델 1개 |
| R6 | 환경 결함(조회 도구의 죽은 가지, 존재 검사 없는 commit 24개) | 분기점 노출률, 지어냄 집계 | delegation-author가 쓰는 환경의 조회·commit 코드를 읽고 수정 목록 관리(v1 §12.6) |

## 10. 첫 세션 순서

1. `docs/`의 초안 다섯과 `.claude/agents/` 정의를 저자가 읽고 고친다.
2. 첫 커밋: `Plan.md`, `PLAN_EXEC.md`, `docs/`, `.claude/agents/`. 이후 PREREG 동결은 태그로.
3. 같은 날 병렬 호출: logic-auditor(S0-1), reviewer-adversary(S0-2), related-work-scout(S0-3), perturbation-deriver(S0-4), harness-engineer 스파이크(S0-7).
4. 결과를 저자가 보고 DECISIONS에 적는다. instrument-designer(S0-5) 시작.
5. delegation-author로 D01(S0-6). 저자가 표만 보고 R 불일치 행을 찾는 시험.
6. PREREG v0(S0-8). Gate 0 점검(§4). 통과 항목부터 Stage 1.
