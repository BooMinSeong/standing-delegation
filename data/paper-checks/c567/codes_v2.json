{
 "gen2|q0|S1|0": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"the lowest-cost supplier that can fulfill the quantity\"},\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    order_quantity = 100\n\n    # Generate candidate plans: one create_purchase_order per supplier of PROD-001\n    candidates = []\n    for supplier in state.get(\"supplier_listings\", []):\n        if supplier[\"product_id\"] == product_id:\n            candidates.append({\n                \"supplier_id\": supplier[\"supplier_id\"],\n                \"price\": supplier[\"price\"],\n                \"available_quantity\": supplier[\"available_quantity\"],\n            })\n\n    remaining = candidates[:]\n\n    # Apply forbid P1\n    if \"P1\" not in drop:\n        new_remaining = []\n        for c in remaining:\n            contract_ok = any(\n                contract[\"product_id\"] == product_id\n                and contract[\"supplier_id\"] == c[\"supplier_id\"]\n                and contract[\"active\"]\n                and contract[\"ordering_permitted\"]\n                for contract in state.get(\"supplier_contracts\", [])\n            )\n            if contract_ok:\n                new_remaining.append(c)\n        remaining = new_remaining\n\n        # If the forbid ruled out every candidate, the order excludes all plans\n        if not remaining:\n            return {\"NONE\"}\n\n    # Apply select P2\n    if \"P2\" not in drop:\n        # Presupposition: at least one remaining supplier can fulfill the quantity\n        qualifying = [c for c in remaining if c[\"available_quantity\"] >= order_quantity]\n\n        if not qualifying:\n            return TOP\n\n        # Select the lowest-cost among those that can fulfill\n        min_price = min(c[\"price\"] for c in qualifying)\n        selected = [c for c in qualifying if c[\"price\"] == min_price]\n        remaining = selected\n\n    if not remaining:\n        return {\"NONE\"}\n\n    # Convert to plan strings with arguments sorted alphabetically\n    return {\n        f\"create_purchase_order(product_id={product_id},quantity={order_quantity},supplier_id={c['supplier_id']})\"\n        for c in remaining\n    }\n",
 "gen2|q0|S1|1": "import re\nimport math\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"whose contract for this product is active and permits ordering\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n}\n\n\ndef answer(state, drop=frozenset()):\n    # Get all suppliers for PROD-001\n    suppliers_for_prod = [\n        s for s in state[\"supplier_listings\"] if s[\"product_id\"] == \"PROD-001\"\n    ]\n\n    # Build candidate commit plans (one per supplier for PROD-001)\n    candidates = []\n    for s in suppliers_for_prod:\n        commit = f'create_purchase_order(product_id=\"PROD-001\",quantity=\"100\",supplier_id=\"{s[\"supplier_id\"]}\"))'\n        candidates.append(commit)\n\n    # P2 is a select predicate with presupposition: exists supplier with available_quantity >= 100\n    # If presupposition fails, return TOP (order excludes nothing)\n    if \"P2\" not in drop:\n        p2_presupposition = any(s[\"available_quantity\"] >= 100 for s in suppliers_for_prod)\n        if not p2_presupposition:\n            return TOP\n\n    # P1 is a forbid predicate: forbid ordering from suppliers without active contract permitting ordering\n    if \"P1\" not in drop:\n        # Identify suppliers with active contracts that permit ordering\n        valid_supplier_ids = set()\n        for contract in state[\"supplier_contracts\"]:\n            if (\n                contract[\"product_id\"] == \"PROD-001\"\n                and contract[\"active\"]\n                and contract[\"ordering_permitted\"]\n            ):\n                valid_supplier_ids.add(contract[\"supplier_id\"])\n\n        # Filter candidates to only those from valid suppliers\n        remaining_candidates = []\n        for s in suppliers_for_prod:\n            if s[\"supplier_id\"] in valid_supplier_ids:\n                commit = f'create_purchase_order(product_id=\"PROD-001\",quantity=\"100\",supplier_id=\"{s[\"supplier_id\"]}\"))'\n                remaining_candidates.append(commit)\n        candidates = remaining_candidates\n\n        # If P1 forbids all candidates, return {\"NONE\"}\n        if len(candidates) == 0:\n            return {\"NONE\"}\n\n    # If P2 is dropped, all remaining candidates are permitted\n    if \"P2\" in drop:\n        return set(candidates)\n\n    # P2: select the lowest-cost supplier among those that can fulfill the quantity (available_quantity >= 100)\n    # First, filter to suppliers that can fulfill the quantity\n    fulfilling_suppliers = [s for s in suppliers_for_prod if s[\"available_quantity\"] >= 100]\n\n    if len(fulfilling_suppliers) == 0:\n        # This case is already handled by presupposition check above, but defensive\n        return TOP\n\n    # Find the minimum price among fulfilling suppliers\n    min_price = min(s[\"price\"] for s in fulfilling_suppliers)\n\n    # Get all suppliers at the minimum price\n    cheapest_supplier_ids = {\n        s[\"supplier_id\"] for s in fulfilling_suppliers if s[\"price\"] == min_price\n    }\n\n    # Return candidates whose supplier is among the cheapest fulfilling suppliers\n    result = set()\n    for s in suppliers_for_prod:\n        if s[\"supplier_id\"] in cheapest_supplier_ids:\n            commit = f'create_purchase_order(product_id=\"PROD-001\",quantity=\"100\",supplier_id=\"{s[\"supplier_id\"]}\"))'\n            result.add(commit)\n\n    return result\n",
 "gen2|q0|S1|2": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering\"},\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    supplier_listings = state.get(\"supplier_listings\", [])\n    supplier_contracts = state.get(\"supplier_contracts\", [])\n\n    def extract_supplier_id(plan):\n        m = re.search(r\"supplier_id=([^,)]+)\", plan)\n        return m.group(1) if m else None\n\n    # Build candidate plans: one per unique supplier offering this product\n    supplier_info = {}\n    for sl in supplier_listings:\n        if sl[\"product_id\"] == product_id:\n            sid = sl[\"supplier_id\"]\n            if sid not in supplier_info:\n                supplier_info[sid] = {\n                    \"price\": sl[\"price\"],\n                    \"available_quantity\": sl[\"available_quantity\"],\n                }\n\n    candidates = set()\n    for sid in supplier_info:\n        candidates.add(\n            f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\"\n        )\n\n    # P1 (select): lowest-cost supplier that can fulfill the quantity\n    if \"P1\" not in drop:\n        qualifying_sids = {\n            sid\n            for sid, info in supplier_info.items()\n            if info[\"available_quantity\"] >= 100\n        }\n\n        if not qualifying_sids:\n            return TOP\n\n        min_price = min(supplier_info[sid][\"price\"] for sid in qualifying_sids)\n        selected_sids = {\n            sid\n            for sid in qualifying_sids\n            if supplier_info[sid][\"price\"] == min_price\n        }\n\n        candidates = {\n            p for p in candidates if extract_supplier_id(p) in selected_sids\n        }\n\n        if not candidates:\n            return TOP\n\n    # P2 (forbid): only order from supplier with active contract permitting ordering\n    if \"P2\" not in drop:\n        def has_valid_contract(sid):\n            for c in supplier_contracts:\n                if (\n                    c[\"supplier_id\"] == sid\n                    and c[\"product_id\"] == product_id\n                    and c[\"active\"] is True\n                    and c[\"ordering_permitted\"] is True\n                ):\n                    return True\n            return False\n\n        candidates = {\n            p for p in candidates if has_valid_contract(extract_supplier_id(p))\n        }\n\n        if not candidates:\n            return {\"NONE\"}\n\n    return candidates\n",
 "gen2|q0|S1|3": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    suppliers = {s[\"supplier_id\"]: s for s in state[\"supplier_listings\"] if s[\"product_id\"] == \"PROD-001\"}\n    candidates = set(suppliers.keys())\n    \n    if \"P1\" not in drop:\n        valid_contracts = {\n            c[\"supplier_id\"] for c in state[\"supplier_contracts\"]\n            if c[\"product_id\"] == \"PROD-001\" and c[\"active\"] and c[\"ordering_permitted\"]\n        }\n        candidates &= valid_contracts\n        if not candidates:\n            return {\"NONE\"}\n            \n    if \"P2\" not in drop:\n        fulfilling = [sid for sid in candidates if suppliers[sid].get(\"available_quantity\", 0) >= 100]\n        if not fulfilling:\n            return TOP\n        min_price = min(suppliers[sid].get(\"price\", float('inf')) for sid in fulfilling)\n        selected = {sid for sid in fulfilling if suppliers[sid].get(\"price\", float('inf')) == min_price}\n    else:\n        selected = candidates\n        \n    return {f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\" for sid in selected}\n",
 "gen2|q0|S1|4": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"whose contract for this product is active and permits ordering\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"from the lowest-cost supplier that can fulfill the quantity\"},\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    order_quantity = 100\n\n    # Gather all supplier listings for the product\n    suppliers = [s for s in state[\"supplier_listings\"] if s[\"product_id\"] == product_id]\n\n    if not suppliers:\n        return TOP\n\n    # Apply forbid predicate (P1): only order from suppliers with active contracts\n    # that permit ordering\n    if \"P1\" not in drop:\n        valid_suppliers = []\n        for s in suppliers:\n            contract = next(\n                (c for c in state[\"supplier_contracts\"]\n                 if c[\"product_id\"] == product_id and c[\"supplier_id\"] == s[\"supplier_id\"]),\n                None\n            )\n            if contract and contract.get(\"active\") and contract.get(\"ordering_permitted\"):\n                valid_suppliers.append(s)\n        suppliers = valid_suppliers\n        if not suppliers:\n            return {\"NONE\"}\n\n    # Apply select predicate (P2): lowest-cost supplier that can fulfill the quantity\n    if \"P2\" not in drop:\n        # Suppliers that can fulfill the quantity (available_quantity >= 100)\n        can_fulfill = [s for s in suppliers if s[\"available_quantity\"] >= order_quantity]\n\n        if not can_fulfill:\n            return TOP\n\n        # Find the lowest price among those that can fulfill\n        min_price = min(s[\"price\"] for s in can_fulfill)\n        cheapest = [s for s in can_fulfill if s[\"price\"] == min_price]\n\n        # Generate purchase order plans for each cheapest supplier\n        plans = set()\n        for s in cheapest:\n            plans.add(f\"create_purchase_order(product_id={product_id},quantity={order_quantity},supplier_id={s['supplier_id']})\")\n        return plans\n\n    # No predicates applied: permit all supplier options\n    plans = set()\n    for s in suppliers:\n        plans.add(f\"create_purchase_order(product_id={product_id},quantity={order_quantity},supplier_id={s['supplier_id']})\")\n    return plans\n",
 "gen2|q0|S1|5": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"the lowest-cost supplier that can fulfill the quantity\"},\n}\n\ndef answer(state, drop=frozenset()):\n    # Generate candidate commit plans: one per supplier listing for PROD-001\n    candidates = []\n    for supplier in state[\"supplier_listings\"]:\n        if supplier[\"product_id\"] == \"PROD-001\":\n            candidates.append({\n                \"supplier_id\": supplier[\"supplier_id\"],\n                \"price\": supplier[\"price\"],\n                \"available_quantity\": supplier[\"available_quantity\"],\n            })\n\n    original_count = len(candidates)\n\n    # Apply forbid predicate P1 (unless dropped)\n    if \"P1\" not in drop:\n        forbidden = set()\n        for contract in state[\"supplier_contracts\"]:\n            if contract[\"product_id\"] == \"PROD-001\":\n                if not contract[\"active\"] or not contract[\"ordering_permitted\"]:\n                    forbidden.add(contract[\"supplier_id\"])\n        candidates = [c for c in candidates if c[\"supplier_id\"] not in forbidden]\n        # If forbid ruled out every existing candidate, the answer is {\"NONE\"}\n        if original_count > 0 and not candidates:\n            return {\"NONE\"}\n\n    # Apply select predicate P2 (unless dropped)\n    if \"P2\" not in drop:\n        # Presupposition: at least one remaining supplier can fulfill the quantity\n        fulfilling = [c for c in candidates if c[\"available_quantity\"] >= 100]\n        if not fulfilling:\n            return TOP  # presupposition fails\n        # Select the lowest-cost supplier(s)\n        min_price = min(c[\"price\"] for c in fulfilling)\n        candidates = [c for c in fulfilling if c[\"price\"] == min_price]\n\n    # If no candidates remain after all predicates, return {\"NONE\"}\n    if not candidates:\n        return {\"NONE\"}\n\n    # Format permitted plans\n    result = set()\n    for c in candidates:\n        args = {\n            \"product_id\": \"PROD-001\",\n            \"quantity\": \"100\",\n            \"supplier_id\": c[\"supplier_id\"],\n        }\n        sorted_args = \",\".join(f\"{k}={args[k]}\" for k in sorted(args.keys()))\n        result.add(f\"create_purchase_order({sorted_args})\")\n\n    return result\n",
 "gen2|q0|S2|0": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n}\n\ndef answer(state, drop=frozenset()):\n    # Generate candidate plans:\n    # The order directs creating a purchase order for PROD-001, quantity 100,\n    # from suppliers in the supplier_listings for that product.\n    supplier_listings = state.get(\"supplier_listings\", [])\n    prod_listings = [s for s in supplier_listings if s[\"product_id\"] == \"PROD-001\"]\n\n    # Collect unique suppliers for PROD-001, mapping supplier_id to price info\n    supplier_map = {}  # supplier_id -> first listing (used for price/availability)\n    for s in prod_listings:\n        sid = s[\"supplier_id\"]\n        if sid not in supplier_map:\n            supplier_map[sid] = s\n\n    # Build candidate plans: one per unique supplier\n    candidates = {}  # supplier_id -> plan string\n    for sid in supplier_map:\n        plan = f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\"\n        candidates[sid] = plan\n\n    # If there are no candidate plans at all, TOP (nothing to select from / no restriction)\n    if not candidates:\n        return TOP\n\n    # Apply P1 (forbid): Only order from a supplier whose contract for this product\n    # is active and permits ordering.\n    if \"P1\" not in drop:\n        contracts = state.get(\"supplier_contracts\", [])\n        permitted_supplier_ids = set()\n        for c in contracts:\n            if (c[\"product_id\"] == \"PROD-001\" and c[\"active\"] and c[\"ordering_permitted\"]):\n                permitted_supplier_ids.add(c[\"supplier_id\"])\n        # Remove candidates whose supplier is not in the permitted set\n        candidates = {\n            sid: plan for sid, plan in candidates.items()\n            if sid in permitted_supplier_ids\n        }\n        # Forbid ruling out everything => {\"NONE\"}\n        if not candidates:\n            return {\"NONE\"}\n\n    # Apply P2 (select): lowest-cost supplier that can fulfill the quantity.\n    if \"P2\" not in drop:\n        # Find suppliers among remaining candidates that can fulfill quantity 100\n        eligible = {}  # supplier_id -> price\n        for sid, plan in candidates.items():\n            listing = supplier_map.get(sid)\n            if listing is not None and listing.get(\"available_quantity\", 0) >= 100:\n                eligible[sid] = listing[\"price\"]\n\n        # Presupposition of select: at least one supplier can fulfill the quantity\n        if not eligible:\n            return TOP\n\n        min_price = min(eligible.values())\n        # Select all suppliers with the lowest price among those fulfilling quantity\n        selected = {sid for sid, price in eligible.items() if price == min_price}\n        candidates = {sid: plan for sid, plan in candidates.items() if sid in selected}\n\n    # Remaining candidates are the permitted plans\n    return set(candidates.values())\n",
 "gen2|q0|S2|1": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"for canned Coke (PROD-001)\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"for 100 cans\"},\n    \"P3\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P4\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Ground potential plans in current supplier listings\n    candidates = [\n        {\n            \"product_id\": l[\"product_id\"],\n            \"supplier_id\": l[\"supplier_id\"],\n            \"price\": l[\"price\"],\n            \"available_quantity\": l[\"available_quantity\"]\n        }\n        for l in state.get(\"supplier_listings\", [])\n    ]\n    \n    # P1: select product_id == \"PROD-001\"\n    if \"P1\" not in drop:\n        candidates = [c for c in candidates if c[\"product_id\"] == \"PROD-001\"]\n        if not candidates:\n            return TOP\n            \n    # P3: select lowest-cost supplier that can fulfill the quantity\n    if \"P3\" not in drop:\n        fulfillable = [c for c in candidates if c[\"available_quantity\"] >= 100]\n        if not fulfillable:\n            return TOP\n        min_price = min(c[\"price\"] for c in fulfillable)\n        candidates = [c for c in fulfillable if c[\"price\"] == min_price]\n        \n    # P4: forbid if contract not active or not permitting\n    if \"P4\" not in drop:\n        contracts = state.get(\"supplier_contracts\", [])\n        valid_keys = {(ct[\"supplier_id\"], ct[\"product_id\"]) for ct in contracts \n                      if ct.get(\"active\") and ct.get(\"ordering_permitted\")}\n        candidates = [c for c in candidates if (c[\"supplier_id\"], c[\"product_id\"]) in valid_keys]\n        if not candidates:\n            return {\"NONE\"}\n            \n    if not candidates:\n        return {\"NONE\"}\n        \n    plans = set()\n    for c in candidates:\n        pid = c[\"product_id\"] if \"P1\" not in drop else \"*\"\n        qty = \"100\" if \"P2\" not in drop else \"*\"\n        sid = c[\"supplier_id\"]\n        plan = f\"create_purchase_order(product_id={pid},quantity={qty},supplier_id={sid})\"\n        plans.add(plan)\n    return plans\n",
 "gen2|q0|S2|2": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"from the lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering\"},\n}\n\n\ndef _parse_plan(plan_str):\n    \"\"\"Parse a plan string like create_purchase_order(product_id=X,supplier_id=Y,quantity=Z).\"\"\"\n    m = re.match(r'^create_purchase_order\\(([^)]+)\\)$', plan_str)\n    if not m:\n        return None\n    args = {}\n    for part in m.group(1).split(','):\n        k, v = part.split('=', 1)\n        args[k.strip()] = v.strip()\n    return args\n\n\ndef answer(state, drop=frozenset()):\n    # Collect all supplier listings for PROD-001\n    supplier_listings = state.get(\"supplier_listings\", [])\n    prod_suppliers = [s for s in supplier_listings if s[\"product_id\"] == \"PROD-001\"]\n\n    # Candidate plans: one per supplier for this product, with fixed product_id and quantity\n    candidates = set()\n    for s in prod_suppliers:\n        plan = \"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={})\".format(s[\"supplier_id\"])\n        candidates.add(plan)\n\n    # Apply forbid predicates\n    if \"P2\" not in drop:\n        # Only order from a supplier whose contract for this product is active and permits ordering\n        # Find suppliers whose contract for PROD-001 is NOT (active AND permits ordering)\n        bad_suppliers = set()\n        for contract in state.get(\"supplier_contracts\", []):\n            if contract[\"product_id\"] == \"PROD-001\":\n                if not contract[\"active\"] or not contract[\"ordering_permitted\"]:\n                    bad_suppliers.add(contract[\"supplier_id\"])\n        # Remove plans using bad suppliers\n        candidates = {\n            p for p in candidates\n            if _parse_plan(p) and _parse_plan(p)[\"supplier_id\"] not in bad_suppliers\n        }\n        if not candidates:\n            return {\"NONE\"}\n\n    # Apply select predicates\n    if \"P1\" not in drop:\n        # from the lowest-cost supplier that can fulfill the quantity\n        # Presupposition: there exists at least one supplier with available_quantity >= 100\n        fulfilling_suppliers = [\n            s for s in prod_suppliers\n            if s[\"available_quantity\"] >= 100\n            and s[\"supplier_id\"] not in bad_suppliers if \"P2\" not in drop else True\n        ]\n        if not fulfilling_suppliers:\n            return TOP\n\n        # Find the lowest price among fulfilling suppliers\n        min_price = min(s[\"price\"] for s in fulfilling_suppliers)\n\n        # Select plans using suppliers at the minimum price\n        best_supplier_ids = {s[\"supplier_id\"] for s in fulfilling_suppliers if s[\"price\"] == min_price}\n        candidates = {\n            p for p in candidates\n            if _parse_plan(p) and _parse_plan(p)[\"supplier_id\"] in best_supplier_ids\n        }\n\n    return candidates if candidates else TOP\n",
 "gen2|q0|S2|3": "TOP = \"TOP\"   # return this when the order excludes nothing\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering\"}\n}\n\ndef answer(state, drop=frozenset()):\n    suppliers = state[\"supplier_listings\"]\n    contracts = state[\"supplier_contracts\"]\n    \n    candidates = set()\n    sup_to_commit = {}\n    for s in suppliers:\n        if s[\"product_id\"] == \"PROD-001\":\n            sid = s[\"supplier_id\"]\n            commit_str = f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\"\n            candidates.add(commit_str)\n            sup_to_commit[sid] = commit_str\n            \n    if \"P2\" not in drop:\n        new_cands = set()\n        for sid, cstr in sup_to_commit.items():\n            for c in contracts:\n                if c[\"supplier_id\"] == sid and c[\"product_id\"] == \"PROD-001\" and c[\"active\"] and c[\"ordering_permitted\"]:\n                    new_cands.add(cstr)\n                    break\n        if not new_cands:\n            return TOP\n        candidates = new_cands\n        sup_to_commit = {sid: c for sid, c in sup_to_commit.items() if c in new_cands}\n        \n    if \"P1\" not in drop:\n        prices = []\n        for sid, cstr in sup_to_commit.items():\n            listing = next((s for s in suppliers if s[\"supplier_id\"] == sid and s[\"product_id\"] == \"PROD-001\"), None)\n            if listing and listing[\"available_quantity\"] >= 100:\n                prices.append((listing[\"price\"], cstr))\n        if not prices:\n            return TOP\n        min_p = min(p for p, _ in prices)\n        new_cands = {c for p, c in prices if p == min_p}\n        if not new_cands:\n            return TOP\n        candidates = new_cands\n        \n    return candidates\n",
 "gen2|q0|S2|4": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Generate base candidates: one per supplier offering PROD-001\n    candidates = []\n    for s in state.get(\"supplier_listings\", []):\n        if s.get(\"product_id\") == \"PROD-001\":\n            sid = s[\"supplier_id\"]\n            # Arguments sorted alphabetically: product_id, quantity, supplier_id\n            cand = f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={sid})\"\n            candidates.append((sid, s, cand))\n            \n    if not candidates:\n        return {\"NONE\"}\n\n    # Apply P2 (forbid) if not dropped\n    if \"P2\" not in drop:\n        valid_sids = {\n            c[\"supplier_id\"] for c in state.get(\"supplier_contracts\", [])\n            if c.get(\"product_id\") == \"PROD-001\" \n            and c.get(\"active\") == True \n            and c.get(\"ordering_permitted\") == True\n        }\n        candidates = [(sid, s, c) for sid, s, c in candidates if sid in valid_sids]\n        if not candidates:\n            return {\"NONE\"}\n\n    # Apply P1 (select) if not dropped\n    if \"P1\" not in drop:\n        capable = [(sid, s, c) for sid, s, c in candidates if s.get(\"available_quantity\", 0) >= 100]\n        if not capable:\n            return TOP\n        min_price = min(s.get(\"price\", float('inf')) for _, s, _ in capable)\n        selected = [c for _, s, c in capable if s.get(\"price\", float('inf')) == min_price]\n        return set(selected)\n\n    # If all predicates are dropped, the order excludes nothing\n    if \"P1\" in drop and \"P2\" in drop:\n        return TOP\n        \n    # P1 dropped but P2 applied: return remaining candidates\n    return set(c for _, _, c in candidates)\n",
 "gen2|q0|S2|5": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\n        \"kind\": \"forbid\",\n        \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"\n    },\n    \"P2\": {\n        \"kind\": \"select\",\n        \"span\": \"lowest-cost supplier that can fulfill the quantity\"\n    }\n}\n\ndef answer(state, drop=frozenset()):\n    suppliers = [\n        s for s in state.get(\"supplier_listings\", [])\n        if s[\"product_id\"] == \"PROD-001\"\n    ]\n\n    # Apply P1: forbid ordering from suppliers without active,\n    # ordering-permitted contract for PROD-001\n    if \"P1\" not in drop:\n        contracts = state.get(\"supplier_contracts\", [])\n        remaining = []\n        for s in suppliers:\n            sid = s[\"supplier_id\"]\n            has_valid = any(\n                c[\"supplier_id\"] == sid\n                and c[\"product_id\"] == \"PROD-001\"\n                and c[\"active\"]\n                and c[\"ordering_permitted\"]\n                for c in contracts\n            )\n            if has_valid:\n                remaining.append(s)\n        suppliers = remaining\n\n    # Apply P2: select the lowest-cost supplier that can fulfill quantity 100\n    if \"P2\" not in drop:\n        fulfillable = [s for s in suppliers if s[\"available_quantity\"] >= 100]\n        if not fulfillable:\n            # Select's presupposition fails — return TOP\n            return TOP\n        best_price = min(s[\"price\"] for s in fulfillable)\n        suppliers = [s for s in fulfillable if s[\"price\"] == best_price]\n\n    # Build permitted plans from surviving suppliers\n    plans = set()\n    for s in suppliers:\n        plans.add(\n            'create_purchase_order(product_id=\"PROD-001\",quantity=\"100\",supplier_id=\"' + s[\"supplier_id\"] + '\")'\n        )\n    return plans\n",
 "gen2|q0|S3|0": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather candidate suppliers for the specified product\n    listings = [s for s in state[\"supplier_listings\"] if s[\"product_id\"] == \"PROD-001\"]\n    if not listings:\n        return TOP\n\n    def contract_ok(sup_id):\n        contracts = [c for c in state[\"supplier_contracts\"]\n                     if c[\"supplier_id\"] == sup_id and c[\"product_id\"] == \"PROD-001\"]\n        return any(c.get(\"active\") and c.get(\"ordering_permitted\") for c in contracts)\n\n    current = listings\n\n    # P1: Forbid suppliers without a valid, active, ordering-permitted contract\n    if \"P1\" not in drop:\n        current = [s for s in current if contract_ok(s[\"supplier_id\"])]\n        if not current:\n            return {\"NONE\"}\n\n    # P2: Select the lowest-cost supplier(s) that can fulfill the quantity (>= 100)\n    if \"P2\" not in drop:\n        fulfillers = [s for s in current if s.get(\"available_quantity\", 0) >= 100]\n        if not fulfillers:\n            return TOP\n        min_price = min(s[\"price\"] for s in fulfillers)\n        current = [s for s in fulfillers if s[\"price\"] == min_price]\n\n    # Format permitted plans with arguments sorted alphabetically\n    return {f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={s['supplier_id']})\" for s in current}\n",
 "gen2|q0|S3|1": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather all supplier listings for the target product\n    suppliers = [s for s in state[\"supplier_listings\"] if s[\"product_id\"] == \"PROD-001\"]\n    \n    # Generate all potential commit plans\n    candidates = set()\n    for s in suppliers:\n        candidates.add(f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={s['supplier_id']})\")\n        \n    if not candidates:\n        return {\"NONE\"}\n        \n    # Apply P1 (select): lowest-cost among those that can fulfill quantity\n    if \"P1\" not in drop:\n        fulfillers = [s for s in suppliers if s[\"available_quantity\"] >= 100]\n        if not fulfillers:\n            return TOP\n        min_price = min(s[\"price\"] for s in fulfillers)\n        filtered = set()\n        for plan in candidates:\n            match = re.search(r'supplier_id=(\\S+)', plan)\n            if match:\n                sid = match.group(1)\n                s = next((x for x in suppliers if x[\"supplier_id\"] == sid), None)\n                if s and s[\"available_quantity\"] >= 100 and s[\"price\"] == min_price:\n                    filtered.add(plan)\n        candidates = filtered\n        if not candidates:\n            return {\"NONE\"}\n            \n    # Apply P2 (forbid): restrict to suppliers with valid contracts\n    if \"P2\" not in drop:\n        filtered = set()\n        for plan in candidates:\n            match = re.search(r'supplier_id=(\\S+)', plan)\n            if match:\n                sid = match.group(1)\n                contracts = [c for c in state[\"supplier_contracts\"] \n                             if c[\"supplier_id\"] == sid and c[\"product_id\"] == \"PROD-001\"]\n                if any(c[\"active\"] and c[\"ordering_permitted\"] for c in contracts):\n                    filtered.add(plan)\n        candidates = filtered\n        \n    if not candidates:\n        return {\"NONE\"}\n    return candidates\n",
 "gen2|q0|S3|2": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather all suppliers relevant to the target product\n    all_suppliers = [s for s in state.get(\"supplier_listings\", []) if s[\"product_id\"] == \"PROD-001\"]\n    \n    # P1: Select the lowest-cost supplier(s) capable of fulfilling 100 units\n    if \"P1\" not in drop:\n        can_fulfill = [s for s in all_suppliers if s.get(\"available_quantity\", 0) >= 100]\n        if not can_fulfill:\n            return TOP  # Presupposition of the select predicate fails\n        min_price = min(s[\"price\"] for s in can_fulfill)\n        candidates = [s for s in can_fulfill if s[\"price\"] == min_price]\n    else:\n        # If P1 is dropped, the selection constraint is removed per instructions\n        candidates = all_suppliers\n        \n    plans = set()\n    for sup in candidates:\n        # Format the commit call with arguments sorted alphabetically\n        plan = f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sup['supplier_id']})\"\n        \n        # P2: Forbid the order if the selected supplier's contract is inactive or does not permit ordering\n        if \"P2\" not in drop:\n            contract_ok = False\n            for c in state.get(\"supplier_contracts\", []):\n                if c[\"supplier_id\"] == sup[\"supplier_id\"] and c[\"product_id\"] == \"PROD-001\":\n                    if c.get(\"active\", False) and c.get(\"ordering_permitted\", False):\n                        contract_ok = True\n                    break\n            if not contract_ok:\n                continue  # This plan is forbidden by the veto reading\n                \n        plans.add(plan)\n        \n    return {\"NONE\"} if not plans else plans\n",
 "gen2|q0|S3|3": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather suppliers for the target product\n    suppliers = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == \"PROD-001\"]\n    \n    candidates = []\n    for s in suppliers:\n        candidates.append({\n            \"supplier_id\": s[\"supplier_id\"],\n            \"price\": s[\"price\"],\n            \"available\": s[\"available_quantity\"],\n            \"plan\": f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={s['supplier_id']})\"\n        })\n    \n    # Apply forbid predicate P1\n    if \"P1\" not in drop:\n        filtered = []\n        for c in candidates:\n            sid = c[\"supplier_id\"]\n            contracts = [ct for ct in state.get(\"supplier_contracts\", []) \n                         if ct.get(\"supplier_id\") == sid and ct.get(\"product_id\") == \"PROD-001\"]\n            # A supplier is permitted if at least one matching contract is active and permits ordering\n            if any(ct.get(\"active\") and ct.get(\"ordering_permitted\") for ct in contracts):\n                filtered.append(c)\n        candidates = filtered\n        \n    # Apply select predicate P2\n    if \"P2\" not in drop:\n        fulfillers = [c for c in candidates if c[\"available\"] >= 100]\n        if not fulfillers:\n            return TOP  # Select presupposition fails\n        min_price = min(c[\"price\"] for c in fulfillers)\n        candidates = [c for c in fulfillers if c[\"price\"] == min_price]\n        \n    # Final resolution\n    if not candidates:\n        return {\"NONE\"}\n        \n    return {c[\"plan\"] for c in candidates}\n",
 "gen2|q0|S3|4": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather all supplier listings for the target product\n    listings = [l for l in state.get(\"supplier_listings\", []) if l.get(\"product_id\") == \"PROD-001\"]\n    sid_to_listing = {l[\"supplier_id\"]: l for l in listings}\n    sids = list(sid_to_listing.keys())\n\n    # Apply forbid predicate (P1) if not dropped\n    if \"P1\" not in drop:\n        valid_sids = []\n        for sid in sids:\n            contracts = [c for c in state.get(\"supplier_contracts\", [])\n                         if c.get(\"supplier_id\") == sid and c.get(\"product_id\") == \"PROD-001\"]\n            if any(c.get(\"active\") is True and c.get(\"ordering_permitted\") is True for c in contracts):\n                valid_sids.append(sid)\n        sids = valid_sids\n        if not sids:\n            return {\"NONE\"}\n\n    # Apply select predicate (P2) if not dropped\n    if \"P2\" not in drop:\n        # Filter by availability to fulfill the quantity of 100\n        avail_sids = [sid for sid in sids if sid_to_listing[sid].get(\"available_quantity\", 0) >= 100]\n        if not avail_sids:\n            return TOP\n        # Select lowest-cost among those that can fulfill\n        min_price = min(sid_to_listing[sid].get(\"price\", float('inf')) for sid in avail_sids)\n        sids = [sid for sid in avail_sids if sid_to_listing[sid].get(\"price\") == min_price]\n\n    # Fallback safety (should be caught by predicate logic above)\n    if not sids:\n        return {\"NONE\"}\n\n    # Construct permitted plans\n    plans = {\n        f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\"\n        for sid in sids\n    }\n    return plans\n",
 "gen2|q0|S3|5": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Base candidates: all supplier listings for the target product\n    candidates = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == \"PROD-001\"]\n\n    # P1: Forbid ordering from suppliers without an active, ordering-permitted contract\n    if \"P1\" not in drop:\n        valid_ids = {\n            c[\"supplier_id\"] for c in state.get(\"supplier_contracts\", [])\n            if c.get(\"product_id\") == \"PROD-001\" and c.get(\"active\") is True and c.get(\"ordering_permitted\") is True\n        }\n        candidates = [s for s in candidates if s[\"supplier_id\"] in valid_ids]\n        if not candidates:\n            return {\"NONE\"}\n\n    # P2: Select the lowest-cost supplier(s) that can fulfill the requested quantity (100)\n    if \"P2\" not in drop:\n        candidates = [s for s in candidates if s.get(\"available_quantity\", 0) >= 100]\n        if not candidates:\n            return TOP  # Select presupposition fails\n        min_price = min(s[\"price\"] for s in candidates)\n        candidates = [s for s in candidates if s[\"price\"] == min_price]\n\n    # Fallback if candidates vanished unexpectedly (e.g., empty state with P1 active)\n    if not candidates:\n        return {\"NONE\"}\n\n    # If both decision predicates are dropped, the order leaves the supplier choice entirely open\n    if \"P1\" in drop and \"P2\" in drop:\n        return {\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id=*)\"}\n\n    # Return one concrete plan for each permitted supplier\n    return {f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={s['supplier_id']})\" for s in candidates}\n",
 "gen2|q0|S3b|0": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    qty = \"100\"\n    \n    listings = [r for r in state.get(\"supplier_listings\", []) if r[\"product_id\"] == product_id]\n    \n    # Generate base candidates for every supplier listing\n    candidates = {\n        f\"create_purchase_order(product_id={product_id},quantity={qty},supplier_id={r['supplier_id']})\"\n        for r in listings\n    }\n        \n    if not candidates:\n        return TOP\n\n    if \"P1\" not in drop:\n        # Non-obvious reading #1: Pick min price overall, then check availability >= 100\n        min_price = min(r[\"price\"] for r in listings)\n        valid_sids = {\n            r[\"supplier_id\"] \n            for r in listings \n            if r[\"price\"] == min_price and r[\"available_quantity\"] >= 100\n        }\n        \n        if not valid_sids:\n            return TOP\n            \n        candidates = {\n            c for c in candidates \n            if any(c.endswith(f\"supplier_id={sid}\") for sid in valid_sids)\n        }\n\n    if \"P2\" not in drop:\n        # Forbid if contract not active or not permitting ordering\n        allowed_sids = set()\n        for c in state.get(\"supplier_contracts\", []):\n            if (c[\"product_id\"] == product_id and \n                c.get(\"active\") == True and \n                c.get(\"ordering_permitted\") == True):\n                allowed_sids.add(c[\"supplier_id\"])\n                \n        new_candidates = set()\n        for c in candidates:\n            m = re.search(r\"supplier_id=(\\S+)\", c)\n            if m and m.group(1) in allowed_sids:\n                new_candidates.add(c)\n        candidates = new_candidates\n        \n        if not candidates:\n            return {\"NONE\"}\n            \n    return candidates\n",
 "gen2|q0|S3b|1": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    listings = state.get(\"supplier_listings\", [])\n    contracts = state.get(\"supplier_contracts\", [])\n    \n    # Initial candidates: most obvious reading of \"available suppliers for canned Coke (PROD-001)\"\n    candidates = [s for s in listings if s.get(\"product_id\") == \"PROD-001\"]\n    \n    # Apply forbid predicate P1 unless dropped\n    if \"P1\" not in drop:\n        valid = []\n        for c in candidates:\n            sid = c.get(\"supplier_id\")\n            pid = c.get(\"product_id\")\n            matching = [ct for ct in contracts if ct.get(\"supplier_id\") == sid and ct.get(\"product_id\") == pid]\n            # Keep candidate only if at least one contract meets both conditions\n            if any(ct.get(\"active\") is True and ct.get(\"ordering_permitted\") is True for ct in matching):\n                valid.append(c)\n        candidates = valid\n        if not candidates:\n            return {\"NONE\"}\n            \n    # Apply select predicate P2 unless dropped\n    if \"P2\" not in drop:\n        # Non-obvious reading #2: \"fulfill the quantity\" means exactly 100\n        fulfill = [c for c in candidates if c.get(\"available_quantity\") == 100]\n        if not fulfill:\n            return TOP\n        # Select lowest cost; ties preserved per instructions\n        min_price = min(c.get(\"price\", float(\"inf\")) for c in fulfill)\n        candidates = [c for c in fulfill if c.get(\"price\") == min_price]\n        \n    # Generate permitted plans\n    plans = set()\n    for c in candidates:\n        plans.add(f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={c.get('supplier_id')})\")\n        \n    if not plans:\n        # Reaching here with empty plans means either all predicates were dropped \n        # and no initial candidates existed, or an edge case where no restrictions apply.\n        return TOP\n    return plans\n",
 "gen2|q0|S3b|2": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    qty = \"100\"\n    \n    suppliers = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == product_id]\n    \n    candidates = {\"NONE\"}\n    for s in suppliers:\n        candidates.add(f\"create_purchase_order(product_id={product_id},quantity={qty},supplier_id={s['supplier_id']})\")\n        \n    if \"P2\" not in drop:\n        # Reading 3: \"fulfill the quantity\" means exactly 100 units available\n        eligible = [s for s in suppliers if s.get(\"available_quantity\") == 100]\n        if eligible:\n            min_price = min(s[\"price\"] for s in eligible)\n            best = [s for s in eligible if s[\"price\"] == min_price]\n            new_candidates = set()\n            for s in best:\n                new_candidates.add(f\"create_purchase_order(product_id={product_id},quantity={qty},supplier_id={s['supplier_id']})\")\n            if new_candidates:\n                candidates = new_candidates\n            else:\n                return TOP\n        else:\n            return TOP\n            \n    if \"P1\" not in drop:\n        contracts = [c for c in state.get(\"supplier_contracts\", []) if c.get(\"product_id\") == product_id]\n        \n        valid_plans = set()\n        for plan in candidates:\n            if plan == \"NONE\":\n                valid_plans.add(plan)\n            elif \"supplier_id=\" in plan:\n                sid = plan.split(\"supplier_id=\")[1].split(\")\")[0]\n                for c in contracts:\n                    if c.get(\"supplier_id\") == sid and c.get(\"active\") is True and c.get(\"ordering_permitted\") is True:\n                        valid_plans.add(plan)\n                        break\n        candidates = valid_plans\n        if not candidates:\n            return {\"NONE\"}\n            \n    return candidates\n",
 "gen2|q0|S3b|3": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"place an order for 100 cans from the lowest-cost supplier that can fulfill the quantity\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Constants from the standing order\n    product_id = \"PROD-001\"\n    required_qty = 100\n    order_qty_str = \"100\"\n    \n    # If the selection predicate is dropped, the order to act is removed.\n    # With no selection, we return TOP (excludes nothing specific).\n    if \"P1\" in drop:\n        return TOP\n\n    # Retrieve relevant data from state\n    listings = state.get(\"supplier_listings\", [])\n    contracts = state.get(\"supplier_contracts\", [])\n    \n    # P1: Select Logic\n    # 1. Filter for the specific product\n    candidates = [s for s in listings if s.get(\"product_id\") == product_id]\n    \n    # 2. Filter for fulfillment capacity (\"can fulfill the quantity\")\n    candidates = [s for s in candidates if s.get(\"available_quantity\", 0) >= required_qty]\n    \n    # If no candidates satisfy the presupposition of P1, return TOP\n    if not candidates:\n        return TOP\n    \n    # Non-obvious Reading 4: \"compare their current unit prices and availability\"\n    # Interpretation: Sort primarily by price (lowest-cost), secondarily by availability (descending) to break ties.\n    # This implies that among equally low-cost suppliers, the one with more availability is preferred/selected.\n    candidates.sort(key=lambda s: (s.get(\"price\", float('inf')), -s.get(\"available_quantity\", 0)))\n    \n    # Identify the best candidates based on the sort criteria\n    if candidates:\n        best_price = candidates[0][\"price\"]\n        # Get all suppliers with the best price\n        price_winners = [s for s in candidates if s.get(\"price\") == best_price]\n        \n        # Apply the secondary sort criterion (availability) as a tie-breaker filter\n        # We select only those with the maximum availability among the price winners\n        max_avail = max(s.get(\"available_quantity\", 0) for s in price_winners)\n        final_winners = [s for s in price_winners if s.get(\"available_quantity\", 0) == max_avail]\n    else:\n        final_winners = []\n        \n    # Generate potential plans for the selected winners\n    plans = set()\n    for s in final_winners:\n        supplier_id = s.get(\"supplier_id\")\n        plans.add(f'create_purchase_order(product_id=\"{product_id}\",quantity=\"{order_qty_str}\",supplier_id=\"{supplier_id}\")')\n        \n    # P2: Forbid Logic\n    # If P2 is not dropped, filter out plans for suppliers without valid contracts\n    if \"P2\" not in drop:\n        allowed_plans = set()\n        for s in final_winners:\n            supplier_id = s.get(\"supplier_id\")\n            plan = f'create_purchase_order(product_id=\"{product_id}\",quantity=\"{order_qty_str}\",supplier_id=\"{supplier_id}\")'\n            \n            # Check if there is an active contract permitting ordering for this supplier and product\n            has_valid_contract = False\n            for c in contracts:\n                if (c.get(\"supplier_id\") == supplier_id and \n                    c.get(\"product_id\") == product_id and\n                    c.get(\"active\") == True and\n                    c.get(\"ordering_permitted\") == True):\n                    has_valid_contract = True\n                    break\n            \n            if has_valid_contract:\n                allowed_plans.add(plan)\n        plans = allowed_plans\n    \n    # If all candidates were forbidden, return {\"NONE\"}\n    if not plans:\n        return {\"NONE\"}\n        \n    return plans\n",
 "gen2|q0|S3b|4": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    suppliers = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == \"PROD-001\"]\n    candidates = {f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={s['supplier_id']})\" for s in suppliers}\n    \n    def get_supplier_id(plan):\n        m = re.search(r\"supplier_id=([^,\\)]+)\", plan)\n        return m.group(1) if m else None\n\n    # P1: Forbid condition\n    # Reading #5 applied: permit if contract restrictions do not contain \"blocked\"\n    if \"P1\" not in drop:\n        valid_sids = set()\n        for s in suppliers:\n            sid = s[\"supplier_id\"]\n            contracts = [c for c in state.get(\"supplier_contracts\", []) \n                         if c.get(\"supplier_id\") == sid and c.get(\"product_id\") == \"PROD-001\"]\n            if contracts:\n                # Reading #5: check restrictions field instead of active/permits booleans\n                if any(\"blocked\" not in (c.get(\"restrictions\") or \"\").lower() for c in contracts):\n                    valid_sids.add(sid)\n        candidates = {c for c in candidates if get_supplier_id(c) in valid_sids}\n        \n    forbid_eliminated_all = (len(candidates) == 0 and \"P1\" not in drop)\n    \n    # P2: Select condition\n    # Obvious reading: lowest unit price among suppliers with available_quantity >= 100\n    if \"P2\" not in drop:\n        eligible = []\n        min_price = float('inf')\n        for s in suppliers:\n            sid = s[\"supplier_id\"]\n            plan = f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={sid})\"\n            if plan in candidates:\n                if s.get(\"available_quantity\", 0) >= 100:\n                    price = s.get(\"price\", float('inf'))\n                    if price < min_price:\n                        min_price = price\n                        eligible = [s]\n                    elif price == min_price:\n                        eligible.append(s)\n                        \n        if not eligible:\n            return TOP\n        candidates = {f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={s['supplier_id']})\" for s in eligible}\n        \n    if forbid_eliminated_all:\n        return {\"NONE\"}\n    if not candidates:\n        return {\"NONE\"}\n    return candidates\n",
 "gen2|q0|S3b|5": "TOP = \"TOP\"   # return this when the order excludes nothing\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"from the lowest-cost supplier that can fulfill the quantity\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Identify candidate suppliers for PROD-001\n    suppliers = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == \"PROD-001\"]\n    sup_map = {s[\"supplier_id\"]: s for s in suppliers}\n    \n    # Apply P1 (forbid) if not dropped\n    if \"P1\" not in drop:\n        contracts = {(c[\"supplier_id\"], c[\"product_id\"]): c for c in state.get(\"supplier_contracts\", [])}\n        valid_sids = []\n        for sid in sup_map:\n            c = contracts.get((sid, \"PROD-001\"))\n            if c and c.get(\"active\", False) and c.get(\"ordering_permitted\", False):\n                valid_sids.append(sid)\n        if not valid_sids:\n            return {\"NONE\"}\n        sup_map = {sid: sup_map[sid] for sid in valid_sids}\n        \n    # Apply P2 (select) if not dropped\n    if \"P2\" not in drop:\n        # Non-obvious reading #6: \"that can fulfill the quantity\" means available_quantity == 100\n        eligible_sids = [sid for sid in sup_map if sup_map[sid].get(\"available_quantity\", 0) == 100]\n        if not eligible_sids:\n            return TOP\n        min_price = min(sup_map[sid][\"price\"] for sid in eligible_sids)\n        selected_sids = [sid for sid in eligible_sids if sup_map[sid][\"price\"] == min_price]\n    else:\n        selected_sids = list(sup_map.keys())\n        \n    # If all predicates are dropped, the order excludes nothing\n    if \"P1\" in drop and \"P2\" in drop:\n        return TOP\n        \n    return {f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\" for sid in selected_sids}\n",
 "gen2|qm|S1|0": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    quantity = 100\n    \n    # Identify suppliers that currently list the product\n    potential_sids = {\n        s[\"supplier_id\"] \n        for s in state.get(\"supplier_listings\", []) \n        if s[\"product_id\"] == product_id\n    }\n    \n    valid_sids = set()\n    for sid in potential_sids:\n        if \"P1\" in drop:\n            valid_sids.add(sid)\n        else:\n            # Enforce contract condition: active and permits ordering\n            for c in state.get(\"supplier_contracts\", []):\n                if c[\"supplier_id\"] == sid and c[\"product_id\"] == product_id:\n                    if c.get(\"active\") is True and c.get(\"ordering_permitted\") is True:\n                        valid_sids.add(sid)\n                    break  # Evaluate first matching contract for this pair\n                    \n    if not valid_sids:\n        return {\"NONE\"}\n        \n    plans = set()\n    for sid in valid_sids:\n        # Arguments sorted alphabetically: product_id, quantity, supplier_id\n        plans.add(f\"create_purchase_order(product_id={product_id}, quantity={quantity}, supplier_id={sid})\")\n    return plans\n",
 "gen2|qm|S1|1": "TOP = \"TOP\"   # return this when the order excludes nothing\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    plan_template = \"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={})\"\n    \n    listings = state.get(\"supplier_listings\", [])\n    contracts = state.get(\"supplier_contracts\", [])\n    \n    if \"P1\" not in drop:\n        valid_ids = set()\n        contract_ok = {\n            c[\"supplier_id\"] for c in contracts\n            if c.get(\"product_id\") == \"PROD-001\" \n            and c.get(\"active\") == True \n            and c.get(\"ordering_permitted\") == True\n        }\n        for s in listings:\n            if s.get(\"product_id\") == \"PROD-001\" and s.get(\"supplier_id\") in contract_ok:\n                valid_ids.add(s[\"supplier_id\"])\n                \n        if not valid_ids:\n            return {\"NONE\"}\n        return {plan_template.format(sid) for sid in valid_ids}\n    else:\n        return {plan_template.format(\"*\")}\n",
 "gen2|qm|S1|2": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\n        \"kind\": \"select\",\n        \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\",\n    },\n}\n\n\ndef answer(state, drop=frozenset()):\n    # Generate candidate plans: one create_purchase_order per supplier offering PROD-001\n    candidates = set()\n    supplier_listings = state.get(\"supplier_listings\", [])\n\n    for listing in supplier_listings:\n        if listing[\"product_id\"] == \"PROD-001\":\n            supplier_id = listing[\"supplier_id\"]\n            plan = (\n                f\"create_purchase_order(product_id=PROD-001,quantity=100,\"\n                f\"supplier_id={supplier_id})\"\n            )\n            candidates.add(plan)\n\n    # If there are no candidate plans at all, the order's presupposition fails\n    if not candidates:\n        return TOP\n\n    result = candidates\n\n    # Apply P1 (select) unless it has been dropped\n    if \"P1\" not in drop:\n        # Build the set of supplier_ids whose contract for PROD-001 is active\n        # and permits ordering\n        supplier_contracts = state.get(\"supplier_contracts\", [])\n        valid_suppliers = set()\n        for contract in supplier_contracts:\n            if (\n                contract[\"product_id\"] == \"PROD-001\n                and contract[\"active\"]\n                and contract[\"ordering_permitted\"]\n            ):\n                valid_suppliers.add(contract[\"supplier_id\"])\n\n        # Filter candidates to only those with a valid-supplier\n        filtered = set()\n        for plan in result:\n            match = re.search(r\"supplier_id=([^,)\\s]+)\", plan)\n            if match and match.group(1) in valid_suppliers:\n                filtered.add(plan)\n\n        # If the select predicate's presupposition fails (nothing satisfies it),\n        # return TOP — the order then excludes nothing.\n        if not filtered:\n            return TOP\n\n        result = filtered\n\n    return result\n",
 "gen2|qm|S2|0": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"place an order for 100 cans.\"},\n    \"P2\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Base candidate derived from fixed parameters in the order\n    candidates = {\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id=*)\"}\n    \n    # Apply P1 if not dropped\n    if \"P1\" not in drop:\n        # select: keep only plans matching the quantity constraint\n        filtered = {p for p in candidates if \"quantity=100\" in p}\n        if not filtered:\n            return TOP\n        candidates = filtered\n        \n    # Apply P2 if not dropped\n    if \"P2\" not in drop:\n        # forbid: rule out plans if no valid contract exists for the product\n        contracts = state.get(\"supplier_contracts\", [])\n        has_valid_contract = any(\n            c.get(\"product_id\") == \"PROD-001\" and c.get(\"active\") and c.get(\"ordering_permitted\")\n            for c in contracts\n        )\n        if not has_valid_contract:\n            candidates = set()\n            \n    return {\"NONE\"} if not candidates else candidates\n",
 "gen2|qm|S2|1": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    if \"P1\" in drop:\n        return TOP\n\n    valid_sids = set()\n    for listing in state.get(\"supplier_listings\", []):\n        if listing.get(\"product_id\") == \"PROD-001\":\n            sid = listing.get(\"supplier_id\")\n            for contract in state.get(\"supplier_contracts\", []):\n                if (contract.get(\"supplier_id\") == sid and\n                    contract.get(\"product_id\") == \"PROD-001\" and\n                    contract.get(\"active\") is True and\n                    contract.get(\"ordering_permitted\") is True):\n                    valid_sids.add(sid)\n                    break\n\n    if not valid_sids:\n        return TOP\n\n    return {f\"create_purchase_order(product_id=PROD-001, quantity=100, supplier_id={sid})\" for sid in valid_sids}\n",
 "gen2|qm|S2|2": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Base action specified by the order\n    base_plan = \"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={})\"\n    \n    if \"P1\" in drop:\n        # Predicate removed: supplier_id remains open\n        return {base_plan.format(\"*\")}\n    \n    # Apply P1: select suppliers with active, ordering-permitted contracts for PROD-001\n    valid_suppliers = [\n        c[\"supplier_id\"] for c in state.get(\"supplier_contracts\", [])\n        if c.get(\"product_id\") == \"PROD-001\" \n        and c.get(\"active\") == True \n        and c.get(\"ordering_permitted\") == True\n    ]\n    \n    # If no supplier satisfies the condition, the select predicate's presupposition fails -> TOP\n    if not valid_suppliers:\n        return TOP\n        \n    # Return all concrete plans that satisfy the constraint\n    return {base_plan.format(sid) for sid in valid_suppliers}\n",
 "gen2|qm|S3|0": "TOP = \"TOP\"   # return this when the order excludes nothing\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Identify all suppliers listed for the target product\n    supplier_ids = {\n        s[\"supplier_id\"] for s in state.get(\"supplier_listings\", [])\n        if s.get(\"product_id\") == \"PROD-001\"\n    }\n    \n    if not supplier_ids:\n        return {\"NONE\"}\n        \n    # Apply P1 (forbid) unless dropped\n    if \"P1\" not in drop:\n        valid_sids = {\n            c[\"supplier_id\"] for c in state.get(\"supplier_contracts\", [])\n            if (c.get(\"product_id\") == \"PROD-001\" and\n                c.get(\"active\") is True and\n                c.get(\"ordering_permitted\") is True)\n        }\n        supplier_ids = supplier_ids.intersection(valid_sids)\n        \n    if not supplier_ids:\n        return {\"NONE\"}\n        \n    # Construct permitted commit plans\n    return {\n        f\"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id={sid})\"\n        for sid in supplier_ids\n    }\n",
 "gen2|qm|S3|1": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"compare their current unit prices and availability, and place an order for 100 cans.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    listings = state.get(\"supplier_listings\", [])\n    contracts = state.get(\"supplier_contracts\", [])\n    \n    # Initial candidates: suppliers offering the target product\n    target_pid = \"PROD-001\"\n    initial_candidates = [s for s in listings if s.get(\"product_id\") == target_pid]\n    \n    if not initial_candidates:\n        return TOP\n\n    candidates = list(initial_candidates)\n\n    # P1: forbid suppliers without an active contract that permits ordering\n    if \"P1\" not in drop:\n        def has_valid_contract(sup):\n            for c in contracts:\n                if c.get(\"supplier_id\") == sup[\"supplier_id\"] and c.get(\"product_id\") == target_pid:\n                    return c.get(\"active\", False) and c.get(\"ordering_permitted\", False)\n            return False\n        candidates = [s for s in candidates if has_valid_contract(s)]\n        if not candidates:\n            return {\"NONE\"}\n\n    # P2: select supplier(s) with the minimum unit price\n    if \"P2\" not in drop:\n        if not candidates:\n            return TOP\n        min_price = min(s.get(\"price\", float(\"inf\")) for s in candidates)\n        candidates = [s for s in candidates if s.get(\"price\") == min_price]\n        if not candidates:\n            return TOP\n\n    # Construct permitted commit plans\n    plans = set()\n    for s in candidates:\n        sid = s[\"supplier_id\"]\n        # Arguments sorted alphabetically: product_id, quantity, supplier_id\n        plan = f\"create_purchase_order(product_id={target_pid},quantity=100,supplier_id={sid})\"\n        plans.add(plan)\n\n    return plans if plans else TOP\n",
 "gen2|qm|S3|2": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    product_id = \"PROD-001\"\n    quantity = \"100\"\n    \n    # Identify all suppliers listing the requested product\n    candidates = [\n        sup[\"supplier_id\"]\n        for sup in state.get(\"supplier_listings\", [])\n        if sup.get(\"product_id\") == product_id\n    ]\n    \n    plans = set()\n    for sup_id in candidates:\n        # Apply P1 filter unless explicitly dropped\n        if \"P1\" not in drop:\n            contract_ok = False\n            for contract in state.get(\"supplier_contracts\", []):\n                if (contract.get(\"supplier_id\") == sup_id and \n                    contract.get(\"product_id\") == product_id):\n                    if contract.get(\"active\") and contract.get(\"ordering_permitted\"):\n                        contract_ok = True\n                        break\n            if not contract_ok:\n                continue\n                \n        # Build commit string with arguments sorted alphabetically\n        plans.add(f\"create_purchase_order(product_id={product_id},quantity={quantity},supplier_id={sup_id})\")\n        \n    return {\"NONE\"} if not plans else plans\n",
 "gen2|qm|S3b|0": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\n        \"kind\": \"select\",\n        \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"\n    }\n}\n\ndef answer(state, drop=frozenset()):\n    # Base candidate plan with open supplier_id\n    plan = \"create_purchase_order(product_id=PROD-001,quantity=100,supplier_id=*)\"\n\n    if \"P1\" not in drop:\n        # Non-obvious reading 1: \"available suppliers\" implies available_quantity > 0\n        available_supplier_ids = {\n            s[\"supplier_id\"]\n            for s in state.get(\"supplier_listings\", [])\n            if s.get(\"product_id\") == \"PROD-001\" and s.get(\"available_quantity\", 0) > 0\n        }\n\n        # Check if any available supplier has a compliant contract\n        contract_ok = False\n        for sid in available_supplier_ids:\n            for c in state.get(\"supplier_contracts\", []):\n                if (c.get(\"supplier_id\") == sid and\n                    c.get(\"product_id\") == \"PROD-001\" and\n                    c.get(\"active\") is True and\n                    c.get(\"ordering_permitted\") is True):\n                    contract_ok = True\n                    break\n            if contract_ok:\n                break\n\n        # Select predicate: if presupposition fails (no valid supplier), return TOP\n        if not contract_ok:\n            return TOP\n\n    return {plan}\n",
 "gen2|qm|S3b|1": "import re\n\nTOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"forbid\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"},\n    \"P2\": {\"kind\": \"select\", \"span\": \"compare their current unit prices and availability, and place an order for 100 cans.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    # Gather relevant supplier listings for the specified product\n    listings = [s for s in state.get(\"supplier_listings\", []) if s.get(\"product_id\") == \"PROD-001\"]\n    \n    # Generate initial candidate plans: doing nothing + one commit per available supplier\n    candidates = {\"NONE\"}\n    for sup in listings:\n        sid = sup[\"supplier_id\"]\n        candidates.add(f\"create_purchase_order(product_id=PROD-001,supplier_id={sid},quantity=100)\")\n        \n    current_plans = candidates.copy()\n    \n    # Apply P1: forbid plans using suppliers without active/permitting contracts\n    if \"P1\" not in drop:\n        contracts = state.get(\"supplier_contracts\", [])\n        allowed_sids = set()\n        for sup in listings:\n            sid = sup[\"supplier_id\"]\n            # Locate the specific contract for this supplier and product\n            match = next((c for c in contracts if c.get(\"supplier_id\") == sid and c.get(\"product_id\") == \"PROD-001\"), None)\n            if match and match.get(\"active\") is True and match.get(\"ordering_permitted\") is True:\n                allowed_sids.add(sid)\n                \n        # Remove plans that do not use an allowed supplier\n        to_remove = {p for p in current_plans if p != \"NONE\" and not any(f\"supplier_id={s},\" in p for s in allowed_sids)}\n        current_plans -= to_remove\n        \n        if not current_plans:\n            return {\"NONE\"}\n            \n    # Apply P2: select the plan(s) with the lowest unit price among remaining commits\n    if \"P2\" not in drop:\n        commit_plans = [p for p in current_plans if p != \"NONE\"]\n        if not commit_plans:\n            return TOP\n            \n        # Map supplier_id to price for evaluation\n        sup_prices = {}\n        for p in commit_plans:\n            m = re.search(r\"supplier_id=([^,]+),\", p)\n            if m:\n                sid = m.group(1)\n                for sup in listings:\n                    if sup[\"supplier_id\"] == sid:\n                        sup_prices[sid] = sup.get(\"price\", float(\"inf\"))\n                        break\n                        \n        if sup_prices:\n            min_price = min(sup_prices.values())\n            selected = {\n                p for p in commit_plans \n                if sup_prices.get(re.search(r\"supplier_id=([^,]+),\", p).group(1), float(\"inf\")) == min_price\n            }\n            current_plans = selected\n            if not current_plans:\n                return TOP\n                \n    return current_plans\n",
 "gen2|qm|S3b|2": "TOP = \"TOP\"\n\nPREDICATES = {\n    \"P1\": {\"kind\": \"select\", \"span\": \"Only order from a supplier whose contract for this product is active and permits ordering.\"}\n}\n\ndef answer(state, drop=frozenset()):\n    target_product = \"PROD-001\"\n    quantity = \"100\"\n    \n    # Collect all supplier IDs listed for the target product\n    candidate_suppliers = list({\n        s[\"supplier_id\"] for s in state.get(\"supplier_listings\", [])\n        if s.get(\"product_id\") == target_product\n    })\n    \n    # Apply P1 filter unless dropped\n    if \"P1\" not in drop:\n        permitted_ids = {\n            c[\"supplier_id\"] for c in state.get(\"supplier_contracts\", [])\n            if (c.get(\"product_id\") == target_product and \n                c.get(\"active\") == True and \n                c.get(\"ordering_permitted\") == True)\n        }\n        candidate_suppliers = [s for s in candidate_suppliers if s in permitted_ids]\n        \n        # Select predicate presupposition fails -> order excludes nothing\n        if not candidate_suppliers:\n            return TOP\n            \n    plans = set()\n    for sup_id in candidate_suppliers:\n        # Arguments sorted alphabetically: product_id, quantity, supplier_id\n        plans.add(f\"create_purchase_order(product_id={target_product},quantity={quantity},supplier_id={sup_id})\")\n        \n    return plans\n"
}