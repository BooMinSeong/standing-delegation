# DECISIONS: 논점 종결 기록

지위: 저자가 실제로 내린 결정만 적는다. 에이전트나 조율자의 제안은 여기 오지 않는다. 새 증거 없이 기각안을 되살리지 않는다. 계획의 단일 출처는 `Plan.md`, 기준점은 `docs/CASE-D01.md`.

2026-09-16 첫 시도의 결정 31건(D-001~D-030)은 브랜치 `stage0-attempt1`의 같은 파일에 있다. 그중 저자가 직접 정한 것만 아래에 원래 번호로 남긴다. 번호가 비는 것은 철회된 것이다.

---

## 2026-09-16

### D-005 로컬 모델은 최신 중형 모델 (채택, 저자 지시)

로컬 3은 대형이 아니라 툴 호출과 추론 트레이스를 지원하는 최신 중형 오픈 모델로 한다. 근거: 최근 중형 모델의 툴 호출 능력이 충분하고 1노드 이하로 서빙된다. 반영: `Plan.md` §3.

### D-006 서브 에이전트 모델 (채택, 저자 지시)

`.claude/agents/*.md` 8개 전부 `model: opus`.

(2026-09-16 밤: 정의 파일 8개는 D-032로 삭제됐다. 다시 쓸 때 이 결정을 따른다.)

### D-007 로컬 모델 집합 (채택, 저자 확정)

파일럿 `Qwen/Qwen3.8-27B-FP8`(27.8B, 48GB 1장) + `zai-org/GLM-4.7-Flash`(31.2B-A3B, 80GB 1장). 본 실험 추가 `openai/gpt-oss-120b`(80GB 1장). 계열 셋이 다르다. 실재·크기·vLLM 파서는 `docs/FEASIBILITY.md`. 반영: `Plan.md` §3, §7.

### D-009 문헌의 사실 교정 (채택, 저자 확정. `docs/related/scout-2026-09-16.md`)

- `Plan.md` §10의 "Silent Assumption 루브릭(arXiv 2605.27898)"은 ID가 무관한 논문(UniACE, Pengyu Zhu)이다. 대상을 특정할 때까지 ID 없이 둔다.
- AskBench = 2602.11199, HiL-Bench = 2604.09408.
- 확인된 실재 14건, 불일치 1건(위), 미확인 1건("2025년의 추론 모델 충실성 연구"는 논문을 지목하지 않음). 반영: `Plan.md` §10.
- 관계 판정(선점·상보)은 사실이 아니라 판단이므로 여기 적지 않는다. 정찰 파일에 있다.

### D-023 하네스 검증 (채택, 저자 지시)

모델 1개(`Qwen/Qwen3.8-27B-FP8`)로 S1 50개를 돌려 러너가 끝까지 도는지만 본다. 원 논문 수치와 비교하지 않는다. 반영: `Plan.md` §2, §7.

### D-028 저자는 눈가림 판정자가 될 수 없다 (채택, 저자 지적)

저자에게 커밋 검토(전부 읽어야 함)와 눈가림 판정(정답을 보면 안 됨)을 동시에 요구했다. 둘은 같은 사람이 못 한다. 저자의 역할은 검토와 승인이다. 인간 비준 연구(`Plan.md` §6 M1, Phase 3)의 판정자는 저자가 아니어야 하고 R과 정답을 본 적이 없어야 한다. 위임의 q와 R 문장은 저자가 읽고 승인해야 "주인 = R 저자"가 성립한다.

### D-029 위임은 AgentAbstain 씨앗에서 유도한다. 지어내지 않는다 (채택, 저자 지적)

`Plan.md` §4.1: "T+ 131개를 위임 씨앗으로". §4.2: "D+는 R을 문장 하나로 명시한 판, D−는 그 문장만 지운 판." 규칙은 씨앗 지시문 안에 이미 있고 우리가 하는 일은 그 절을 지우는 것뿐이다. 지시문은 절차서가 아니라 목표만 말해야 하고 모델이 도구 순서와 분기를 찾는다. 규칙이 모든 칸을 정하지 않는 것이 실험의 목적이다. 반영: `data/delegations/D01/`.

### D-031 첫 시도를 되돌린다 (채택, 저자 지시 2026-09-16 저녁)

- 되돌리는 것: 첫 시도의 감사·반론·결정·척도·스키마·계측기·검사·상태·판독기·결합 규칙 전부. 브랜치 `stage0-attempt1`에 보존.
- 남기는 것: 위 결정들, 사실 교정, `docs/CASE-D01.md`, `docs/FEASIBILITY.md`, `docs/derivation/`, `docs/related/`, D01의 씨앗 유도 q·R, 환경 사본과 패치 기록, 에이전트 정의.
- 이유: 저자가 검증할 수 없는 속도와 양이었다. 검증은 저자가 표를 읽는 것이지 에이전트가 항목을 쌓는 것이 아니다. `docs/CASE-D01.md`가 연구가 재려던 것이고, 하루 종일 장치를 지으면서 그 표를 한 번도 만들지 않았다.
- 앞으로: 한 번에 한 논점. 저자가 정하기 전에는 짓지 않는다. 첫 실험은 CASE-D01의 여덟 상태를 프록시에서 실행해 예측 표와 나란히 놓는 것.
- 프로젝트 메모리도 같이 정리했다(첫 시도를 성과로 적은 파일 삭제, 교훈 파일 재작성).

### D-032 첫 실험의 형태 정정과 잔여 제거 (채택, 저자 지시 2026-09-16 밤)

- 단발 표 프롬프트(`docs/CASE-D01.md` §1의 형식)는 에이전트가 아닌 리즈닝 모드에서의 성능과 예시를 확보하기 위한 것이었다. 실험 설계 결정이 아니다.
- CASE-D01에서 볼 것은 단발 여부가 아니라 **갈림**이다. 상태가 어떻게 달라지고, 모델이 주인 몰래 어떤 조건을 끼워 넣어 결론을 갈라놓는가.
- 최종 관찰 대상은 에이전트·툴콜링 상황이다. 첫 실험이 프록시 실행인 것은 그대로다. (2026-09-20: 이 줄의 순서는 D-034로 바뀌었다. 프록시 실행은 B2a·B2b 뒤로 간다. 앞 두 줄은 그대로다.)
- 잔여 제거: 첫 시도의 문서 체계(LOGIC·SCHEMA·PREREG·OBJECTIONS, `src/gen/`, `checks.md`, 철회된 D-번호)를 전제한 것을 main에서 지운다. 에이전트 정의 8개 삭제(`stage0-attempt1`과 git 이력에 남음), `rule.py`·`data/env_patches.md`·`README.md`의 참조 정리, pytest 캐시 삭제. 프로젝트 메모리에서 단발 프롬프트를 통제 수단으로 적은 항목을 고쳐 쓰고, 에이전트 로딩 항목을 지웠다.
- 이유: 잘못된 경험이 다음 세션의 판단에 스며드는 것을 막는다. 이날 밤 조율자가 옛 메모리를 근거로 "첫 실험을 단발 프롬프트로 할 수도 있다"고 잘못 올린 것이 실례다.

### D-033 논제를 문제·처방·결과 형태로 다시 쓴다 (채택, 저자 지시 2026-09-16 밤)

- 문제는 위임의 품질이 아니라 채움의 불가시다. 모호한 위임은 실행되고, 빈 자리는 일관된 정책으로 채워지며, 그 정책은 결과에 없다.
- "위임을 잘 쓰면 된다"는 처방이 아니라 경쟁 방법(B0/B1)이다. 자리는 상태가 와야 열리고, 조항을 더하면 새 자리가 생긴다.
- 채움의 존재는 하네스가 정한다(상태를 바꿔 행동의 의존을 본다). 모델의 보고는 검출 수단이 아니라 그 존재에 대한 재현율로 재는 측정 대상이다.
- 기여의 형태는 프레임워크다. 상황 생성, 모의 실행(프록시), 정책표와 승인. 결과의 형태는 위임을 켜기 전의 정책표와 승인 기록.
- 미결: `Plan.md` §6 M1의 비준을 "R 문장 삽입"에서 "정책 승인·거부 기록"으로 고칠지는 §6 개정 때 정한다.
- 반영: `Plan.md` §0, `README.md`.

---

## 2026-09-20

### D-034 첫 실험은 B2a·B2b 먼저, M1은 그 뒤 (채택, 저자 지시 2026-09-20)

CASE-D01의 여덟 상태를 고정하고 **통로만** 움직인다. B2a(상태 낱개, 텍스트 예측, 툴 없음) → B2b(여덟 상태를 한 표에. `docs/CASE-D01.md` §1 재현) → M1(프록시 툴 실행, 독립 여덟 런). 위임 1개(D01), 모델 2~3개.

근거 셋.

- **B1과 M1은 비교가 서지 않는다.** `Plan.md` §6의 B1은 모델이 상태를 지어내고 M1은 fork set을 프록시에서 실행한다. 상태 출처와 통로가 동시에 다르므로 차이가 어느 쪽에서 왔는지 가를 수 없다. B2는 상태를 M1과 공유하고 통로만 바꾼다.
- **CASE-D01의 여덟 행은 한 응답에서 나왔다.** 프롬프트는 각 실행이 독립이라고 말했지만 모델은 여덟 행을 동시에 보았다. "사전식 순서 하나가 여덟 행을 전부 설명한다"(`docs/CASE-D01.md` §4)가 상태를 넘어 유지되는 정책인지, 한 컨텍스트에서 조화시킨 답인지 지금 표는 가르지 못한다. B2b − B2a가 그 크기다.
- **B2는 오늘 돌아간다.** 러너·환경 파일·로컬 서빙 셋 다 없다(`docs/INTRO.md` §10). B2a·B2b는 셋 없이 돌고 M1은 셋이 있어야 한다.

읽는 법.

| 결과 | 뜻 |
|---|---|
| B2a ≈ M1 | 방법에 필요한 것은 실행이 아니라 구체 상태다. 프레임워크의 2단계를 예측 질의로 바꾸고 프록시 실행은 검증용으로 남긴다 |
| B2a < M1 | 말·행동 격차가 증거로 남는다. 프록시 실행이 방법의 필수 부분이다 |
| B2b > B2a | CASE-D01 표는 상한이다. 사례로 인용할 때 그 자격을 함께 적는다 |

**첫 실험의 M1은 통로 비교용이다.** CASE-D01의 여덟 상태는 저자가 R을 알고 만든 측정용이다. `Plan.md` §4.3은 방법의 fork set이 R을 몰라야 한다고 못박았고("이 분리가 없으면 M1은 동어반복이다"), 둘은 실제로 겹치지 않는다 — 여덟 행 중 여덟 행이 fork set 8행으로 재현되지 않는다(`docs/DATA-GEN.md` §4). 따라서 첫 실험이 내는 것은 통로 간 비교이지 M1의 노출률·감지율이 아니다. 논문의 그 수치는 R 무관 fork set에서만 낸다.

**읽을 것은 행 단위 일치율이다.** 대안 규칙 집합 v0로는 CASE-D01의 다섯 규칙에 이름을 못 붙인다(`docs/CASE-D01.md` §7). 첫 실험의 귀속은 "집합 밖"으로 나올 것이고 그것은 실패가 아니다. 귀속은 열린 코딩으로 v1을 만든 뒤다.

D-031·D-032와의 관계. D-031의 "첫 실험은 프록시 실행"은 취소가 아니라 **순서가 밀린 것**이다. D-032("단발 표 프롬프트는 실험 설계 결정이 아니다")는 유지된다. B2는 주 방법이 아니라 경쟁 방법(베이스라인)이고, 최종 무대는 그대로 에이전트·툴콜링이다.

반영: `Plan.md` §1·§6·§7·§8, `docs/CASE-D01.md` §8, `docs/INTRO.md` §8·§9·§10. 상태 출처 대조는 `docs/DATA-GEN.md` §4·§7.

### D-035 채움의 계측을 정답 R에서 최소쌍 의존과 발산으로 옮긴다 (채택, 저자 지시 2026-09-20)

외부 심사 2회(1차 우려 여섯, 2차 재설계 §1~§8)를 조율자가 저장소와 대조해 올린 권고안을 저자가 "진행해"로 채택했다. 문헌 실재는 `docs/related/scout-2026-09-20.md`.

**핵심 한 줄.** 정답 규칙 R을 기준으로 재던 것(피해율, 정책 정확도, D+ 오경보율, "R 문장 삽입" 비준)을 버리고, 특징 하나만 다른 상태 쌍에서 행동이 달라지는가(최소쌍 의존)와 모델 간·사람 간 발산을 기준으로 잰다. `Plan.md` §0 "정의의 자리"의 문장을 그대로 계측으로 옮긴 것이다. R은 위임 절반에서 심은 자리의 회수율(방법의 민감도)로만 남는다.

**결정 항목 아홉.**

| # | 항목 | 결정 |
|---|---|---|
| 1 | 심사자 2차 설계(주장 넷·정의 교체·자리 풀·생성기·안정성·표 형식·인간 연구 둘·규모) | 채택. 아래 일곱 수정과 함께 |
| 2 | 관습적 기본값(채움은 있으나 모델·사람이 모두 같은 선택) | 표에서는 접되 승인 기록에는 기본 "수락"으로 남겨 주인이 뒤집을 수 있게 한다. 지우지 않는다. 오경보율은 접히지 않은 행 중 사람 다수가 당연하다고 본 비율 |
| 3 | 상황 생성기 조건 | 기계 v1(섭동표, 절제) / 기계 v2(`docs/DATA-GEN.md` §6: 모든 도달 필드 × 경계 템플릿, 프로그램만) / LLM 파이프라인(심사자 §4) / 저자 설계(상한). 사전 등록 분기: v2 ≈ LLM이면 방법은 프로그램만으로 선다 |
| 4 | 승인 반영 방식 | 지시문 패치와 런타임 가드 둘 다 잰다. 패치 뒤 생성기를 다시 돌려 새로 열린 자리 수를 센다. 이것이 "조항을 더하면 새 자리가 생긴다"의 측정이다 |
| 5 | 샘플링 | 기본 온도(제공자 기본값)로 셀당 k=5. T=0 기본을 버린다. 표면 변형 둘(레코드 순서, ID 재명명). **추가(저자 지시, 같은 날 저녁): B2a도 k=5.** M1과 의존 검출력을 맞춘다. k=1이면 회수율 비교 B2a ≤ M1이 k 차이로 부풀 수 있다 |
| 6 | 9/27 체크포인트 | **저자 확정 2026-09-20("체크포인트와 비준 모두 동의").** 둔다. 내용은 최소 러너로 D01 여덟 상태 × 모델 3 × k=5 툴콜링 + B2a·B2b. 게이트는 현상(채움 발생)과 안정성(최빈 행동 비율). 밀리면 1월 투고를 고집하지 않고 다음 사이클로 |
| 7 | ACCORD(arXiv 2606.16432) 위치 | 관련 연구에서 "환경 상태가 답을 정하는 자리"(ACCORD) 대 "주인만 아는 자리"(우리)로 구분한다. 인간 발산 기준선이 그 구분의 실증이다. 다음 작업: `docs/CASE-D01.md` 여덟 상태를 두 부류로 분류한 표(저자 검증) |
| 8 | "조항을 더하면 그 조항에 또 자리가 생긴다"(`Plan.md` §0, `docs/INTRO.md` §5) | 문헌으로 못 받친다(Hadfield-Menell에 없음, 정찰 §3). 인용 없이 측정(항목 4)으로만 쓴다. "쓰지 않는 문장"에 추가: 이 문장을 문헌 근거로 쓰지 않는다 |
| 9 | "비준" 용어 | **저자 확정 2026-09-20.** 유지한다. 원고에 밝힌다: Restatement (Third) of Agency § 4.01의 ratification은 사후 승인이고, 우리는 프록시가 기록한 commit을 "이미 행해진 행위"로 보아 그 승인을 ratification이라 부른다. § 4.07(행위 전체성)과 행 단위 부분 승인의 불일치는 한계 절에 적는다. 대안은 actual authority(§§ 2.01, 3.01) |

**일곱 수정(심사자 설계에 더한 것).**

1. 관습적 기본값은 접되 기록에 남긴다(항목 2).
2. "미리 닫을 수 없다"를 주장에 넣는다. C4 안에 패치 대 가드 비교로(항목 4).
3. 기계 v2 조건(항목 3). 이유: `docs/DATA-GEN.md` §4.1 — "관련된 특징"을 고르는 판단이 재고 필드를 뺐고 그것이 R5를 지웠다. LLM 열거도 같은 실수를 할 수 있다.
4. 의존 판정의 잡음 수준: 같은 상태를 두 묶음(k=5씩) 돌려 나오는 가짜 의존률을 재고 그 위에 임계를 둔다. 사람 기준 오경보율과 별개의 보정량이다.
5. 자리 풀 닫기: B0가 이름 붙인 자리는 생성기 1단계 입력으로 넣어 상태로 구체화한다. 의존이 안 나오면 "이름만 있는 자리"로 세어 통로별 정밀도를 낸다.
6. P2 비교는 P2 자신의 행동과 한다(Why2Speak, `docs/related/scout-2026-09-16.md`).
7. 일정 병목은 저자 설계 상태다. 10/9 게이트의 "저자 설계 절반 이상"은 위임 4개의 저자 설계 상태를 전제한다.

**주장 넷과 반증 조건(사전 등록).**

| # | 주장 | 반증되면 |
|---|---|---|
| C1 현상 | 무인 툴콜링에서 채움은 일어나고, 표본·표면 변형에 걸쳐 안정적이며, 모델 간에는 갈린다 | 안정성이 낮으면 "정책이 아니라 분포"로 옮기고 표는 분포를 보여 준다 |
| C2 통로 | 같은 상태 위에서 자리 회수율은 B0 < B2a ≤ M1이고, 실행 중 보고(P2)는 일부만 잡는다 | B2a ≈ M1이면 2단계를 예측 질의로 바꾼 경량판이 기여다(D-034 유지) |
| C3 생성 | R을 보지 않는 생성기(기계 v2 또는 LLM)가 섭동표 v1보다 자리를 더 회수하고 저자 설계에 근접한다 | 근접하지 못하면 생성은 사람 보조 절차로 두고 한계에 적는다 |
| C4 비준 | 정책표를 본 사람이 원치 않는 정책을 더 걸러내고 held-out 행동을 더 잘 예측한다. 승인 기록을 가드로 반영하면 이탈이 줄고, 패치로 반영하면 새 자리가 열린다 | 효과가 없으면 표 형식 연구로 축소한다 |

**정의(`Plan.md` §1로 간다).**

- 차원 f: q의 엔티티가 조인으로 닿는 모든 컬렉션의 필드, 그리고 명시 필드의 경계 해석(단독 후보, 동률, 부족, 정확히 충족, 전제 이미 충족, 기준 간 충돌, 제약 문구).
- 언급 등급 m(f) ∈ {명시, 함축, 부재}. 명시는 문자열 대조(프로그램), 함축·부재는 판정기 + 2인 κ.
- 최소쌍 (s, s′): 기준 상태에서 f 하나만 단일 편집. 각 상태 k=5 실행.
- 의존: 최빈 행동이 다르거나 행동 분포 차가 임계 이상. 임계는 잡음 수준(같은 상태 두 묶음) 위.
- 자리(채움): 의존 ∧ m(f) ∈ {함축, 부재}.
- 발산: 자리에서 모델 간 최빈 행동 불일치, 사람 간 불일치(인간 발산 기준선).
- 관습적 기본값: 자리이되 발산이 없는 것. 접되 기록.
- 자리 풀: 위임별로 모든 통로(B0, B2a, M1, P2)와 저자 설계 상태에서 발견된 자리의 합집합. 통로별 회수율·정밀도의 분모.
- 심은 자리: 위임 절반에서 D+/D− 절제를 남긴 R 절의 차원 f_R. D−에서 자리로 잡히고 D+에서 명시로 빠지면 회수.
- 정책표: 차원마다 대조 행("가용량이 부족해지면 → 분할 발주") + 분포 + 발산. 발산 큰 자리를 위에, 관습적 기본값은 접음. 규칙 요약은 평가 대상 밖 모델이 쓰되 프로그램으로 행에 재생해 일치한 것만 표시.
- 승인 기록: 자리별 수락·금지·지정. 반영은 패치 또는 가드. 그 뒤 held-out 재실행 → 승인 후 이탈률, 패치 후 새 자리 수.

**척도 아홉(+보정량 하나).** 자리 수, 안정성(셀 내 최빈 행동 비율, 변형 간 표 일치율), 발산, 통로별 회수율·정밀도(자리 풀 분모), 예측 타당도(표를 읽은 사람·모델의 held-out 예측 정확도), 심은 자리 회수율, 보고 재현율(P2 자신의 행동 대비), 오경보율(항목 2 정의), 승인 후 이탈률. 보정량: 잡음 의존률.

**덜어낸 것.** τ²-bench 제외(후속). E0는 하네스 검증(D-023)으로만. 명제 3은 §0 동기 한 문장으로만 남고 측정하지 않는다. 추론 재현율은 분석 절(FACE-Eval 선점). B1(자기 대입)은 통로에서 빼고 LLM 생성기의 1단계로 흡수. 대안 규칙 집합 v0·귀속 판독기·규칙 명시 판정기 폐기. 유형 4×8 구조 폐기 → 위임 14(파일럿 4), 환경 다양성 기준.

**닫히는 미결.** D-033 미결(비준의 정의) → 승인 기록으로 종결. 통로별 척도의 사건·분모 불일치 → 자리 풀로 해소. `docs/DATA-GEN.md` §7 미결 1·2(R 절 식별, 유형 분류) → 주선에서 소멸, 심은 자리 절반에만 남음. 미결 3·4 → 생성기 조건으로 흡수. `docs/INTRO.md` §10 "비준의 정의"·"상태 설계의 원칙" 종결.

**D-034와의 관계.** 첫 실험(B2a → B2b → M1, 여덟 상태)은 그대로다. 여덟 상태는 최소쌍이 아니므로 9/27은 현상·안정성만 보고, 최소쌍 판정기는 생성기 v0의 상태부터 적용한다. B2a ≈ M1 분기는 C2의 반증 조건으로 그대로 산다.

**반영.** `Plan.md` §0~§12 전면, `docs/INTRO.md` §5·§7·§9·§10, `README.md`, `docs/DATA-GEN.md` §7.

**반증 조건.** 위 표. 추가로: 잡음 의존률이 임계를 넘으면 k를 올리거나 의존 정의를 최빈 행동만으로 좁힌다. 언급 등급 κ < 0.6이면 함축을 부재에 합쳐 2단으로 되돌린다.

### D-036 생성기는 혼합형: 범위는 프로그램, 뜻은 LLM (채택, 저자 지시 2026-09-21)

생성기 v0(기계 v2, `docs/GEN-V0.md`)은 D01의 여덟 자리를 냈지만 항공 씨앗에서 뜻을 모르는 편집(3을 경유 횟수에, 공항 단독 후보로 목적지 삭제, 브랜드를 Lays로)을 냈다. 저자: "단순히 기계적으로 작동할 것 같지 않다. 적절한 수준에서 LLM을 혼합해야 한다." 그리고 "상태는 두 가지다. 표가 변형되거나 문제가 변형되어서 다른 결과가 나온다."

**결정.**

| 축 | 담당 | 내용 |
|---|---|---|
| 표(초기 상태)의 변형 | 생성기 v1 | 프로그램이 결정 지점 표를 연다(지시의 ID에서 조인으로 닿는 모든 레코드·필드, 지시의 상수). LLM이 각 지점에 상황 값을 채운다(상수↔필드 대응, 필드 뜻·등급, 값 구체화, 레코드 추가, 그럴듯함 표시). 프로그램이 검사한다(한 군데만 바뀜, 타입, 조인 정리, 환경 재생) |
| 문제(지시)의 변형 | 기존 장치 | D+/D− 절제(심은 자리, 위임 절반)와 승인 후 지시문 패치(C4 "패치 후 새 자리 수"). 생성기가 지시를 바꾸지 않는다 |

**LLM의 제약.** (1) R(지운 절)과 저자 설계 상태를 보지 못한다. 입력은 q·스키마·예시 상태·기준 상태뿐. (2) 필드를 지우지 못한다. 더하거나 값을 채우거나 표시만 한다(`docs/DATA-GEN.md` §4.1: 관련성 판단이 재고 필드를 지웠다). (3) 모든 프롬프트와 출력을 manifest에 남긴다. (4) 그럴듯함 표시는 저자 검토용이고 자동 제외하지 않는다(첫 판).

**LLM 모델.** 저자 지시: Qwen ("gemma보다 qwen이 좀 더 일반적인 선택지"), 평가 대상과의 분리는 "상관 없어". 따라서 이미 서빙 중인 `Qwen/Qwen3.8-27B-FP8`을 생성기에도 쓴다. 생성기 모델이 평가 대상과 같다는 점은 한계 절에 적는다(생성 상태에 그 모델의 편향이 실릴 수 있다). 상업 판정기 키가 생기면 교체할 수 있고 재생성 비용은 위임당 단발 호출 수십 번이다.

**D-035 3과의 관계.** 조건 셋은 유지된다. 기계 v2 = 절제(범위만, 뜻 없음), 혼합 = 방법, 저자 설계 = 상한. 사전 등록 분기 "기계 v2 ≈ LLM이면 프로그램만"은 그대로 시험한다. 심사자 §4의 "LLM이 자리를 열거"는 채택하지 않는다. 열거는 프로그램이 한다.

**반증 조건.** 혼합 생성기가 항공 씨앗에서도 무의미한 상태를 v0만큼 내면 LLM 단계의 설계를 바꾼다. 혼합이 저자 설계의 절반을 못 내면(10/9 게이트) C3의 반증 조건대로 생성은 사람 보조 절차로 둔다.

반영: `src/gen/`, `docs/GEN-V1.md`, `scripts/vllm_serve.sbatch`(gemma 키).

## 2026-09-21

### D-037 상태 생성은 변수와 변형이다. q 변형과 state 변형을 한 틀로 (채택, 저자 지시 2026-09-21)

생성기 v0(기계, `docs/GEN-V0.md`)과 v1(혼합, D-036)을 폐기하고, 그 위에 세운 절차 문서도 폐기했다. 저자 판정: "고상하고 위대한 척하는 아이디어는 전혀 연구의 동기와 관련이 없다." 규칙과 근거는 `docs/GEN-ALGO.md`(전면 재작성).

**폐기 이유.** v0·v1은 **필드에서 출발**해 타입별 템플릿을 걸었다. v1이 D01에 낸 56차원 중 21개가 `sales_history.revenue: 5040→4830` 류의 무목적 편집이었고, 쓸모 있는 상태도 무엇을 시험하는지 기록하지 못했다. 그리고 v1의 LLM이 `inventory_items.quantity_on_hand`를 `axis: false`로 판정하며 적은 이유("Instruction focuses on supplier availability, not internal warehouse stock")는 `docs/DATA-GEN.md` §4.1이 R을 아는 생성기의 치명적 실수로 인용한 문장과 같다. **LLM에게 "이 필드가 중요한가"를 묻지 않는다.** 그 판단이 R을 아는 판단이다. 같은 판정이 `lead_time_days`도 죽였고, 저자 여덟 상태의 R4·R5가 정확히 그 두 필드에서 나왔다.

**순서.** 빈 곳은 q에 처음부터 있다. 상태는 그 빈 곳이 보이게 하는 장치다. 원본 씨앗에서는 어느 해석을 쓰든 답이 같아서 빈 곳이 안 보인다. **변수를 찾는다 → 변형한다 → 답이 갈리는지 확인한다.**

**규칙과 근거** (`docs/GEN-ALGO.md`).

| # | 규칙 | 근거 |
|---|---|---|
| 1 | q도 key(무엇을 찾을지)와 value(어떤 값인지)를 갖는다. 모델은 q의 key로 state에서 **검색**하고 가져온 value로 **추론**한다. 빈 곳이 두 단계에서 각각 생긴다 | 저자 지시 |
| 2 | state에서 바꿀 수 있는 층은 스키마·튜플·값 셋뿐 | 관계형 인스턴스의 정의(Codd) |
| 3 | 값 변형의 종류는 **도메인의 순서관계**가 정한다. 전순서는 미달·동률·초과, 이산은 뒤집기·교체, 자유 텍스트는 닫히지 않는다 | 전순서의 삼분법 |
| 4 | 변수가 둘 이상이면 순서의 일치/불일치가 축 하나 더 | |
| 5 | 집합 크기는 0·1·여럿 셋뿐 | 한정 기술의 존재·유일성 전제(Russell) |
| 6 | q에서 뺄 수 있는 것은 툴 인자의 명세(툴 지정·검색 대상·조건·기준·값) | |
| 7 | 변수의 범위는 **툴 응답**이 정한다. 1차 = q가 지목한 필드, 2차 = 같은 툴 응답에 실려 오는 필드, 3차 = q의 상수와 도메인이 같은 필드 | 툴이 레코드를 통째로 돌려준다(환경 코드 확인) |

축은 타입이 아니라 순서관계다. 날짜와 수를 다른 부류로 세면 v0의 실수를 반복한다. **경계(정확히 충족)는 `=`이지 별도 종류가 아니다.** v2의 "경계 템플릿" 일곱(단독 후보/동률/부족/정확히 충족/전제 이미 충족/기준 간 충돌/제약 문구)은 위 규칙으로 전부 환원되므로 폐기한다.

**q 변형 통합.** q 변형은 state 변형과 같은 틀 안에 있다. D+/D− 절제는 그중 "기준"을 뺀 한 사례이고 별개 장치가 아니다. 따라서 **위임 절반이 아니라 모든 위임에 둔다.** 씨앗 지시문을 원판 q⁰으로 두고 성분을 하나씩 뺀 판 q⁻ᵏ를 만든다(위임당 2~3, 파일럿에서 확정). 같은 상태에서 q 성분 하나만 다른 두 판이 **q 최소쌍**이고 상태 최소쌍과 대칭이다. 뺀 것이 무엇인지는 우리가 설계하므로 채점지가 되고, 생성기는 주어진 q만 보므로 눈가림은 그대로다. 씨앗 선정에서 "R 절이 문장으로 있는 것" 제약이 사라진다.

**전제 실패 격자의 지위.** 버리지 않고 규칙 5 한 곳으로 옮겼다. 집합 크기를 셋으로 나누는 근거로만 쓰이고 출발점이 아니다. 출발점으로 삼았던 것이 폐기 사유다 — 연구가 보려는 것은 **모델이 주인 몰래 끼워 넣은 조건**이고 그 틀은 **q가 논리적으로 답을 안 정하는 지점**을 찾는다. R5(q는 100캔 발주라고 했는데 발주 안 함)와 R8(100캔인데 85캔)은 논리적 미결정이 아니라 위반이라 그 틀 밖으로 나간다.

**반증 조건.**

| 검사 | 반증되면 |
|---|---|
| 층 셋 중 어디에도 안 들어가는 변형이 있다 | 완전성 주장이 무너진다. 층을 다시 센다 |
| 전순서 변수인데 미달·동률·초과로 안 갈리는 사례 | 삼분법 적용이 틀렸다 |
| 규칙이 낸 상태의 의존이 저자 여덟 개보다 적다 | 생성은 사람 보조 절차로 둔다(C3 반증 조건) |
| 3차 변수가 한 번도 의존을 안 낸다 | 범위를 1·2차로 좁힌다. D01의 R5가 3차이므로 이미 반례가 있다 |

**D-036과의 관계.** 폐기한다. "범위는 프로그램, 뜻은 LLM"에서 LLM에 맡긴 "뜻"이 곧 관련성 판단이었고 그것이 실패했다. LLM은 **자유 텍스트에 조건을 써넣는 자리에만** 남는다(규칙 3의 닫히지 않는 도메인). 프로그램은 써넣은 조건을 다시 파싱해 술어로 되읽을 수 있는지만 검사한다.

**삭제.** `src/gen/hybrid.py`, `scripts/gen_v1.py`, `data/delegations/D01/states/gen-v1/`(상태 56 + manifest + llm_log). 전부 미커밋이었다.

**반영.** `docs/GEN-ALGO.md` 전면 재작성. `Plan.md` 머리말·§0·§1(차원 f·최소쌍·심은 자리 정의)·§4.2·§4.3·§6 C2·척도 표·§7 예산·§10 한계.

### D-038 언급 등급은 (차원, 변형) 쌍에 붙는다 (채택, 저자 지시 2026-09-21)

`Plan.md` §1의 자리 정의 `의존 ∧ m(f) ∈ {함축, 부재}`에서 m이 **필드에 붙어 있었다.** q가 `availability`를 언급했다는 사실 하나로 `available_quantity`의 모든 변형이 명시가 된다. 그래서 `docs/CASE-D01.md`의 여덟 상태 중 **넷이 자리에서 빠졌다.**

| | 바뀐 차원 | 옛 m(f) | 옛 정의로 자리 | 실제 채움 |
|---|---|---|---|---|
| R2 동률 | price | 명시(`unit prices`) | ✗ | 동률을 리드타임으로 깸 |
| R3 60+60 | available_quantity | 명시(`availability`) | ✗ | 분할 발주 허용 |
| R6 딱 100 | available_quantity | 명시 | ✗ | 가용량 ≥ 수량이면 충분 |
| R8 40+45 | available_quantity | 명시 | ✗ | 100캔을 85캔으로 |

`docs/CASE-D01.md` §4의 다섯 규칙으로 보면 수량 유연화와 충족 판정이 통째로 빠지고 선택 기준은 R4만 남는다. **가장 명백한 채움들이 정의에서 빠져 있었다.**

**원인.** m(f)는 검색 단계만 재고 추론 단계를 못 잰다(`docs/GEN-ALGO.md` §2). q가 `availability`를 보라고 지목한 것은 **무엇을 검색할지**를 정한 것이고, 가져온 값이 모자랄 때 **무엇을 할지**는 안 정했다. 두 문장이 다르다.

**결정.** m을 (차원 f, 변형 v) 쌍에 붙인다. 물음은 "q가 f를 언급했는가"가 아니라 **"q가 v가 일어난 상황에서의 행동을 정하는가"**다.

**판정.**

| 등급 | 누가 | 규칙 |
|---|---|---|
| 명시 | **프로그램** | q의 명시 성분(툴 지정·검색 대상·조건·기준·값)을 술어로 추출해 변형된 상태에서 평가한다. **답이 유일하게 정해지면 명시.** 문자열 대조에서 술어 평가로 바뀐다 |
| 부재 (구조적) | 프로그램 | **후보를 0으로 만들거나 여럿으로 남기는 변형은 q에 성분이 있어도 부재다.** q는 거기까지만 정하고 그 다음을 안 정한다. `docs/GEN-ALGO.md` §6의 집합 크기 0·1·여럿이 그대로 기준이다 |
| 함축 / 부재 | 판정기 + 2인 κ | 술어로 안 잡히는 나머지 |

예. `active`를 뒤집으면 q의 조건이 그 업체를 탈락시키므로 명시다. D+의 `lowest-cost`는 price 순서가 바뀌어도 답을 정하므로 명시, D−는 부재다. D+의 `that can fulfill the quantity`는 한둘이 미달일 때는 명시지만 **전부 미달이면(R8) 공집합을 남기므로 부재**다.

**대가.** 라벨 단위가 필드에서 쌍으로 늘어난다. 다만 술어 평가가 명시를 먼저 걷어내므로 사람이 보는 것은 나머지다. `Plan.md` §4.5의 2인 κ 대상이 "차원 전부"에서 "실제로 만들어진 쌍 전부"로 바뀐다.

**기각한 안 둘.** (B) m을 버리고 "그 상태에서 q가 답을 하나로 정하는가"로만 판정 — D− q는 어느 상태에서도 업체를 안 정하므로 모든 상태가 자리가 된다. (C) m 조건을 빼고 의존만으로 — `active`를 뒤집어도 자리가 된다.

**반영.** `Plan.md` §0·§1 정의 표·§4.5·§5 최소쌍 판독기·언급 등급 판정기·§6 척도 표.
