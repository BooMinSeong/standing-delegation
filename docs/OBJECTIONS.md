# OBJECTIONS: 반론 장부

작성 2026-09-16 (초안 v0). 지위: 심사자가 던질 반론과 우리 답의 단일 장부. reviewer-adversary가 반론을 제안하고 저자가 답한다. 게이트마다(Gate 0, 10/9, 10/23, Phase 3, 집필) 다시 돌린다. 상태가 "닫힘"인 반론은 답이 약해졌을 때만 다시 연다.

형식.
- 등급: 치명(이대로면 reject 사유) / 중대(major revision) / 경미(minor).
- 답의 종류: 설계(계획의 요소가 답) / 데이터(어느 실험·척도·게이트가 답) / 한계(한계 절에 적으면 됨) / 변경(계획을 바꿔야 함) / 없음.
- 상태: 열림 / 닫힘(DECISIONS 번호).

## 장부

| ID | 반론 | 등급 | 답의 종류 | 답 (또는 해소 조건) | 상태 |
|---|---|---|---|---|---|
| O1 | AgentAbstain이 이미 "모델은 멈추지 않는다"를 17개 모델에서 보였다. 무엇이 새로운가 | 치명 | 설계 | 현상은 그들이 이름 붙였다. 우리 단위는 상태에 대한 정책(귀속·일관·R과의 차)이고, 기여는 통로 구분(증언 대 대입)과 비준 절차다. `Plan.md` §10 첫 줄. 금지 표현(v1 `MOTIVATION.md` §10)을 서론에서 지킨다 | 열림 |
| O2 | 주인이 t0에 있으니 그때 물어보면 된다 (B0). 대입은 과잉 | 치명 | 데이터 | 명제 1의 측정. B0/B1 자리 재현율 < M1 노출률(같은 사건, L1). 반대면 무게중심을 옮긴다(`Plan.md` §7 베이스라인) | 열림 |
| O3 | fork set과 섭동표를 R을 아는 저자가 만들었다. M1은 동어반복 | 치명 | 설계 | fork set 생성기는 R을 입력받지 않는다(`src/gen/forkset.py`의 assert). 섭동표는 눈가림 에이전트가 T+ 131에서 도출(`docs/derivation/`). 커버리지 밖 6개가 한계를 잰다 | 열림 |
| O4 | 주인 = R 저자는 순환이다. 진짜 주인은 R을 모른다 | 중대 | 한계 + 데이터 | 인정. 인간 비준 연구(5~10명, 표 20개)로 "표만 보고 불일치를 잡는가"를 잰다. 한계 절 | 열림 |
| O5 | M1은 메타모픽·행동 테스트(CheckList)의 재탕이다 | 중대 | 설계 | 뿌리 인정(`Plan.md` §10). 기여는 정책 단위, 증언과의 같은 문항 비교, 비준·보강 절차 | 열림 |
| O6 | 보고 판정기가 LLM이다. 판정기가 결과를 만든다 | 중대 | 데이터 | 2인 검수 κ ≥ 0.7(파일럿 50, 본 실험 150), 판정기-인간 일치도 보고, 판정기는 평가 대상 밖 모델 1개 고정. 층화 추출(L13) | 열림 |
| O7 | 위임 32개, 저자 저작, 출처 2개. 일반화가 안 된다 | 중대 | 데이터 + 한계 | 위임 단위 클러스터 부트스트랩, 출처별 분리 보고, 유형 4 × 8. 한계 절에 "출처 2개, 저자 저작" | 열림 |
| O8 | 추론 재현율을 로컬 3개에서만 잰다. 상업 모델은 요약뿐이라 비교가 불공정 | 경미 | 한계 | `Plan.md` §3에 명시. 상업 모델의 추론 재현율은 주장하지 않는다 | 닫힘 후보(심사 1회차 충분) |
| O9 | P0 계약("무인 실행, 답 없음")이 실행을 유도했다. 현상은 프롬프트의 산물 | 중대 | 설계 + 데이터 | 계약 사다리 P0/P1/P2와 E0의 원 계약 대조로 "계약이 무엇을 바꾸는가"를 잰다. P0 문구는 PREREG에 동결, 수정 허용 범위 명시(L10) | 열림 |
| O10 | "정책"의 존재는 규칙 집합 v0에 걸려 있다. 집합 밖은 정책이 없다는 뜻인가 | 중대 | 설계 + 한계 | 집합 밖은 "비일관"과 구분해 따로 센다. 파일럿 열린 코딩으로 v1 확정 후 본 실험. 한계에 "집합은 관찰된 정책의 열거" | 열림 |
| O11 | 프록시가 샌드박스다. 배포에서는 그런 프록시가 없다 | 경미 | 한계 | `Plan.md` §10 한계(툴 shim 필요). 주장은 "드러낼 수 있다"이지 "배포된다"가 아니다 | 열림 |
| O12 | fork set 8개로 규칙 5개를 가를 수 있나 | 중대 | 설계 + 데이터 | 분리 설계 기준(어느 두 규칙도 최소 한 행에서 다름) 자동 검사. 크기 2/4/8 ablation(`Plan.md` §6 M1) | 열림 |
| O13 | 결과가 T=0 한 번의 우연. 프롬프트 민감성 | 경미 | 데이터 | 일관성 T=1 3회 다수결, 판 문자열·seed 기록 | 열림 |
| O14 | AgentAbstain 환경은 LLM 생성물이라 얕다. 결과가 환경의 인공물 | 중대 | 한계 + 데이터 | 환경 코드를 읽고 결함 수정 목록 공개(`data/env_patches.md`). τ²(Phase 1)가 둘째 출처. 분기점 노출 확인(L6)으로 조회 실패를 분모에서 분리 | 열림 |
| O15 | 명제 3은 구성상 참인데 결과처럼 쓰였다 | 경미 | 설계 | `Plan.md` §1이 이미 동기로 강등. 보고 구분 불가능성만 측정 | 닫힘 후보(심사 1회차 충분. 집필 시 "발견" 어휘만 감시) |
| O16 | 무대의 신규성이 계약 문구 한 줄로 줄어든다. AgentAbstain은 이미 응답 없는 무대이고(질문하면 에피소드 종료, system_prompt 526개 중 사용자 부재 언급 0. v1 `AGENTABSTAIN.md:36-38`), 우리 러너는 실행 간 이력을 주지 않으므로(`Plan.md:42`) "반복"은 모델이 아니라 저자의 분석 단위다. `Plan.md:248`의 차이("반복·무대화")는 P0 문장 하나와 우리가 지은 상태뿐 | 중대 | 변경 + 설계 | 같은 위임의 이전 실행 보고를 문맥에 넣은 "이력 있는 상시 실행" 팔을 1 모델 × 위임 8에서 돌려 정책 정확도·DISCLOSE가 변하는지 본다. 변하지 않으면 서론에서 "반복"을 주인의 분석 단위로 한정하고 신규성을 통로 구분으로만 좁힌다 | 열림 |
| O17 | 2026년 배포 전 시뮬레이션·인증 계열(arXiv 2606.04037)과 에이전트 메타모픽 계열(arXiv 2512.20083)이 §10에 없다(`Plan.md:250,252`는 ToolEmu 2023, CheckList 2020만). §10의 arXiv ID 넷(2607.07405, 2512.04864, 2606.14589, 2605.27898)은 v1 `REFERENCES.md`에 없다 | 경미 | 설계 | 관련 연구에 두 계열의 차이 한 문장씩(그들은 위험·최적성 위반 탐지, 우리는 미명세 값의 정책을 주인에게 비준). ID 넷은 S0-3 결과(`docs/related/scout-2026-09-16.md`: 2605.27898은 불일치, D-009) | 열림 |
| O18 | 프록시 정책표의 예측 타당도가 척도표에 없다. 귀속 ρ가 fork set(프록시) 정책표에서만 계산되고(`docs/LOGIC.md:18,21`, `Plan.md:127`) 정책 정확도·집합 밖 비율도 같은 산출물에서 나오므로 사슬 2단계(정책의 존재)와 6단계(M1의 회수)가 독립 증거가 아니다. "프록시 행동 = 실제 행동"(`Plan.md:47`)이 검증되지 않는다. 일관성(`docs/LOGIC.md:68`)은 온도와 프록시/실제 차이를 한 수치에 섞는다 | 치명 | 변경(척도 추가, 새 롤아웃 없음) | 이미 도는 측정용·held-out 분기 상태에서 "프록시 귀속 규칙의 예측 = 실제 commit 대상" 일치율을 (위임, 모델)로 보고하고 PREREG에 하한(예 ≥ 0.8)을 적는다. 미달이면 M1이 드러낸 것은 샌드박스 정책이므로 주장 범위를 축소. 진단용 귀속은 실제 실행(측정용 분기 4)에서 따로 계산 | 열림 |
| O19 | 명제 1의 비교가 상태·예산·정보에서 비대칭이다. B0/B1은 호출 2회·환경 실행 없음·상태를 모델이 지어냄(`Plan.md:136-137`), M1은 실제 상태 8개 × 프록시 실행(`Plan.md:159`). 이겨도 원인이 "프록시 실행"인지 "구체적 상태 제시"인지 "호출 4배"인지 가릴 수 없다. `docs/PREREG.md`의 베이스라인 게이트도 이 비대칭을 그대로 등록 | 치명 | 변경 | B2 팔 추가: fork set 상태 8개를 툴 결과와 같은 직렬화로 텍스트로 주고 각 상태의 commit을 예측하게 한다(환경 실행 없음, 호출 8회). 명제 1은 E_expose(M1) − E_expose(B2)의 위임 클러스터 부트스트랩 95% CI가 0을 넘을 때만 유지. B2에서 이미 노출되면 주장은 "실행 전 대입"이 아니라 "구체적 상태 제시"로 바뀐다 | 열림 |
| O20 | 가장 센 베이스라인을 가장 약하게 돌린다. P2는 "허수아비로 읽히지 않기 위한 상한"(`Plan.md:110`)인데 파일럿은 1 모델·64 롤아웃 탐침(`Plan.md:202`), 본 실험도 held-out D−만(`Plan.md:228`). `PLAN_EXEC.md` §1 5단계가 이 팔이 논문의 크기를 정한다고 인정 | 치명 | 변경 | P2를 M1과 같은 분모(위임 32 × 모델 5 × 분기 D−, 측정용 포함)로 돌리고, P2 대 M1+P2의 잔여 피해 차이를 위임 클러스터 부트스트랩 95% CI로 보고. CI가 0을 포함하면 M1의 주장 범위를 "P2를 쓸 수 없는 계약"으로 축소 | 열림 |
| O21 | 방법 효과의 상한 팔이 없다. 비준은 R 저자 정의 아래 자동(`Plan.md:162`)이므로 M1의 이득은 "전면 D+"와의 거리로 정의되는데 `Plan.md:163,193`의 비교 팔에 전면 D+가 없고, 보강 대상 선택의 정밀도·재현율도 위임 단위로 없다 | 중대 | 변경(기존 롤아웃 재사용) | 같은 held-out에서 세 팔의 잔여 피해를 나란히: D− P0 / M1 비준 후 / 전면 D+ P0(이미 `Plan.md:225`에서 돈다). "보강해야 할 위임(ρ ≠ R)"에 대한 M1 선택의 정밀도·재현율을 위임 단위로 보고 | 열림 |
| O22 | 커버리지 안 26 / 밖 6의 비율이 저자 재량인데 효과 크기를 정한다(`Plan.md:100`). 실제 상시 위임에서 R이 섭동표 특징에 의존하는 비율은 추정되지 않음 | 중대 | 데이터 | S0-4 도출 기록에서 T+ 131개 중 섭동표 v1(fork set 8행)로 덮이는 비율을 자연 커버리지로 제시하고, 안/밖 결과를 그 비율로 가중한 값을 함께 낸다. 결과: k=8에서 86.3%(D-010) | 답 있음(데이터). 가중 보고는 PREREG에 |
| O23 | 명제 1의 두 팔을 다른 판정자가 판정한다. B 쪽 E_expose는 LLM 자리 판정기(`docs/LOGIC.md` §1), M1 쪽은 프로그램 판독. D-004 철회 조건이 이미 인지. 판정자가 다르면 부등호가 판정기 관용도 차이일 수 있다 | 중대 | 변경 | A(D)를 섭동표 행 ID로 코드화해 두 팔 모두 프로그램으로 판정하거나, 두 팔 모두 눈가림 인간 2인이 판정하고 κ 보고. 자리 판정 라벨을 3단(자리 언급 / 자리+후보 / 자리+규칙)으로 사전 등록해 세 단 모두에서 방향 유지를 본다 | 열림 |
| O24 | 피해율과 미완료율의 교환이 사전 등록에 없다. 예측은 "잔여 피해 M1+P0 < P0, P2 < P0"뿐이고(`Plan.md:193`) 미완료율(`Plan.md:182`)에는 예측도 상한도 없다. 중지를 늘리면 피해는 0이 되고, P1/P2는 중지를 명령하는 계약이다(`Plan.md:109-110`) | 치명 | 변경 | PREREG §1에 쌍 판정 규칙: M1+P0가 P0 대비 피해를 낮추면서 미완료율 증가가 사전 등록 상한(예 +5%p) 안일 때만 "회수". 계약 간 비교는 동일 미완료율 수준에서, 또는 (피해율, 미완료율) 두 축 지배로만 | 열림 |
| O25 | 하네스 검증이 두 가지를 동시에 바꾼다. 원 수치는 4개 하네스 어댑터 + 판정기 GPT-5.4(v1 `AGENTABSTAIN.md:35,95`), 우리는 자체 루프 + Gemini 3.1 Pro. ±10%p 통과가 두 오차의 상쇄일 수 있다 | 중대 | 변경 | (i) 원 어댑터(OpenClaw)로 같은 78문항을 1회 재생해 어댑터 기준선, (ii) 채점은 원 `commit_check.py` + 원 판정기(GPT-5.4), (iii) 우리 루프를 그 기준선 대비 ±10%p로 판정. 불가하면 "러너가 성능을 깎지 않는다"를 주장하지 않고 한계로 | 열림 |
| O26 | 자기 교정 기회 라벨이 v2에서 빠졌다. v1 실측: 식별자 필수 commit 115개 중 73.0%만 없는 대상을 거부, 20.9%는 수용(v1 `AGENTABSTAIN.md:500,510,632`). `Plan.md:124`의 라벨은 비가역성뿐이라 피해율이 두 사건을 합친다 | 중대 | 변경 | commit 176개 라벨링에 "존재 검사 여부" 칸 추가(v1에서 115개 측정됨), 피해율·잔여 피해를 이 축으로 분리 보고 | 열림 |
| O27 | fork set 8개가 모든 위임에 동일해서(`Plan.md:95`) 위임 간 독립성이 깨진다. 위임 단위 클러스터 부트스트랩(`Plan.md:191`)은 클러스터 독립을 가정 | 경미 | 변경 | 섭동 행 × 위임 교차 랜덤효과, 또는 위임 단위와 섭동 행 단위 부트스트랩을 병행 보고 | 열림 |
| O28 | 모델 5개가 계열로는 3개 이하일 수 있다. 로컬 3이 같은 계열이면 "모델 간 수렴"(`Plan.md:154`)은 계열 공유 편향. 로컬 3이 중형이므로 프런티어 2개에서 현상이 약하면 "중형 모델 이야기"가 된다 | 중대 | 변경 + 데이터 | 로컬 3을 서로 다른 계열로(D-007 제안이 Qwen·GLM·gpt-oss로 이미 그렇다). 현상 게이트를 3모델 평균 외에 모델별로도 보고. 프런티어 2개에서 분기 D− commit 발생률 < 50%면 주장 범위를 계열과 함께 명시 | 열림 |
| O29 | 양자화판(FP8)이 정책을 바꿀 수 있는데 기록은 판 문자열과 seed뿐(`Plan.md:44,65`) | 경미 | 데이터 | 위임 4 × 상태 8에서 FP8과 BF16 귀속 일치율을 한 번 보고, 불일치가 있으면 한계에 | 열림 |
| O30 | M1이 싸다는 주장을 뒷받침할 비용 축이 없다. M1은 프록시 롤아웃 8회 + 주인의 표 비준, B0는 호출 2회 + 질문 열거. 인간 비준 연구에 비교 조건이 없다(`Plan.md:164`) | 중대 | 데이터 | 인간 연구에 같은 평가자의 두 조건: (i) 정책표 20개 비준, (ii) 같은 위임의 B0식 자리 질문 목록에 답하기. 소요 시간·정확도를 나란히, M1의 롤아웃 비용(토큰·시간)을 같은 표에 | 열림 |
| O31 | "반증되면 무게중심을 옮긴다"가 반증 불가능성으로 읽힌다(`PLAN_EXEC.md` §1 4·5단계, `Plan.md:214`). 어떤 결과든 논문이 되는 계획은 사전 등록의 반대 | 중대 | 변경 | PREREG에 주 주장 하나를 고정(사슬 6단계: held-out 잔여 피해 M1+P0 < P0, O24의 쌍 규칙 아래). 4·5단계가 반증되면 주 주장을 유지한 채 명제 1·2를 한계로 내린다고 미리 적고 "무게중심 이동" 문구는 지운다 | 열림 |
| O32 | "피해율"이 재지 않은 크기를 함축한다. 정의는 "commit 대상 ≠ R(s)"(`Plan.md:181`)이고 R은 저자가 지은 규칙. v1 §10은 "피해가 크다"를 금지 | 경미 | 변경 | 척도 이름을 "R 불일치율"로 바꾸고, 비가역 × 금액·영향 축을 붙인 별도 수치를 낼 때만 "피해" | 열림 |

## 심사 1회차 (2026-09-16, reviewer-adversary. 파일은 조율자가 옮김)

기존 반론의 답에 대한 판정.

| ID | 판정 | 이유 |
|---|---|---|
| O1 | 불충분 | 기여가 "통로 구분"이면 그 비교(명제 1)가 공정해야 하는데 B는 호출 2회·환경 실행 없음, M1은 프록시 롤아웃 8회(O19). AgentAbstain도 "실행 전 추론" 갈래를 이미 갖는다(v1 `AGENTABSTAIN.md:27`) |
| O2 | 불충분 | 예측과 정면으로 부딪히는 문헌(behavioral self-awareness, arXiv 2501.11120: 미세조정으로 심은 암묵 정책을 예시 없이 진술)이 §10과 REFERENCES에 없다 |
| O3 | 조건부 충분 | 논리는 선다. `src/gen/forkset.py`의 assert와 눈가림 도출 기록이 Gate 0까지 실물로 있어야 답이 된다(도출 기록은 `docs/derivation/`에 생김) |
| O4 | 불충분 | 인간 비준 연구가 대체 비용(t0에 자리 질문 목록에 답하기)을 재지 않는다(O30). 평가자 5~10명·표 20개는 검정력이 없다 |
| O5 | 불충분 | 근거가 CheckList(2020)에 머문다. 2025~2026 에이전트 메타모픽·배포 전 시뮬레이션 계열과의 차이 한 문장이 없다(O17) |
| O6 | 조건부 충분 | κ ≥ 0.7 + 층화 + 고정 판정기는 표준. 하네스 검증에서 판정기를 바꾸는 문제는 별개(O25) |
| O7 | 불충분 | τ² 8개면 출처별 분리 보고가 클러스터 8개 구간. AgentAbstain 재사용의 실질은 환경·도구 코드뿐이고 분기 상태는 전부 우리가 심는다 |
| O8 | 충분 | 주장 범위를 로컬 3으로 제한(`Plan.md:64,255`) |
| O9 | 불충분 | P0가 무인 프레이밍 + 예약 실행 + "완료하라" 지시를 한 덩어리로 묶는다(`Plan.md:108`). 1 모델에서 세 변형(무인만 / 예약만 / 완료 지시 제거) ablation이 있어야 답이 된다 |
| O10 | 불충분 | v1 확장이 파일럿 관찰로 일어나 본 실험의 "집합 밖" 분모가 사후에 정해진다. v1 동결 시점과 규칙 추가 기준이 PREREG에 없다 |
| O11 | 불충분(등급은 최소 중대) | 프록시가 가장 필요한 비가역 commit(send/transfer/publish)이 배포에서 가장 프록시하기 어렵다. "드러낼 수 있다"로 낮추면 방법 절이 처방이 아니라 진단 보조가 된다 |
| O12 | 불충분 | 분리 설계 기준은 규칙 쌍 단위만 보장. R 유형별 구분 행 수의 불균형(특히 기수 조건 R(s)=∅)은 보장하지 않는다 |
| O13 | 불충분(경미 유지) | 일관성은 온도만 흔든다. 계약 문구 변형 민감성과 양자화 효과(O29)가 빠져 있다 |
| O14 | 불충분 | 얕음은 결함이 아니라 우리 설계 요구("분기 개체는 빈손 조회 하나로 전부"). τ²도 같은 조건. "정책은 표가 한 화면에 들어올 때만 관측된다"에 답이 없다 |
| O15 | 충분 | 동기로 강등하고 측정을 보고 구분 정확도로 좁혔다 |

전체 판정: **예상 점수 2.5/5** (예측 방향대로 결과가 나와도). 이유: 기여가 "증언 대 대입"의 통로 비교인데 그 비교가 상태·예산·판정자에서 비대칭이고(O19, O23), 가장 센 증언 베이스라인 P2를 가장 약하게 돌린다(O20). O18·O19·O20·O24가 닫히면(모두 새 롤아웃 없이 또는 소량으로 척도·분모·사전 등록 변경으로 닫힌다) 3.5 수준. Gate 0 기준 "치명 등급 중 답 없는 것 0"은 현재 미달: 치명 4개(O18, O19, O20, O24)와 재개된 치명 2개(O1, O2) 전부 열림.

심사가 인용한 문헌(신규성 판단용): 2607.10059 AgentAbstain, 2501.11120 behavioral self-awareness(후속 2511.04875, 2604.16812), 2606.04037 배포 전 인증, 2512.20083 에이전트 메타모픽, 2609.11957 Look Before You Leap(실행 전 검증 + 조용한 실패 프레이밍).
