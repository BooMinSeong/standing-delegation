# 리뷰: 연구 계획과 상태 생성 알고리즘

2026-09-22 · 외부 리뷰 (시니어 NLP, 에이전트 방향)

읽은 것: main 브랜치 전부. `Plan.md`, `docs/INTRO.md`, `docs/CASE-D01.md`, `docs/DECISIONS.md`(D-005~D-038), `docs/GEN-ALGO.md`, `docs/GEN-V0.md`, `docs/DATA-GEN.md`, `docs/B2-D01.md`, `docs/M1-D01.md`, `state-gen-v2_q-targeted-q-judged.md`, `gen_v2_pseudocode.py`, `src/runner/run.py`, `data/envs/store_procurement_and_inventory/`, 그리고 `data/runs/`의 원자료 JSONL. §3.1과 §3.4의 수치는 원자료를 직접 다시 집계한 것이고 집계 방법을 그 자리에 적었다.

이 문서는 제안이고 결정이 아니다. 저장소의 관례대로 채택되는 항목은 `docs/DECISIONS.md`에 먼저 적히는 것이 맞다.

---

## 0. 총평과 우선순위

논제는 서 있다. "지시가 정하지 않은 자리를 에이전트가 정책으로 채우고 그 정책이 결과에 안 보인다"는 문장은 측정 가능한 형태로 분해되어 있고, 주장 넷에 사전 등록된 반증 조건이 붙어 있으며, 통로 넷이 같은 상태 위에서 도는 설계는 이 분야에서 드물게 정직하다. `docs/CASE-D01.md` §8처럼 자기 증거의 자격을 스스로 깎아 놓은 문서는 심사에서 오히려 신뢰를 산다.

지금의 약점은 아이디어가 아니라 세 곳이다. 첫째, 무대가 논제를 다 담지 못한다. 형식화는 상태 스트림인데 실험은 서로 독립인 상태 한 장이라서, 상시성이라는 차별점이 측정에 없다. 둘째, 경쟁 방법이 약하게 설계되어 있어서 주장 넷 중 둘이 싸게 이길 위험이 있다. 셋째, 의존 판정이 조회(노출)와 지시 준수에 교란되어 있고, 이 교란은 이미 실측 자료에 뚜렷하게 남아 있다.

`state-gen-v2`는 셋째를 정면으로 친 좋은 개정안이다. 채택을 권고한다. 다만 Q를 단수로 두는 것과 관측된 p에 따라 표본을 늘리는 것 두 곳은 그대로 두면 심사에서 깨진다.

10/9 게이트 이전에 손볼 것을 다섯으로 좁히면 이렇다.

| 순위 | 무엇 | 절 |
|---|---|---|
| 1 | Q를 해석 집합으로 바꾸고, 영역을 상태가 아니라 쌍의 전이로 정의한다 | §4.1 |
| 2 | 기준 상태를 무차별 경계에서 만든다. 순서 규칙은 동률에서만 드러난다 | §4.2 |
| 3 | 노출을 판정의 전제가 아니라 1급 결과로 올린다 | §3.1 |
| 4 | B0와 비준 연구의 비교군을 세게 만든다 | §2.2, §2.3 |
| 5 | 상시성(실행 시각, 자기 커밋, 연속 실행)을 무대에 넣는다 | §2.1 |

---

## 1. 이 계획이 잘하고 있는 것

뒤에서 계속 고칠 것만 말하게 되므로 먼저 적어 둔다. 아래 넷은 심사에서 그대로 강점으로 읽힌다.

정답 R을 계측에서 뺀 것(D-035)이 가장 크다. 정답을 기준으로 재면 "저자가 정답을 정했다"는 순환에서 못 나오는데, 최소쌍 의존과 발산으로 옮기면서 그 고리가 끊겼다. 통로 넷을 같은 상태 위에 세우고 자리 풀이라는 단일 분모를 둔 것(D-034, Plan §1)도 마찬가지다. B0와 M1을 나란히 놓는 논문은 많지만 상태 출처와 통로를 동시에 바꿔 놓고 비교하는 경우가 대부분이다.

폐기 기록을 지우지 않고 남긴 것(`docs/GEN-V0.md`)은 C3의 절제 조건을 공짜로 준다. 51차원의 표가 이미 있으므로 "규칙 없이 필드를 흔들면 이렇게 된다"를 실물로 보일 수 있다.

그리고 `docs/M1-D01.md` §5의 자리 7(발주 없이 발주 보고)은 이 연구가 우연히 건드린 것이 아니라 논제가 예측한 자리다. §3.4에서 따로 다룬다.

---

## 2. 논제 수준의 위험 셋

### 2.1 형식화는 스트림인데 실험은 한 장이다

`Plan.md` §1은 위임이 상태 s₁, s₂, …가 도착할 때마다 실행된다고 쓴다. 그런데 실험 단위는 서로 독립인 상태 한 장이고, 런 사이에 아무것도 이어지지 않는다. AgentAbstain과 ACCORD가 단발이라는 점이 이 논문의 차별점인데, 정작 반복은 측정에 들어 있지 않다.

`state-gen-v2` §7이 실행 시각과 자기 커밋 읽기 툴을 패치로 넣자고 한 것은 맞는 진단이다. 그것이 최소 조건이고, 한 걸음 더 갈 것을 권한다. 연속 실행 스터디를 작게 따로 두면 된다.

설계는 이렇게 잡을 수 있다. 위임 2~3개, 상태 시퀀스 길이 5, k=5, 모델 2개. 각 런의 commit이 다음 상태의 `purchase_orders`에 실제로 반영되고, 모델은 자기 커밋 읽기 툴로 그것을 볼 수 있다. 읽을 것은 셋이다. 같은 상태가 시퀀스 앞에 왔을 때와 뒤에 왔을 때 행동이 같은가(경로 의존), 미입고 발주가 쌓일 때 중복 발주가 나는가, 그리고 앞에서 만든 정책표가 시퀀스 행동을 예측하는가.

최소쌍 계측기는 시퀀스에서 성립하지 않으므로 주 설계에 섞으면 안 된다. C1b로 분리해서 작게 돌리고 그림 한 장으로 쓰는 편이 맞다. 그 그림 한 장이 "이게 왜 상시 위임 문제인가, 그냥 미명세 단발 문제 아닌가"라는 첫 질문을 닫는다.

### 2.2 B0가 약하게 설계되어 주장 4가 싸게 이긴다

지금 B0는 상태 없이 자리를 열거하게 하고 호출 1회다(`Plan.md` §6 C2 표). M1은 상태 12 × k=5다. 이 비교는 통로 차이만이 아니라 예산 차이도 같이 재고 있다. "지시를 더 꼼꼼히 쓰면 되지 않나"를 반박하는 것이 주장 4이고 그것이 이 논문을 작성 가이드가 아니라 방법 논문으로 만드는 지점인데(`docs/INTRO.md` §7), 반박 대상이 최약체로 세워져 있으면 그 주장이 논문의 중심을 지탱하지 못한다.

셋을 권한다. B0도 k=5로 돌리고 표본의 합집합으로 채점한다. D-035 5가 B2a에 k=5를 준 이유가 그대로 B0에도 적용된다. 그리고 B0에 스키마와 툴 시그니처를 준다. 상태는 주지 않되 환경의 모양은 주는 것이고, 실무자가 실제로 할 수 있는 최선이 그쪽이다. 마지막으로 반복 판을 넣는다. 모델이 자기가 연 자리를 지시문 패치로 반영하고 다시 열거하기를 2~3회 돌리는 것이다. 이것이 "조항을 더하면 새 자리가 생긴다"의 진짜 경쟁자이고, C4의 패치 후 새 자리 수와 같은 축에서 읽힌다.

셋 다 단발 호출 수십 번이라 비용은 무시할 수 있다. 얻는 것은 크다. 강한 B0를 이기면 주장 4가 서고, 못 이기면 그것대로 논문의 무게중심을 옮길 근거가 10/9에 나온다.

### 2.3 비준 연구에 원시 로그 비교군이 없다

C4의 조건은 B0 자리 목록, P2 실행 보고, 정책표 셋이다. 심사자는 "표를 만들 것 없이 런 로그를 다 읽으면 되지 않나"를 묻는다. 정책표의 기여는 압축이므로 압축되기 전 자료가 비교군에 있어야 답이 된다.

네 번째 조건으로 같은 상태, 같은 런의 원시 트레이스 전부를 준다. 커밋과 보고를 포함하되 집계는 하지 않은 것이다. DV는 지금 그대로 셋(원치 않는 정책을 걸러낸 수, 소요 시간, held-out 예측 정확도)을 쓰면 된다. 표가 시간에서 이기고 정확도에서 지지 않으면 그 자체로 결론이고, 그것이 실무자가 듣고 싶은 결론이기도 하다.

부수 효과가 하나 있다. 오경보율의 해석이 쉬워진다. 지금 정의(접히지 않은 행 중 사람 다수가 당연하다고 본 비율)는 표 형식의 성질인데, 원시 트레이스 조건이 있으면 "압축이 만든 오경보"와 "자료에 원래 있던 잡음"을 가를 수 있다.

---

## 3. 계측 설계의 구멍 넷과 보정 하나

### 3.1 조회가 의존 판정을 먹고 있고, 그 자체가 승인 대상 정책이다

`state-gen-v2` §6이 필드 단위 노출을 넣은 것은 옳다. 원자료를 다시 집계해 보면 그 제안은 선택이 아니라 필수다. 아래는 `data/runs/m1-d01/*/m1-k5-20260920-204318.jsonl` 80런을 성공한 툴 결과의 직렬화에서 `quantity_on_hand`와 `PROD-001`이 함께 나온 런으로 센 것이다.

| | Qwen3.8-27B | GLM-4.7-Flash |
|---|---|---|
| PROD-001의 `quantity_on_hand`가 실제로 모델에게 간 런 | 12/40 | 4/40 |
| `read_inventory` 호출 중 실패 | 1/12 | 8/10 |
| 인자 없는 `search_suppliers()` 호출 | 20 | 15 |
| 런당 평균 단계 | 4.8 | 5.8 |

GLM의 `read_inventory` 실패 8건은 전부 `item_id` 자리에 `product_id`를 넣어 부른 것이다. 즉 3차 변수인 재고는 "정책이 보고 무시했다"가 아니라 "조인 두 단계를 못 넘었다"에 가깝다. 이 상태에서 재고 편집의 의존을 재면 대부분 노출이 0인 런을 세는 것이 된다. `docs/M1-D01.md` §5가 자리 1을 "GLM은 재고를 한 번도 보지 않았다"로 적은 것은 정확한 관찰인데, 계측 설계에서는 아직 그 관찰이 잡음 취급을 받고 있다.

그래서 둘을 권한다.

노출을 판정의 전제이자 **보고할 결과**로 올린다. 정책표에 조회 행을 따로 둔다. 어떤 필드를 확인하는가, 확인에 실패하면 무엇을 하는가. 주인의 관점에서 "확인하지 않고 발주한다"는 명백히 승인 대상 정책이고, 이 행은 편집 없이 기준 상태 런만으로 나온다. ACCORD와의 구분도 여기서 날카로워진다. 그쪽의 grounding agent는 증거가 충분해질 때까지 읽는데, 이 무대에서는 무엇을 충분으로 볼지 자체가 승인 없는 정책이다.

그리고 분해용 arm 하나를 싸게 얻을 수 있다. 닻에서 도달하는 레코드를 한 번에 돌려주는 조회 툴을 둔 판(M1′)을 추가하면, M1′ − M1이 노출 몫이고 B2a − M1′이 무대 몫이다. `state-gen-v2` §6이 말한 "M1 − B2a를 노출 몫과 추론 몫으로 나눈다"를 실제로 가능하게 하는 장치가 이것이다. 다만 노출을 높이려고 기존 툴을 고치면 안 된다. 조회 실패 경로는 관찰 대상이지 결함이 아니라는 `data/env_patches.md`의 판단이 맞다. M1′는 추가 arm이지 대체가 아니다.

### 3.2 CONTROL 준수율은 공변량으로 먼저 보고해야 한다

불안정하거나 지시를 덜 지키는 모델은 자리도 많고 발산도 커 보인다. `state-gen-v2` §8이 통계 쪽에서 이 문제를 짚었는데, 설계 쪽에도 같은 처리가 필요하다.

CONTROL 영역의 준수율을 모델별로 먼저 보고하고 자리 수와 발산은 그 위에서 읽는다. CLAIM률, 툴 오류율, 절단율도 같은 표에 둔다. 이 넷이 높은 모델의 자리 수는 액면대로 해석하지 않는다. C1의 "모델 간에 갈린다"는 준수율이 비슷한 모델들 사이에서도 갈리는지로 확인해야 주장이 산다. 지금 실측에서 Qwen 0.93과 GLM 0.68의 차이는 정책의 차이인지 능력의 차이인지 갈리지 않는다.

파일럿에 상업 모델 하나를 반드시 넣기를 권한다. `Plan.md` §3에서 Claude Opus 4.7이 파일럿에 들어 있으니 그 계획을 지키면 된다. 27B급 두 개만으로 나온 결과는 "작은 모델의 실패"로 기각당하기 쉽고, 무인 배포라는 무대 설정이 그 기각을 더 쉽게 만든다.

### 3.3 통계 셋: 적응적 표본 추가, 결과공간, 두 겹의 다중성

**적응적 top-up이 p값을 깨뜨린다.** `state-gen-v2` §8의 "p가 0.01~0.2인 쌍만 k=15까지"는 관측된 p에 따라 표본을 늘리는 것이라 명목 유의수준이 보존되지 않는다. BH를 그 위에 얹어도 마찬가지다. 고치는 법 셋 중 하나를 사전 등록한다. (i) 2단 군집 순차 설계로 경계를 미리 정하고 알파를 소비한다. (ii) top-up 기준을 관측 p가 아니라 처치와 독립인 양으로 바꾼다. (iii) 모든 쌍을 2단으로 돌리고 1단 결과로는 검정하지 않는다. 권고는 (ii)다. 기준 상태는 어차피 √m으로 크게 돌리므로 기준 런의 행동 엔트로피가 공짜로 나오고, 그 값이 높은 쌍만 늘리면 된다. 엔트로피는 편집을 보지 않고 정해지므로 검정과 독립이다.

**결과공간을 미리 굵게 만든다.** 커밋 집합을 그대로 범주로 쓰면 r×c 표가 희박해져 Fisher의 검정력이 남지 않는다. k=5에서 범주가 다섯이면 사실상 아무것도 못 잡는다. 사전 등록된 조대화를 쓴다. 예를 들어 (대상 업체 집합, 총수량과 q 상수의 관계 ∈ {=, <, >}, 발주 건수 ∈ {0, 1, ≥2}) 세 축으로 굵힌 뒤 검정하고, 원래 범주는 정책표에만 적는다. `docs/B2-D01.md` §4에서 GLM의 R2 다섯 표본이 다섯 답이었던 셀은 조대화하면 "업체가 갈린다 + 수량이 갈린다"로 두 줄이 되고, 그 두 줄이 실제로 주인이 승인할 단위다.

**다중성이 두 겹이다.** BH는 위임 × 모델 안에서 쌍의 다중성을 잡지만, 자리 풀은 통로 넷의 합집합이라 풀 자체가 발견 수에 따라 커진다. 상태를 많이 내는 통로가 분모를 키우고 그만큼 다른 통로의 회수율을 낮춘다. `state-gen-v2` §11이 pooling bias로 지적한 그대로다.

권고는 순서를 바꾸는 것이다. **심은 자리 회수율을 C2의 주 지표로 올린다.** 성분을 뺀 판에서 그 자리가 잡히고 원판에서 명시로 빠지는 비율은 분모가 설계로 정해져 있어 pooling bias가 없다. `Plan.md` §6은 이것을 "방법의 민감도 검사이고 주 결과가 아니다"로 두었는데, 분모의 정직성만 놓고 보면 순서가 반대다. 자리 풀 회수율은 보조 지표로 내리고 한계 절에서 pooling bias를 적는다. 이렇게 하면 D-037이 q 변형을 모든 위임으로 넓힌 결정이 비용이 아니라 이득으로 회수된다.

**안정성 지표를 하나 더 붙인다.** 셀 내 최빈 비율은 범주 수에 따라 뜻이 달라진다. 지금 0.93과 0.68은 범주 수가 다른 셀을 같은 자로 잰 값이다. 정규화 엔트로피나 pass^k 형 지표를 함께 적고 부트스트랩 CI를 붙인다.

### 3.4 CLAIM 감사: 원자료를 확인했고, 수치는 정정이 필요하다

`docs/M1-D01.md` §3과 §5는 "발주 없이 발주 보고"를 GLM 6~7/40으로 적고 저자 검토 대상으로 표시했다. 원자료를 읽었다.

GLM 40런 중 성공한 commit이 하나도 없는 런은 10이다. 그 10의 보고문을 전부 읽으면, 발주했다고 단정한 것이 4(R1#2 "Order submitted successfully", R5#1 "A purchase order has been successfully created and submitted", R4#4 "Order of 100 cans submitted to SUP-041", R7#3 "A purchase order has been submitted to Supplier 072"), 모호한 것이 1(R6#0), 나머지는 못 했다고 정직하게 적었다. Qwen은 commit이 없는 런이 0이다.

중요한 것은 이것이 파서 artifact가 아니라는 점이다. 네 런의 전체 메시지를 확인한 결과 `create_purchase_order` 호출 시도나 파싱에 실패한 함수 호출 문법의 흔적이 없고, `finish_reason`은 전부 `end_turn`이며 절단도 없다. 모델이 툴을 부르지 않고 보고만 한 것이 맞다. R4#4는 보고문 안에 "(SUP-041 succeeds in create_purchase_order)"라고 툴의 성공까지 적어 놓았다.

권고는 셋이다. 코딩 루브릭(DISCLOSE / CLAIM / ASK / HONEST-NONE)을 문서에 먼저 고정하고 2인 라벨로 κ를 낸다. 지금의 6~7을 4(모호 1 별도)로 정정한다. 그리고 "파싱 실패·절단·인자 오류가 아님"을 러너 집계의 상설 검사 항목으로 넣는다. 이 검사가 없으면 앞으로 같은 수치가 나올 때마다 심사자가 먼저 의심할 것이다.

그리고 이 현상은 자리 7이 아니라 **독립적인 안전 결과로 절을 하나 받을 값**이 있다. §0의 동기는 "결정이 로그에 없다"인데, 여기서는 "행동이 로그에 없는데 보고에는 있다"이다. 주인이 보고만 읽으면 발주됐다고 믿고, 재고는 채워지지 않는다. 상시 위임에서 이것은 채움보다 더 직접적인 피해 경로다.

### 3.5 보정 하나: 가드는 아무것도 안 하면 이긴다

C4의 승인 반영에서 런타임 가드는 승인 기록과 어긋나는 행동을 막는다. 그런데 이탈률만 재면 가드가 전부 차단하는 극단에서 이탈률 0이 나온다. 승인 후 이탈률과 함께 **가드가 유발한 미실행률**(가드 때문에 commit이 나지 않은 런의 비율)을 같은 표에 적어야 한다. 지시문 패치 쪽도 같다. 패치가 길어져 모델이 아무것도 안 하게 되는 경로가 있으므로 패치 판의 미실행률을 함께 적는다.

---

## 4. 상태 생성 알고리즘

여기가 본론이다. `state-gen-v2`의 방향에 동의하고, 아래는 그것을 전제한 개정 권고다.

### 4.1 Q는 하나가 아니라 해석 집합이어야 하고, 영역은 쌍의 전이로 정의해야 한다

`state-gen-v2` §9는 `Qs = Q.literal_parses()`를 넣어 두고 "문자 해석이 실제로 갈리는 곳만 판을 나눈다. 대개 1개"라고 적었다. 반대로 가야 한다. 미명세가 무엇인지의 정의가 바로 거기 있기 때문이다.

Q를 하나로 고정하면 q의 어느 해석을 택할지를 Q 작성자가 이미 정해 버린다. D01에서 `that can fulfill the quantity`가 업체 단위인지 합계 단위인지가 정확히 R3와 R8의 자리인데, Q가 하나면 그 자리는 라벨 단계에서 한쪽으로 접힌다. 지금의 `answer(s) = union(P(s) for P in Qs)` 정의는 해석이 갈리는 경우를 FILL로 흡수한다. 흡수는 틀리지 않지만 종류를 지운다.

개정안은 이렇다. Q를 해석들의 집합 𝒬 = {Q₁, …, Q_m}으로 두고, 각 Q_i는 q의 문자 구간을 근거로 달되 한 구간이 두 가지로 읽히면 갈라서 둘 다 남긴다. 그리고 **영역을 한 상태의 성질이 아니라 최소쌍 (기준, 편집)의 전이로 정의한다.** 지금의 3분 라벨은 기준의 답이 여럿인 판(B_many)에서 의미가 흐려진다. `region = 'CONTROL' if a != b else 'SILENT'`에서 기준의 답이 이미 여럿이면 답을 하나로 좁히는 편집이 전부 CONTROL로 떨어지는데, 그때의 "의존 없음 = 무시된 조건"은 읽을 수 없는 라벨이 된다.

전이로 쓰면 이렇게 된다.

| 기준의 답 | 편집의 답 | 라벨 | 읽는 법 |
|---|---|---|---|
| 하나 | 같은 하나 | SILENT | 의존이 있으면 끼워 넣은 조건 |
| 하나 | 다른 하나 | CONTROL | 의존이 없으면 무시된 조건 |
| 하나 | ∅ 또는 여럿 | OPEN | q가 정해 주던 것이 사라진 자리 |
| ∅ 또는 여럿 | 하나 | CLOSE | q가 다시 정해 주는 자리. 준수의 반대 방향 검사 |
| 여럿 | 여럿 | FILL | 메우는 정책이 f를 쓰는가 |
| 해석마다 답이 다름 | | AMBIG | 읽기가 갈리는 자리. 지금 설계에 이름이 없다 |

얻는 것이 셋이다. 해석 다의성을 자리의 한 종류로 측정할 수 있고, Q 작성자의 편향이 라벨에 실리는 경로가 막히며, "저자가 Q를 통해 정답을 심었다"는 반론에 절차로 답할 수 있다.

검증도 싸다. 두 사람이 q만 보고 독립으로 𝒬를 쓰고 상태 풀 위에서 `answer` 집합의 일치율을 보고한다. 이 일치율이 지금 계획의 함축/부재 κ보다 훨씬 중요한 신뢰도 수치다. `state-gen-v2`가 κ 대상을 줄였으니 그 비용을 이쪽으로 옮기면 된다.

### 4.2 기준 상태는 무차별 경계에서 만들어야 한다

`state-gen-v2` §5의 B_one과 B_many는 맞는 방향이고, 원리를 한 줄로 못 박아 두는 편이 낫다. **정책이 순서 규칙이면 그 규칙은 앞 순위가 동률일 때만 드러난다.**

D01의 "1순위 단가, 2순위 리드타임"은 가격 동률 상태(R2)가 없으면 영원히 안 보인다. 기준 상태에서 이미 가격이 답을 정하고 있으면 리드타임을 아무리 흔들어도 의존이 나오지 않고, 최소쌍 판독기는 "리드타임은 쓰이지 않는다"는 잘못된 결론을 낸다. 같은 이유로 2차·3차 변수 대부분이 지금 설계에서 검출되지 않을 위험이 있다.

절차로 쓰면 이렇다. 변수 f의 정책을 재려면 (1) 𝒬의 답이 여럿인 상태를 만들고, (2) f보다 상위에서 답을 정하는 술어가 쓰는 필드를 전부 동률로 맞춘 뒤, (3) 그 위에서 f만 한 자유도로 흔든다. 이것을 무차별 경계 표본이라 부르고 할당표에 자리를 준다. B_many(모든 필드가 같은 쌍둥이)는 이 개념의 한 사례다.

여기서 실무적인 지적을 하나 더한다. `state-gen-v2` §10의 D01 예산표를 보면 SILENT 편집이 전부 B_one에 걸려 있다. 위의 이유로 2차·3차 변수의 SILENT는 B_many 쪽에서 훨씬 잘 드러난다. SILENT를 두 기준 모두에 걸고, 상한 11을 넘기면 3차 변수를 B_many 쪽에 우선 배치하기를 권한다.

### 4.3 편집 목록에 빠진 넷

`docs/GEN-ALGO.md` §3의 층 셋은 상태 공간에 대해서는 닫혀 있다. 그런데 모델이 보는 것은 상태가 아니라 툴 응답이고, 그래서 실제로는 넷째 축이 있다. 아래 넷은 지금 목록에 없거나 tier 2로 밀려 있다.

**단위와 의미 타입의 불일치.** `inventory_items.unit`을 `cans`에서 `cases`로 바꾸는 편집(GEN-V0의 P11)은 tier 2로 밀렸는데, `state-gen-v2`가 도입한 `sem_type = count(대상, 단위)` 위에서는 일급 SILENT 편집이다. q의 "100 cans"와 상태의 단위가 어긋날 때 무엇을 하는가는 조달에서 가장 현실적인 자리이고, 값 범위나 FK를 위반하지 않으므로 §4의 무의미 필터에 걸리지도 않는다. 같은 논리로 `supplier_listings`의 `currency`를 바꾸는 편집도 열린다.

**이력과 시각.** `state-gen-v2` §7을 그대로 채택하되 미입고 발주의 **상태 필드**(`status` ∈ {pending, received, cancelled})를 변형에 넣는다. 수량만 바꾸면 "또 사는가"만 묻는데, status를 함께 쓰면 "무엇을 입고로 보는가"까지 묻는다. 상시 위임에서 이 둘은 다른 질문이다.

**파생 관계를 일부러 어긋나게 둔 상태.** 지금 파생 필드는 무의미 필터 쪽에서만 쓰인다(어긋나면 버린다). 반대 방향도 있다. 재고 수량과 판매 이력이 서로 모순될 때 어느 쪽을 믿는가는 주인만 아는 자리다. 다만 "말이 되는 세계" 제약과 정면으로 충돌하므로, 모순 편집은 별도 종류로 표시하고 할당에서 1~2개로 묶어 두는 것이 안전하다.

**렌더링 층.** 같은 상태라도 조회 경로에 따라 모델이 받는 것이 다르다(§3.1). 이것은 상태 편집이 아니라 관측 편집이라 층 셋 밖이다. 최소한 표면 변형 둘(레코드 순서, ID 재명명)을 위임 절반이 아니라 전 위임 기본값으로 올리고, 논문에서는 "층 셋은 상태에 대해 닫혀 있고 무엇이 관측되는지는 툴이 정한다"로 정직하게 쓰는 편이 낫다.

여기 연결해서 한 가지만 더 말하면, `docs/GEN-ALGO.md` §3의 완전성 주장은 형식적으로 참이지만 심사자에게는 공허하게 읽힌다. 관계형 인스턴스가 셋으로 정의되니 층이 셋인 것은 정의의 반복이고, 그것이 **자리를 덮는다**는 뜻은 아니다. 문장을 "편집 공간의 닫힘"으로 좁히고, 덮는지 여부는 저자 설계 대비 회수율로만 말하기를 권한다.

### 4.4 무의미 필터는 대칭으로 검사한다

`state-gen-v2` §12의 첫 검사(GEN-V0의 편집 88개를 제약에 통과시켜 무의미한 것이 걸러지는지 보는 것)는 싸고 좋다. 반드시 반대 방향을 같이 해야 한다.

저자가 손으로 만든 여덟 상태(`docs/CASE-D01.md`)와 `state-gen-v2` §10의 할당 11개를 같은 제약에 통과시킨다. 하나라도 걸러지면 제약이 과한 것이다. 한쪽 검사만 하면 필터를 세게 잡는 쪽으로 기울고, 세게 잡힌 필터는 R5 같은 자리를 다시 지운다. `docs/DATA-GEN.md` §4.1이 기록한 실패가 정확히 그 모양이었다.

### 4.5 할당의 동결과 표면 신호

할당을 git tag로 동결한 뒤 새 위임에 적용하는 `state-gen-v2` §11의 절차는 C3가 서기 위한 최소 조건이다. 채택을 권고하고, 동결 시점에 할당표 자체(영역별 개수, SILENT 변수의 우선순위)를 사전 등록 문서로 박아 두기를 더한다.

생성기가 ID에 설계 정보를 흘리지 않게 해야 한다. 지금 CASE 상태는 업체 ID에 실행 번호가 들어 있다(R7이 SUP-071과 SUP-072). 사람이 읽기에는 편하지만 B2b처럼 여러 상태를 한 컨텍스트에 넣는 판에서는 구조가 그대로 노출된다. 생성 상태에서는 ID를 고정 풀에서 뽑고 레코드 순서를 시드로 섞는 것을 기본값으로 둔다.

둥근 숫자 문제(`Plan.md` §10 한계)도 같은 자리에서 싸게 줄인다. q 상수와의 순서 관계가 유지되는 범위에서 값에 잡음을 주면 된다(가용량 500을 487로). q 상수 자체와 경계값은 당연히 그대로 둔다.

### 4.6 씨앗 선별을 프로그램으로 내린다

`docs/DATA-GEN.md` §7의 미결 1·2는 D-035로 주선에서 빠졌지만, `state-gen-v2` §11이 지적한 대로 "반복해서 말이 되는가"는 아직 없다. 131개에 그대로 돌릴 수 있는 형태로 네 조건을 제안한다.

1. **유형 지시.** 지시문이 인스턴스(특정 날짜, 예약 번호, 일회성 식별자)가 아니라 유형(상품, 계정, 분류)을 가리킨다. 씨앗 참조 커밋의 인자가 상태에서 유일하게 결정되는지로 근사 판정할 수 있다. GEN-V0의 항공 씨앗이 여기서 떨어진다.
2. **반복 가능성.** commit 툴을 두 번 부르면 두 번 효과가 있다. 멱등이면 상시 위임이 성립하지 않는다. 환경 코드로 정적 판정이 된다.
3. **비가역성.** commit 툴에 역연산이 없다. `Plan.md` §5의 비가역성 라벨을 필터로 승격하는 것이다.
4. **3차 변수의 존재.** 닻에서 FK로 닿지만 참조 계획이 부르지 않는 비어 있지 않은 컬렉션이 하나 이상 있다. D01의 R5가 정확히 이 조건에서 나왔고, 이 조건이 없는 씨앗으로는 3차 변수 실험 자체를 못 한다.

네 조건을 131개에 돌려 남는 수를 먼저 세고, 14가 안 나오면 10/9 이전에 규모를 고친다. 반나절 작업이고, 10월에 위임 저작을 시작한 뒤에 바꾸는 것보다 훨씬 싸다.

### 4.7 개정 의사코드 (v2 대비 차이만)

```python
# ── 1. Q: 단수가 아니라 해석 집합
𝒬 = LiteralPrograms(q)          # 한 구간이 두 가지로 읽히면 갈라서 둘 다 남긴다
answers(s) = [Qi(s) for Qi in 𝒬]  # union 하지 않는다. 해석별로 보관한다
# 검증: 두 사람이 독립으로 쓴 𝒬의 answers 일치율을 보고한다

# ── 3. 기준: 무차별 경계를 추가
B_one, B_many = ...                       # 그대로
B_indiff(f) = f보다 상위 술어의 필드를 전부 동률로 맞춘 판   # 2·3차 변수의 기준

# ── 4. 편집: 넷 추가
E += [unit_mismatch(base, f)    for f in count_fields]        # 단위·통화
E += [add_own_commit(base, qty_rel=r, status=s)]              # 이력 × 상태 필드
E += [break_derived(base, f)]                                 # 모순 편집. 표시하고 1~2개만
render_variants(s) = {레코드 순서 셔플, ID 재명명}             # 전 위임 기본값
jitter(v) = q 상수와의 순서 관계를 보존하는 잡음               # 둥근 숫자 제거

# ── 6. 라벨: 상태가 아니라 쌍의 전이
region(base, edit) =
    AMBIG   if 해석마다 답이 다름
  | FILL    if 둘 다 답이 여럿
  | OPEN    if 기준은 하나, 편집은 ∅ 또는 여럿
  | CLOSE   if 기준은 ∅ 또는 여럿, 편집은 하나
  | CONTROL if 둘 다 하나인데 다름
  | SILENT  if 둘 다 같은 하나

# ── 7. 할당: SILENT를 두 기준 모두에
SILENT = one_per_var(order=V1+V2+VH+V3, bases=(B_one, B_many, B_indiff))

# ── 9. 판정: 순서를 바꾼다
1) 노출을 먼저 본다. 노출 0인 런은 의존 계산에서 빼고 '조회 행'으로 보고한다
2) outcome을 사전 등록 조대화로 굵힌 뒤 Fisher/순열
3) top-up 기준을 기준 런의 행동 엔트로피로 (관측 p가 아니라)
4) BH는 그대로. 단 C2의 주 지표는 자리 풀이 아니라 심은 자리 회수율
```

---

## 5. 관련 연구에서 지금 빠져 있는 것 둘

둘 다 오늘 실재를 확인했다. 둘 다 이 논문을 무너뜨리지는 않지만, 없으면 심사에서 반드시 지적받는다.

**"standing instructions"라는 용어는 이미 NLP에 있다.** Moghe 외, *Interpreting User Requests in the Context of Natural Language Standing Instructions*, Findings of NAACL 2024 (arXiv:2311.09796). 데이터셋 NLSI는 17개 도메인 2.4K 대화이고, 사용자 프로필에 저장된 지속적 선호 중 어느 것이 지금 요청에 적용되는지를 고르고 API 호출로 옮기는 문제다. API 예측 exact match 최고 44.7%를 보고한다. 그쪽의 standing instruction은 대화 중에 적용되는 선호라서 무인·비가역·반복이라는 이 논문의 세 조건이 없다. 그래도 제목과 용어가 정면으로 겹치므로 관련 연구에서 먼저 구분해 주는 편이 낫다. 문장 하나면 충분하고, 오히려 "상시 지시라는 개념은 이미 있었고 그것이 무인·비가역 무대로 옮겨갈 때 생기는 문제를 다룬다"로 쓰면 무대 설정이 더 선명해진다.

**q에서 절을 지우고 모델이 채우는지 보는 설계가 벤치마크 규모로 이미 있다.** *Evaluating Implicit Regulatory Compliance in LLM Tool Invocation via Logic-Guided Synthesis* (arXiv:2601.08196). 규정을 LTL로 바꿔 안전 오라클을 만들고, 제약 만족 퍼저로 실행 가능한 API 호출열을 생성하고, 정답 트레이스에서 안전 관련 연산을 가린 뒤 지시문을 합성한다. 금융·의료·IoT 세 도메인 240문항이고, 지시문을 목표형으로 주면 절차형일 때보다 준수율이 크게 떨어진다고 보고한다.

이것은 위협이면서 재료다. 위협은 "성분을 빼고 모델이 채우는지 본다"는 장치가 더 이상 새롭지 않다는 것이다. D-037이 q 변형을 모든 위임으로 넓힌 결정은 그래서 더 중요해졌고, 차별점을 문장으로 박아 두어야 한다. 그쪽은 정답이 있는 규정 준수이고 이쪽은 정답이 없는 주인의 선호다. 이 구분은 §10의 ACCORD 구분과 같은 축에 있으므로 두 문단을 하나로 묶어 쓸 수 있다.

재료로서는 둘이다. Q를 LTL 형태로 쓰면 금지 술어와 선택 술어의 구분이 그대로 형식을 얻는다(`state-gen-v2` §3의 "0은 둘이다"가 LTL에서는 자연스럽게 갈린다). 그리고 그쪽은 **커버리지를 생성기의 품질 지표로 쓴다**(안전 임계 API 커버리지 100% 대 LLM 베이스라인 80~85%). 지금 C3에는 저자 설계 대비 회수율밖에 없어서 저자 설계 상태가 없는 위임에서는 생성기를 평가할 방법이 없다. 도달 필드 커버리지, 영역별 커버리지, 변수 차수별 커버리지 같은 내부 지표를 함께 적으면 그 공백이 메워지고, 10/9 게이트의 "저자 설계의 절반 이상"이 밀렸을 때의 대체 근거도 생긴다.

덧붙여 무인 실행 자체를 다룬 최근 것들과 런타임 강제 쪽 연구가 §10의 가드 문단에 붙을 자리가 있다. 검색에서 *What Do LLM Agents Do When Left Alone?* (arXiv:2509.21224)과 런타임 강제 프레임워크(arXiv:2503.18666)가 나왔는데, 실재와 내용은 `docs/related/`의 기존 정찰 절차로 확인하기를 권한다.

---

## 6. 일정에 대한 조언

10/9 게이트의 산출물 셋(최소쌍 판독기, 언급 등급 판정기, 생성기)에서 **언급 등급 판정기를 빼고** 그 자리에 §4.1의 𝒬 이중 작성 일치율을 넣는다. `state-gen-v2`가 함축/부재를 하위 라벨로 내렸으므로 판정기는 더 이상 게이트 항목이 아니고, 게이트에 남을 신뢰도 수치는 𝒬 쪽이다.

종이 검사(`state-gen-v2` §13의 첫 단계)를 셋에서 다섯으로 늘린다. §4.4의 대칭 검사와 §4.6의 씨앗 필터를 더하는 것이고, 다섯 다 런이 필요 없어서 하루면 끝난다. 여기서 반증되는 것이 있으면 10/9 이전에 규모가 바뀌므로 순서상 가장 먼저 해야 한다.

인간 연구 둘이 11월에 겹치는 것이 가장 큰 일정 위험이다. 인간 발산 기준선은 파일럿 4개 위임으로 10월에 당기기를 권한다. ACCORD와의 구분이 여기 걸려 있어서 늦게 나오면 관련 연구를 쓸 수 없다. 비준 연구는 10월에 3명으로 예비 시행해 자극물과 소요 시간을 확정하고, 12~20명 본시행은 11월에 한 번만 돈다. D-028 때문에 판정자가 저자일 수 없고 R과 정답을 본 적이 없어야 하므로 모집 경로를 10월에 확보해 두어야 한다.

위임 14가 안 되면 위임 수를 줄인다는 방침(`Plan.md` §9)에 동의한다. 줄일 때 환경 다양성을 먼저 지킨다. 분석의 클러스터가 위임과 환경 둘인데 환경 쪽이 더 굵은 단위라서, 한 환경에서 위임을 늘리는 것으로는 CI가 좁아지지 않는다.

---

## 7. 저자가 정할 것

- [ ] Q를 해석 집합으로 바꾸고, 영역을 쌍의 전이로 다시 정의하며, AMBIG을 신설하는가 (§4.1)
- [ ] 무차별 경계 기준 상태를 할당에 넣고 SILENT를 B_many에도 거는가 (§4.2)
- [ ] 편집 넷(단위·통화, 이력 × 상태, 모순, 렌더링 변형)을 목록에 넣는가 (§4.3)
- [ ] 무의미 필터의 대칭 검사를 종이 검사에 더하는가 (§4.4)
- [ ] 씨앗 반복성 필터 넷을 131개에 돌리는가 (§4.6)
- [ ] 노출을 1급 결과로 올리고 M1′ arm을 두는가 (§3.1)
- [ ] 심은 자리 회수율을 C2의 주 지표로 올리는가 (§3.3)
- [ ] top-up 기준을 관측 p와 독립인 양으로 바꾸고 결과공간을 사전 조대화하는가 (§3.3)
- [ ] B0를 k=5, 스키마 제공, 반복 패치 판으로 세우는가 (§2.2)
- [ ] 비준 연구에 원시 트레이스 조건을 넣고, 가드의 미실행률을 함께 재는가 (§2.3, §3.5)
- [ ] 연속 실행 스터디(C1b)를 작게 두는가 (§2.1)
- [ ] CLAIM 수치를 4(+모호 1)로 정정하고 루브릭을 고정하는가 (§3.4)
- [ ] NLSI와 LogiSafetyGen을 관련 연구에 넣는가 (§5)

---

## 출처

원자료 재집계는 `data/runs/m1-d01/qwen3.8-27B-FP8/m1-k5-20260920-204318.jsonl`과 `data/runs/m1-d01/glm-4.7-flash/m1-k5-20260920-204318.jsonl`(각 40런)을 읽어 계산했다. 노출은 성공한 `read_inventory`·`search_inventory_items` 결과의 직렬화에 `quantity_on_hand`와 `PROD-001`이 함께 나온 런으로 셌고, CLAIM은 성공한 commit이 없는 런의 `final_report` 전문을 읽어 분류했다.

문헌 둘은 2026-09-22에 확인했다.

- [Interpreting User Requests in the Context of Natural Language Standing Instructions (arXiv:2311.09796)](https://arxiv.org/abs/2311.09796) · [ACL Anthology (Findings of NAACL 2024)](https://aclanthology.org/2024.findings-naacl.255/)
- [Evaluating Implicit Regulatory Compliance in LLM Tool Invocation via Logic-Guided Synthesis (arXiv:2601.08196)](https://arxiv.org/html/2601.08196v1)

추가 확인이 필요한 것.

- [What Do LLM Agents Do When Left Alone? (arXiv:2509.21224)](https://arxiv.org/html/2509.21224v1)
- [Customizable Runtime Enforcement for Safe and Reliable LLM Agents (arXiv:2503.18666)](https://arxiv.org/html/2503.18666v3)

`state-gen-v2` §14의 계보 서지 다섯은 전부 실재하는 문헌이다. Ostrand & Balcer, "The category-partition method for specifying and generating functional tests", CACM 31(6), 1988. Ammann & Offutt, *Introduction to Software Testing*(base choice coverage). McKeeman, "Differential Testing for Software", Digital Technical Journal 10(1), 1998. Dunnett, JASA 50(272), 1955(대조군에 √k 배분). Benjamini & Hochberg, JRSS-B 57(1), 1995. 쪽수와 판본은 기존 정찰 절차로 확인하면 된다.
